# 需求变更记录

## 一、变更概况

| 项目 | 内容 |
|---|---|
| 变更申请总数 | 31 |
| 已批准 | 31 |
| 待评估 | 0 |
| 已拒绝 | 0 |
| 最新更新 | 2026-09-05 |
| 当前产品阶段 | B0-B13 development `GO`。B13 最终 SHA `250cbb824ad11f4b0e11338c19494655ec384ff6` 的 development migration Run `33952031401` 与随后 rollback-only smoke Run `33952283771` 同 SHA 依次成功；B13.9 已退出，CH-029 已自动失效。B14.0-B14.1 已完成；B14.2 的 Admin Web 实现与构建已完成，认证后手工核对待执行，B14.3 尚未准入。CH-030 将当前产品/API 基线升级为 `v2.4.12 / 2.4.12-ch030`，CH-031 仅覆盖 B14.1-B14.3 的单维护者脱敏 development。B12 orphan `P2=1` 与 B13.8 已登记 P2 继续阻断 staging/真实数据；真实微信退款、staging、production、真实客户数据、真实微信、真实支付和真实物流仍为 `NO-GO` |

## 二、变更记录表

| 变更 ID | 申请日期 | 变更内容 | 来源 | 类型 | 影响程度 | 决策 | 状态记录 |
|---|---|---|---|---|---|---|---|
| CH-001 | 2026-08-10 | 两端商城调整为“总部后台 + 一级代理工作台 + 消费者小程序”三端，并新增单层代理归属、佣金和提现 | 业务调整 | 新增 + 修改 | 重大 | 已批准，纳入当前版本 | 已并入 v2.2 冻结基线；原型与技术设计已完成，生产开发未开始 |
| CH-002 | 2026-08-11 | 佣金从代理档案固定比例调整为全代理共用的“平台默认 + 一级分类默认 + SKU 覆盖”商品规则 | 用户批准的业务调整 | 修改 | 重大 | 已批准，立即更新设计基线 | 已并入 v2.2 冻结基线；原型与技术设计已完成，生产开发未开始 |
| CH-003 | 2026-08-11 | 修订推广白名单、SKU 交易、支付/退款异常、售后占用、账户手机号、隐私删除、软删除和高风险操作规则 | 用户批准的问题整改 | 新增 + 修改 | 重大 | 已批准，纳入 v2.2 设计基线 | 文档、三端原型、接口与数据库设计已完成并通过当前准入门禁，生产开发未开始 |
| CH-004 | 2026-08-11 | 数据库由 MySQL 调整为 Supabase 托管 PostgreSQL；Supabase 仅承担数据库托管 | 用户批准的技术选型调整 | 修改 | 重大 | 已批准，纳入 v2.3 设计基线 | 产品与技术设计已同步并通过当前准入检查；业务功能与原型不变，迁移与生产开发未开始 |
| CH-005 | 2026-08-11 | 修复支付恢复、单包裹发货、售后计价/验货、退款双轴、并发裁决、管理员 TOTP、高风险矩阵、商品生命周期和指标口径缺口 | 用户批准的全量准入审计整改 | 新增 + 修改 | 重大 | 已批准，立即升级 v2.4 | 产品、三端原型、生成报告、API/OpenAPI、PostgreSQL 设计、Prisma 与首迁移已同步并通过研发准入；业务开发尚未开始 |
| CH-006 | 2026-08-13 | 修复 B3 品牌排序、主数据生命周期、创建状态和文件上传完整性/存储边界的冻结契约阻塞 | 技术限制 | 修改 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | B3.0 至 B3.3 已通过各自本地/一次性环境验收并暂停；B3.3 本地 `GO` |
| CH-007 | 2026-08-14 | GitHub Actions 与 Supabase development rollback-only 远端证据延期，不阻塞整个 B3 的本地/一次性环境逐段实施 | 开发优先级调整 | 修改交付门禁 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | CH-007 收口时 B3 整体 `NO-GO`；后续证据与状态见 CH-009 |
| CH-008 | 2026-08-14 | 更正文件下载签名 GET 被误写为 HASH_ONLY 的幂等语义 | 技术契约勘误 | 澄清，不改变线协议 | 低 | 技术负责人批准，随 B3.1 立即执行 | 无幂等记录与 `READ_SENSITIVE` 审计已验证；路径、参数、响应、统计、数据库结构均不变 |
| CH-009 | 2026-08-24 | 单人维护项目无法配置独立 GitHub Environment reviewer，以开发阶段补偿控制替代该组织条件 | 团队资源约束 | 修改交付门禁 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | B3/B4 development 证据均已闭合；例外到 B4 development 结束即耗尽，staging/production 仍关闭 |
| CH-010 | 2026-08-24 | 修复 B4 商品/SKU 无重新启用路径、创建状态、最低活动价、图集、库存初值和归档依赖等冻结契约阻塞 | 技术限制 | 修改需求 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | B4.0 至 B4.4 已验收暂停；B4 development `GO`，staging/production `NO-GO` |
| CH-011 | 2026-08-25 | 单人维护项目在 B5 development 继续以自动化与远端证据补偿独立 reviewer 缺失 | 团队资源约束 | 修改交付门禁 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | B5.4 已完成，例外自动失效；不适用于后续 development/staging/production |
| CH-012 | 2026-08-25 | 修复 B5 Banner 生命周期/幂等、库存低库存口径、HR-07 preview 与流水类型等冻结契约阻塞 | 技术限制 | 修改需求与接口契约 | 中等 | 产品负责人 + 技术负责人已批准，已执行 | B5.0 至 B5.4 已完成；B5 development `GO` |
| CH-013 | 2026-08-25 | 单人维护项目在 B6 development 继续以自动化、准确 SHA 双绿和无残留证据补偿独立 reviewer 缺失 | 团队资源约束 | 修改交付门禁 | 中等 | 产品负责人 + 技术负责人已批准，已执行 | B6.4 已完成，例外自动失效；不适用于后续 development/staging/production |
| CH-014 | 2026-08-25 | 闭合 B6 匿名商城目录的售罄投影、综合排序、搜索、首页分区降级与公共限流契约 | 技术限制 | 修改需求与接口契约 | 中等 | 产品负责人 + 技术负责人已批准，已执行 | B6.0-B6.4 已完成；B6 development `GO` |
| CH-015 | 2026-08-26 | 单人维护项目在 B7 development 继续以自动化、同 SHA 双绿和无残留证据补偿独立 reviewer 缺失 | 团队资源约束 | 修改交付门禁 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | B7.5 已完成；最终 SHA `3f844bfb9866854ceedb975ad0dc4fd7cacfb04a` 同 SHA 双绿，例外自动失效 |
| CH-016 | 2026-08-26 | 闭合消费者法律文本、微信登录/CUSTOMER 会话、资料手机号、归因候选、服务代理与同步账号注销契约 | 技术限制 | 修改需求与接口契约 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | B7.0-B7.5 已完成；B7 development `GO` |
| CH-017 | 2026-08-27 | 单人维护项目在 B8 development 继续以自动化、同 SHA 双绿和无残留证据补偿独立 reviewer 缺失 | 团队资源约束 | 修改交付门禁 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | B8.5 已完成；最终 SHA `0fc5a8d3d1f07d3b5c9fcadf7ea4ca9560a0911a` 同 SHA 双绿，例外已自动失效 |
| CH-018 | 2026-08-27 | 闭合登录后收藏、服务端购物车、游客购物车合并、收货地址及对应小程序页面契约 | 技术限制 | 修改需求与接口契约 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | B8.0-B8.5 已完成；最终 SHA `0fc5a8d3d1f07d3b5c9fcadf7ea4ca9560a0911a` 双绿，B8 development `GO` |
| CH-019 | 2026-08-28 | 单人维护项目在 B9 development 继续以自动化、准确 SHA 双绿和无残留证据补偿独立 reviewer 缺失 | 团队资源约束 | 修改交付门禁 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | B9.5 已完成；最终 SHA `19f9ad57190b28d11922db805b39af95b2f7ba3b` 同 SHA 双绿，例外已自动失效 |
| CH-020 | 2026-08-28 | 闭合 B9 结算报价、下单、库存预占、取消与超时释放契约，并以 0002 前向索引迁移解决 B5-C09 | 技术限制 | 修改需求 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | 产品/API 基线为 v2.4.7/2.4.7-ch020；B9.0-B9.5 已完成，B9 development `GO` |
| CH-021 | 2026-08-29 | 单人维护项目在 B10 脱敏 development 继续以自动化、同 SHA 远端证据和无残留断言补偿第二 reviewer 缺失 | 团队资源约束 | 修改交付门禁 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | B10.6 已在最终 SHA `f5e59169b53a97704711c3aae3049e5b5d16a930` 取得同 SHA 远端双绿，失效条件已闭合，CH-021 自动失效；从未适用于 staging/production |
| CH-022 | 2026-08-29 | 闭合 Mock 支付、支付结果消费、关单对账、迟到支付自动退款及支付事实唯一索引契约 | 技术限制 | 修改需求与接口契约 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | 产品/API 基线保持 v2.4.8/2.4.8-ch022；B10.0-B10.6 已完成，最终 SHA `f5e59169b53a97704711c3aae3049e5b5d16a930` 同 SHA 双绿，B10 development `GO` |
| CH-023 | 2026-08-30 | 修复不可变佣金快照触发器 `FOR SHARE` 与 `mall_runtime` 最小权限冲突，新增 0004 前向函数修复迁移 | 技术限制 | 修改需求 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | 不改变 v2.4.8/CH-022 在线契约；0004 与完整 `0001→0004` 迁移历史、20 个 partial indexes、`migration diff=0` 及最终 B10 同 SHA 远端双绿均已闭合 |
| CH-024 | 2026-08-30 | 冻结 B11 一单一包裹、总部人工物流、Store 本人物流/确认收货、Admin 订单/履约地址/发货/物流/兜底完成，以及首次完成时佣金一次入账 | 业务调整 | 修改需求 | 重大 | CCB 已批准，立即执行 | B11.0-B11.5 与最终远端同 SHA 双绿均已完成，零迁移；B11 development `GO` |
| CH-025 | 2026-08-31 | 单人维护项目在 B11.1-B11.5 脱敏 development 继续以自动化、逐批暂停、最终同 SHA 双绿和无残留证据补偿第二 reviewer 缺失 | 团队资源约束 | 修改交付门禁 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | B11 最终同 SHA 双绿已闭合，随 B11.5 自动失效；不适用于 staging/production/真实客户/真实支付/真实物流 |
| CH-026 | 2026-09-01 | 闭合 B12 普通售后、退货验货、普通退款、库存回库、佣金冲正与金额补偿契约，并增加退款事实防护迁移 | 技术限制 | 修改需求 | 重大 | CCB 已批准，立即执行 | B12.0 已在 SHA `03a1e9f98bea0926887fb68354ca602566148f6a` 取得 CI、development migration、rollback-only smoke 三项成功证据并完成退出 |
| CH-027 | 2026-09-01 | 单人维护项目在 B12.1-B12.6 脱敏 development 以自动化、逐批暂停、远端证据和无残留断言补偿独立 reviewer 缺失 | 团队资源约束 | 修改交付门禁 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | B12.1-B12.6 已完成；最终 SHA `8c3589af…` 的 CI `33592754575` 与 rollback-only smoke `33594513127` 同 SHA 双绿，例外已自动失效，不适用于 staging/production/真实数据/真实资金 |
| CH-028 | 2026-09-02 | 进入 B13 一级代理经营、归属、佣金、钱包和提现闭环；同步契约、配置与前向 `0006_b13_agent_finance_guards` | 产品路线图/技术限制 | 新增需求与接口契约 | 重大 | CCB 已批准，立即执行 | 目标基线 `v2.4.11 / 2.4.11-ch028`；B13.0 治理已建立，业务实现按 B13.1-B13.9 分批准入；不适用于真实资金、staging/production |
| CH-029 | 2026-09-02 | 单人维护项目在 B13.1-B13.9 脱敏 development 以自动化、逐批暂停、同 SHA 远端双绿和无残留断言补偿第二 reviewer 缺失 | 团队资源约束 | 修改交付门禁 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | 仅覆盖 B13.1-B13.9、Mock Provider 和脱敏 development；B13.9 后自动失效，不适用于 staging/production/真实数据/真实资金 |
| CH-030 | 2026-09-05 | B14 ADM-02 经营分析契约最小修正；保持既有路径和 operationId，闭合概览、日/月销售与商品/客户排行线协议 | 技术契约修正 | 修改需求与接口契约 | 重大 | CCB 已批准，立即执行 | 基线升级为 `v2.4.12 / 2.4.12-ch030`；B14.0 已准入；不新增迁移、Worker、图表依赖或 GitHub CI |
| CH-031 | 2026-09-05 | 单维护者在 B14.1-B14.3 脱敏 development 以 2 个定向基础测试、受影响构建、逐批暂停和最终同 SHA 远端证据补偿第二 reviewer 缺失 | 团队资源约束 | 修改交付门禁 | 中等 | 产品负责人 + 技术负责人已批准，立即执行 | 仅覆盖 B14.1-B14.3；不建立 GitHub CI；最终 migration attestation 与 rollback-only smoke 成功后自动失效，不适用于 staging/production/真实数据 |

说明：CH-001 至 CH-020 各节保留其批准当时的历史表述；CH-017、CH-019、CH-021、CH-025、CH-027 已分别随 B8.5、B9.5、B10.6、B11.5、B12.6 最终同 SHA 双绿自动失效。CH-026 将产品/API 基线升级为 `v2.4.10 / 2.4.10-ch026`；B12 最终 SHA `8c3589af…` 的普通 CI `33592754575` 与 Supabase rollback-only smoke `33594513127` 同 SHA 双绿，B12 development `GO`。CH-028 将基线升级为 `v2.4.11 / 2.4.11-ch028`；B13 最终 SHA `250cbb824ad11f4b0e11338c19494655ec384ff6` 的 development migration `33952031401` 与 rollback-only smoke `33952283771` 同 SHA 依次成功，B13 development `GO`，CH-029 已自动失效。CH-030 将当前基线升级为 `v2.4.12 / 2.4.12-ch030`；CH-031 仅替代 B14.1-B14.3 脱敏 development 无法配置第二 reviewer 的组织条件。已失效例外不得沿用，任何 development 例外均不适用于 staging/production/真实客户/真实支付/真实退款/真实物流。

## 三、CH-001 变更详情

### 3.1 变更申请

- **申请日期**：2026-08-10
- **申请来源**：业务负责人明确调整
- **变更类型**：新增需求与原有需求修改
- **紧急程度**：高，必须在开发前完成范围纠偏

变更前产品只有消费者小程序和总部管理后台，后台采用两类内部权限，渠道推广与结算能力尚未纳入当前范围。

变更后产品为三个终端和三类人员：

1. 总部管理后台，角色 `SUPER_ADMIN`。
2. 一级代理工作台，角色 `AGENT_ADMIN`，属于外部推广合作方。
3. 消费者微信小程序，角色 `CUSTOMER`。

游客仍为未登录状态，不新增人员角色。原总部最高权限映射为 `SUPER_ADMIN`，原总部内部普通操作角色从当前范围移除。

### 3.2 业务边界

- 代理严格单层，由超级管理员创建，不存在下级代理。
- 总部统一商品、零售价、库存、收款、发货、退款和售后。
- 代理仅推广和只读查看本人脱敏客户、订单、售后进度、佣金与提现。
- 代理不能改价、改库存、代客下单、发货、退款或处理售后。
- 未绑定游客保存 30 分钟有效候选；只有客户端携带有效旧候选 token 时新候选才替换该事实，未携带时旧候选自然过期；登录后确认成功才建立长期绑定，拒绝则清空候选并保持直营。
- 超级管理员可转移到其他 ACTIVE 代理或 DIRECT，但只影响转移后提交的新订单，历史订单与佣金快照不变。
- 提交订单只记录归因候选，支付成功时复核代理 ACTIVE 后最终冻结；支付前停用转 DIRECT，支付后停用不影响已冻结 EXPECTED。
- 佣金按订单项净商品实付金额和支付时有效比例计算；具体比例来源由后续已批准的 CH-002 统一商品规则定义。
- 确认收货或总部兜底完成后，佣金立即转为可提现，不设置额外等待期。
- 完成前全退将 EXPECTED 标记为 CANCELLED；完成后保留 AVAILABLE 并追加 REFUND_DEBIT；余额可为负并阻止提现。
- 提现由代理申请、总部审核、线下银行转账、上传凭证并标记已支付；完整收款账号默认掩码，仅付款环节二次验证后短时查看/复制并全量审计。
- 代理默认授权全部当前及未来在售商品，超级管理员可切换自定义白名单。
- ADM-16 配置默认 100 元最低提现与默认 7 天售后申请期；ADM-02 纳入日/月销售、商品销量和客户消费排行。

### 3.3 影响评估

| 影响维度 | 级别 | 具体影响 |
|---|---|---|
| 产品范围 | 重大 | 新增完整一级代理工作台及总部代理/提现管理 |
| 信息架构 | 重大 | 两端变三端，新增 AGT 页面体系，扩展 MP 与 ADM 页面 |
| 权限模型 | 重大 | 旧版后台角色改为三类人员角色，并增加代理数据范围控制 |
| 核心流程 | 重大 | 新增开通、推广、绑定、佣金、退款冲正、提现和归属转移 |
| 数据概念 | 重大 | 新增代理、绑定历史、订单代理快照、佣金账本、钱包、银行卡与提现概念 |
| API 边界 | 重大 | 增加代理端授权边界、总部财务动作与行级数据隔离要求 |
| 用户体验 | 重大 | 新增代理 PC/H5；消费者增加绑定确认；总部增加代理和财务页面 |
| 安全隐私 | 重大 | 增加手机号/地址脱敏、银行卡加密、越权防护和财务审计 |
| 测试范围 | 重大 | 增加三端 E2E、佣金/提现状态、并发、幂等和数据隔离测试 |
| 数据迁移 | 无当前存量 | 开发未开始，不涉及线上数据迁移；后续设计以最新 PRD v2.2 为基线 |
| 开发进度 | 待重新估算 | 旧两端排期不可继续沿用，设计评审后重新拆分 |
| 成本预算 | 待评估 | 增加代理端、财务审计、安全和测试工作量 |

**综合影响程度**：重大。该变更涉及三个以上核心模块、角色模型、交易归属和财务账本，必须先冻结设计再开发。

### 3.4 风险评估

| 风险 | 等级 | 控制措施 |
|---|---|---|
| 把“二级代理端”误解为多级代理 | 高 | 统一命名“一级代理工作台”，禁止父子关系和团队页面 |
| 代理越权查看隐私或其他代理数据 | 高 | 服务端行级授权、字段最小化、脱敏和安全测试 |
| 比例规则或状态变化造成错误归因 | 高 | 提交时记录候选，支付时复核 ACTIVE 并冻结当时规则来源、比例与佣金快照 |
| 完成后退款导致已提现佣金倒挂 | 高 | 追加负向账本、允许负余额、阻止新提现、未来抵扣 |
| 审核通过被误认为已经打款 | 高 | `APPROVED` 与 `PAID` 分离，付款凭证必填 |
| 付款环节完整收款账号泄露 | 高 | 加密存储、默认掩码、二次验证、短时申请级授权、自动遮蔽与审计 |
| 代理停用影响历史结算 | 中 | 阻止新业务但保留历史账本和已有提现审核 |
| 角色术语残留造成错误开发 | 高 | 对 MVP、PRD、原型和验收进行全文一致性检查 |

### 3.5 变更决策

- **审批结果**：已批准。
- **执行时机**：纳入当前版本，先完成需求与原型设计。
- **审批依据**：业务负责人明确要求三端和三类人员，并确认单层代理、归属、佣金、退款冲正与线下提现规则。

批准条件：

- 不进入多级代理、代理进货、独立商户或自动打款。
- 不复用 `AGENT_ADMIN` 作为总部内部普通员工。
- 设计文档和原型评审通过前不开始生产开发。
- 技术设计不得绕过订单快照、不可变账本、隐私脱敏和审计要求。
- 归因候选、支付最终冻结、停用/重启、商品授权与规则配置必须按最新 PRD v2.2、CH-002 与 CH-003 口径实现。
- 固定销售报表属于当前版本，Excel 导出和自定义报表中心仍延期。

### 3.6 执行计划

| 步骤 | 交付物 | 状态 |
|---|---|---|
| 更新需求基线 | MVP 方案、三端角色与代理需求确认 | 已完成 |
| 更新产品方案 | PRD v2.0、页面编号、流程、状态、规则和验收 | 已完成 |
| 更新原型方案 | 三端原型设计方案与 Figma 重建规范 | 已完成 |
| 完成三端交互原型 | 消费者、一级代理、总部联动页面 | 已完成并通过自动验收 |
| 需求/设计评审 | 业务、产品、设计共同确认 | 已按 CH-003 冻结基线 |
| 技术设计 | 架构、ERD、API、OpenAPI、Prisma 草案与部署拓扑 | 已完成 |
| 开发与测试 | 三端工程与后端实现 | 未开始 |

### 3.7 验收条件

- 三端、三类人员和登录入口一致，无旧版后台角色残留。
- 单层代理边界清楚，无下级、团队和多级分佣入口。
- 消费者 30 分钟候选覆盖、确认/拒绝、总部转移和支付时历史快照规则可演示。
- 代理端商品、客户和订单均只读且脱敏。
- 默认全部在售与自定义白名单两种代理商品授权模式可演示。
- 收货即入账、完成前全退 CANCELLED、完成后 REFUND_DEBIT、负余额和提现冻结可演示。
- 提现审核与付款分离，完整账号受控查看可审计，已支付需要付款凭证。
- 最低提现、售后天数和固定销售报表可配置/查看并覆盖空错状态。
- PRD、原型方案、页面编号和可点击原型一致。

## 四、CH-002 变更详情

### 4.1 变更申请

- **申请日期**：2026-08-11
- **申请来源**：用户明确提出并批准的业务规则调整
- **变更类型**：修改现有佣金配置与计算需求
- **影响程度**：重大
- **执行时机**：设计阶段立即实施，开发前完成基线替换

变更前，佣金比例配置在代理档案上，同一个代理推广的全部商品使用同一比例。这不能表达不同品类、商品规格的毛利和推广策略差异。

变更后，所有一级代理共用一套由商品维度决定的百分比佣金规则：

1. 平台默认比例提供兜底。
2. 商品所属一级分类可配置分类默认比例。
3. SKU 可配置覆盖比例。
4. 解析优先级固定为 `SKU 覆盖 > 商品一级分类默认 > 平台默认`。
5. 空值表示继承，`0%` 表示明确无佣金并终止继承。
6. 现有商品一级分类直接作为佣金分类来源，不另建运营分类体系。
7. 不再在代理档案维护佣金比例，所有一级代理在同一规则版本下推广相同 SKU 时采用相同比例。

### 4.2 业务边界

- 佣金基数继续使用订单项净商品实付金额，首期不含运费。
- 单个订单项佣金 = `订单项净商品实付金额 × 支付时有效比例 / 100`；订单佣金为各订单项逐项舍入后的金额之和。
- 支付成功时逐订单项冻结规则版本、命中来源、一级分类、商品、SKU、比例、基数和佣金金额。
- 待付款订单按支付成功时有效规则结算；规则修改、商品移动分类或 SKU 覆盖清空不追溯已支付订单。
- 常规退款按原订单项支付快照比例和实际成功退款金额冲减。
- 同一订单项累计冲减不得超过原佣金；最后一笔使订单项全额退清时，直接冲减剩余佣金以消除舍入尾差。
- 保留 `EXPECTED`、`CANCELLED`、`AVAILABLE`、`REFUND_DEBIT` 生命周期，以及不可变流水、负余额、提现限制和线下付款规则。
- 当前版本排除代理专属比例、“代理 × SKU”、固定金额、销量/金额阶梯和限时活动佣金。

### 4.3 影响评估

| 影响维度 | 级别 | 具体影响 |
|---|---|---|
| 产品规则 | 重大 | 佣金从代理维度改为平台/分类/SKU 商品维度，并增加继承和 0% 语义 |
| 信息架构 | 重大 | 总部新增 ADM-22 佣金规则管理，代理页面移除独立比例并增加 SKU 预计佣金/订单项快照 |
| 数据概念 | 重大 | 新增规则版本、规则目标、命中来源和订单项佣金快照；代理档案不再承担比例真相 |
| API 边界 | 重大 | 新增规则查询/写入/版本解释；支付与退款必须逐订单项使用快照，拒绝客户端比例 |
| 用户体验 | 中 | 总部增加继承、0%、影响预览和版本审计；代理看到当前预计值与支付快照 |
| 测试范围 | 重大 | 增加优先级、空值/0%、多 SKU、分类移动、支付并发、分次退款和尾差用例 |
| 数据迁移 | 无当前存量 | 开发未开始，无线上订单迁移；技术设计直接以 PRD v2.2 为基线 |
| 开发进度 | 待重新估算 | 需在技术拆分时新增规则解析与版本快照工作量 |

**综合影响程度**：重大。该变更同时影响总部配置、代理展示、支付结算、退款冲正、数据模型、接口和测试，不能作为普通字段调整处理。

### 4.4 风险与控制

| 风险 | 等级 | 控制措施 |
|---|---|---|
| 空值与 0% 混淆 | 高 | 接口和页面明确区分 `null` 继承与 `0` 无佣金，并设置专项验收 |
| 多级配置命中错误 | 高 | 固定 `SKU > 一级分类 > 平台默认`，支付保存命中来源与规则版本 |
| 规则更新污染历史 | 高 | 规则版本只追加；支付逐订单项快照；历史计算只读快照 |
| 商品移动分类引发争议 | 中 | 只影响移动后支付的订单，已支付订单保留原分类和规则快照 |
| 多 SKU 使用平均比例 | 高 | 逐订单项计算、展示和退款，订单层只汇总金额 |
| 分次退款舍入不平 | 高 | 累计封顶原订单项佣金，尾笔全退冲清剩余金额 |
| 配置范围膨胀 | 中 | V1 明确排除代理维度、固定金额、阶梯和活动佣金 |

### 4.5 变更决策

- **审批结果**：已批准。
- **批准人**：业务负责人/用户。
- **执行范围**：MVP、角色确认、PRD v2.1、原型设计方案、可点击 HTML 原型、文档生成报告和本变更记录。
- **执行限制**：CH-002 批准时只更新前期设计与演示原型；后续 CH-003 已单独批准进入接口和数据库技术设计，正式前端与后端业务开发仍未开始。

批准条件：

- 所有一级代理共用同一规则，不保留代理档案固定比例的兼容口径。
- 百分比配置范围、金额精度和舍入方式在技术设计阶段固定，但不得改变本文优先级与退款尾差原则。
- ADM-22、订单项快照、退款冲正和版本审计必须同时进入后续技术设计，不能只增加一个后台输入框。
- 现有佣金状态、负余额和提现流程不得因本次变更弱化。

### 4.6 执行计划与状态

| 步骤 | 交付物 | 状态 |
|---|---|---|
| 更新上游需求 | 三端角色确认、MVP v2.1 | 已完成 |
| 更新产品基线 | PRD v2.1，新增 ADM-22、FR、AC、实体与接口边界 | 已完成 |
| 更新原型说明 | 三端原型设计方案中的页面、状态与 Figma 画板 | 已完成 |
| 更新文档审计 | 文档生成报告、计数与全文一致性检查 | 已完成 |
| 同步可点击原型 | 代理端移除固定比例、总部新增 ADM-22 | 已完成并纳入 v2.2 总体验收；统一佣金关键交互通过专项验证 |
| 技术设计 | ERD、规则解析契约、API 与 Prisma 草案 | 已完成 |
| 迁移、开发与测试实现 | 数据库迁移、后端与三端工程、自动化测试 | 未开始 |

### 4.7 验收条件

- 全文不存在“每代理固定/专属佣金比例”作为当前规则的表述。
- ADM-22 覆盖平台默认、一级分类默认、SKU 覆盖、空值继承、0% 无佣金、影响预览和版本审计。
- 代理推广中心可表达各 SKU 当前预计佣金；订单佣金详情可表达支付时订单项快照。
- 多 SKU 订单按项使用不同比例并正确汇总，不出现订单平均比例。
- 支付后规则或分类变化不修改历史；不同代理在同一规则版本推广同一 SKU 比例一致。
- 分次退款按原订单项快照冲减，累计不超原佣金，尾笔全退无舍入尾差。
- `EXPECTED`、`CANCELLED`、`AVAILABLE`、`REFUND_DEBIT`、负余额和提现规则保持完整。

## 五、CH-003 变更详情

### 5.1 变更申请

- **申请日期**：2026-08-11
- **申请来源**：用户要求对全部文档和三端原型进行逻辑、功能与技术准入审计，并批准整改方案
- **变更类型**：新增需求与现有交易、佣金、隐私和风控规则修改
- **影响程度**：重大
- **执行时机**：开发前立即实施，形成 v2.2 设计基线

审计发现 v2.1 在商品白名单计佣语义、SKU 购物车、待付款订单、支付超时并发、未发货退款、售后并发额度、账户手机号来源、邀请码轮换、软删除、账号删除和高风险操作交互上存在未决或缺失。若直接进入接口与数据库设计，将导致交易状态、库存、佣金和隐私模型出现不可兼容分支。

用户批准以下五项核心决策：

1. 自定义商品白名单只限制代理查看和生成推广物料；已绑定客户购买全店任意 SKU 均按统一商品佣金规则计佣。
2. 客户账户手机号独立、可空且由消费者自愿授权，不从收货地址手机号提取。
3. 未发货退款成功后自动回补对应 SKU 库存；全额退款终止履约。
4. 订单超时后的迟到支付自动原路退款，不恢复订单、库存、履约或佣金。
5. 售后申请提交时立即占用订单项可退数量和金额。

### 5.2 业务边界

- 购物车、订单项、库存预占、发货、售后和退款统一以 `sku_id` 为最小单位；同商品不同 SKU 不合并。
- 提交订单即创建 `PENDING_PAYMENT`、库存预占和 `pay_expires_at`；稍后支付、支付失败或取消均保留订单，可在未过期时继续支付。
- 超时任务先关闭支付意图和订单，再释放库存。关单后支付成功记录为迟到支付并自动原路退款；失败进入人工财务异常，绝不恢复履约和佣金。
- 订单、支付、退款和履约采用独立状态轴，接口统一返回四状态、`close_reason` 和 `display_status`。
- 未发货部分退款自动回库退款数量并只发剩余数量；未发货全额退款后订单 `CLOSED`、`close_reason=FULL_REFUND_BEFORE_SHIPMENT`、履约 `CANCELLED`。
- 售后申请事务性占用可退数量/金额，待处理数量不得发货；驳回或允许阶段取消释放占用，`REFUND_FAILED` 保留占用供幂等重试。
- 原订单项佣金快照和正向流水不可修改；完成前退款追加预计减少/取消流水，完成后退款追加 `REFUND_DEBIT`。
- 白名单撤权、商品下架或邀请码轮换后，旧推广链接可打开普通商品页但不得建立新候选；已有绑定、历史订单和全店计佣资格不变。
- 已登录未绑定消费者打开有效代理链接时立即确认；邀请码支持轮换、停用和有效期，旧码立即失效但不影响既有绑定。
- 账户手机号保存验证来源、时间和协议版本；支付脱敏快照只保存当时账户手机号尾号且不追溯补写。
- 品牌、分类、商品和 SKU 采用软删除；存在在售商品的分类禁止停用，须先迁移或下架。
- 消费者可申请删除账号；有未完成订单或售后时阻断，满足条件后撤销会话、结束绑定并去标识化资料，交易与审计按合规策略保留。
- 代理停用、客户转移、邀请码轮换、提现/售后拒绝、退款和佣金规则变更必须包含原因、影响预览、二次确认、防重复和审计反馈。
- 完整收款账号仅在 `APPROVED` 提现显示；二次验证签发绑定管理员与申请的 60 秒单次授权，连续失败 5 次锁定 15 分钟。

### 5.3 影响评估

| 影响维度 | 级别 | 具体影响 |
|---|---|---|
| 产品规则 | 重大 | 重定义白名单、支付时限、退款回库、售后占用、手机号和删除规则 |
| 页面与交互 | 重大 | 补齐小程序登录/支付/订单/物流/售后/地址/收藏/隐私页面，总部缺失管理页和异常状态 |
| 状态模型 | 重大 | 订单、支付、退款、履约四轴分离；新增迟到支付异常与 `REFUND_FAILED` |
| 数据概念 | 重大 | 新增 SKU 行键、库存预占、支付尝试/回调、包裹项、售后占用、手机号验证、删除请求和财务异常 |
| API 边界 | 重大 | 创建订单、支付回调、退款、售后并发、邀请码轮换、手机号和银行卡短时授权均需新契约 |
| 财务与佣金 | 重大 | 退款不得修改原快照，迟到支付不得生成佣金，白名单不参与计佣 |
| 安全隐私 | 重大 | 手机号来源隔离、账号删除、高风险操作保护和银行卡锁定策略 |
| 测试范围 | 重大 | 增加并发、幂等、状态轴、动态记录绑定、交互和敏感数据专项测试 |
| 数据迁移 | 无当前存量 | 开发未开始，无线上迁移；技术设计直接以 v2.2 建模 |

**综合影响程度**：重大。该变更跨交易、库存、售后、渠道、佣金、隐私与财务安全，属于接口和数据库设计的前置准入条件。

### 5.4 风险与控制

| 风险 | 等级 | 控制措施 |
|---|---|---|
| 同商品不同 SKU 串项 | 高 | 购物车、订单、库存、发货和售后统一使用 `sku_id` |
| 超时释放库存后迟到支付 | 高 | 先关支付意图再释放；迟到成功只自动退款并告警 |
| 并发售后超退或误发 | 高 | 申请时事务占用数量/金额，待处理数量从可发数量扣除 |
| 未发货退款库存未恢复 | 高 | 退款成功按 SKU 自动回库，全退关闭履约 |
| 退款污染历史佣金 | 高 | 快照和账本只追加，禁止更新原金额 |
| 白名单被误用为计佣资格 | 高 | 明确仅控制推广，支付链路不读取白名单 |
| 地址手机号被当作账户手机号 | 高 | 独立实体和写接口，地址接口无权写账户手机号 |
| 敏感或高风险动作缺乏追责 | 高 | 原因、影响、二次确认、幂等、锁定和审计统一门禁 |

### 5.5 变更决策

- **审批结果**：已批准。
- **批准人**：业务负责人/用户。
- **执行范围**：MVP v2.2、三端角色确认、PRD v2.2、原型设计方案、三端可点击原型、文档生成报告和本记录。
- **执行限制**：先完成需求与原型整改及验证，再进入接口文档、ERD、MySQL 表和 Prisma 模型设计；不在本阶段实现生产业务代码。

批准条件：

- 白名单、手机号、支付异常、售后占用和库存回库不得留给开发阶段临时决策。
- 金额使用 `DECIMAL(18,2)`、佣金比例使用 `DECIMAL(7,4)`，订单项按 `HALF_UP` 两位舍入后汇总。
- 支付/退款回调采用收件箱幂等，领域事件采用事务 Outbox；关键外部回调和写操作必须有幂等键。
- 页面 ID、FR、AC、状态枚举和原型交互通过一致性检查后，方可冻结 v2.2 并进入技术设计。

### 5.6 执行计划与状态

| 步骤 | 交付物 | 状态 |
|---|---|---|
| 更新需求基线 | MVP v2.2、三端角色确认 | 已完成 |
| 更新产品基线 | PRD v2.2 的页面、FR、状态、规则、接口边界、AC 与测试 | 已完成 |
| 更新原型规范 | 缺失页面、异常状态、动态记录和高风险交互要求 | 已完成 |
| 同步三端可点击原型 | 小程序、代理端、总部端关键流程与状态 | 已完成 |
| 文档与原型验证 | 编号、术语、关键交互、响应式、破图、敏感明文和业务状态 | 已通过；75 个渲染视图、8 条小程序流程、6 条管理/代理流程 |
| 接口与数据库设计 | 技术架构、API 文档、OpenAPI、ERD、MySQL 约束与 Prisma 模型 | 已完成冻结稿，尚未执行迁移 |
| 生产开发 | 三端工程与后端业务实现 | 未开始 |

### 5.7 验收条件

- 不同 SKU 的购物车、结算、库存、订单快照、售后和退款完全一致且不串项。
- 稍后支付、支付取消、30 分钟超时、迟到成功自动退款和失败转人工异常可验证。
- 四条状态轴、未发货部分/全额退款、自动回库和剩余数量发货规则一致。
- 并发售后不得超数量/金额，允许阶段取消/驳回释放占用，`REFUND_FAILED` 保留并可重试。
- 白名单外购买仍计佣，撤权旧链接不建候选，原佣金快照和正向流水不可变。
- 账户手机号自愿、可空且与地址隔离；账号删除资格、会话撤销和去标识化可验证。
- 邀请码轮换、软删除、分类停用阻断和所有高风险操作具备完整保护与审计。
- 银行卡明文仅在正确状态和 60 秒单次授权下出现，失败锁定和离页清除有效，日志/截图无敏感明文。
- 三端缺失页面、动态抽屉和代表性加载/空/错误/权限/冲突/重复/成功状态全部覆盖。

## 六、CH-004 变更详情

### 6.1 变更申请

- **申请日期**：2026-08-11
- **申请来源**：用户明确调整数据库技术选型
- **变更类型**：基础设施与数据库引擎选型修改
- **影响程度**：重大
- **执行时机**：业务代码开发和首次数据库迁移前，形成 v2.3 技术基线

用户决定将当前设计中的 MySQL 改为 Supabase 托管 PostgreSQL。该变更只调整数据库引擎和托管方式，不改变三端业务范围、人员角色、页面、交互、功能需求、用户故事或验收标准。

### 6.2 选型与使用边界

- 数据库采用 Supabase 托管的 PostgreSQL，Prisma 继续作为后端 ORM 和迁移入口。
- Supabase 仅提供 PostgreSQL 托管能力；当前不采用 Supabase Auth、Supabase Storage、Realtime 或面向客户端的 Data API/PostgREST。
- 消费者小程序、一级代理工作台和总部管理后台只能访问 NestJS 后端 API，不得持有数据库连接、匿名 key、service role key 或其他可直接访问 Supabase 数据面的凭据。
- 微信授权登录、管理员/代理账号登录、会话、RBAC、代理数据范围和高风险操作授权继续由既有后端认证授权模块承担。
- 商品图片、Banner、售后凭证和付款凭证继续使用既有对象存储抽象，不因 CH-004 切换至 Supabase Storage。
- 业务事实、状态、金额精度、幂等、Inbox/Outbox、审计、软删除和隐私规则保持 CH-002/CH-003 口径，不因托管平台改变。

### 6.3 影响评估

| 影响维度 | 级别 | 具体影响 |
|---|---|---|
| 业务功能 | 无 | 三端功能、用户故事、FR、AC、页面与原型统计不变 |
| 数据库引擎 | 重大 | MySQL 方言、枚举、生成列、唯一约束和迁移 DDL 需改为 PostgreSQL 语义 |
| ORM | 中 | Prisma 保留，但 datasource、字段映射、索引和 migration 需按 PostgreSQL 复核 |
| API | 低 | 三端 API 业务契约不变；不得新增客户端直连 Supabase 的旁路 |
| 认证与权限 | 无业务变化 | 不采用 Supabase Auth；服务端 RBAC 与行级数据范围继续作为唯一授权入口 |
| 文件存储 | 无 | 不采用 Supabase Storage，现有对象存储边界不变 |
| 部署与运维 | 中 | 增加 Supabase 项目、区域、连接池、备份恢复、密钥和网络访问配置 |
| 数据迁移 | 无线上存量 | 生产开发和首次迁移未开始，不涉及 MySQL 存量数据搬迁；只需重写设计与首版 migration |
| 原型与统计 | 无 | 不新增或删除页面，FR/AC/US 和自动验收数量不变 |

**综合影响程度**：重大。业务表面无变化，但数据库引擎切换会影响所有 DDL、事务和并发约束，必须在首次迁移和业务代码开发前完成技术契约复核。

### 6.4 风险与控制

| 风险 | 等级 | 控制措施 |
|---|---|---|
| 直接照搬 MySQL DDL | 高 | 全量复核 enum、部分唯一索引、生成列、CHECK、时间和自增/ID 语义，输出 PostgreSQL migration |
| 三端绕过后端直连 Supabase | 高 | 禁止客户端 Data API/PostgREST/Realtime 与数据库凭据；所有业务读写只经 NestJS |
| 误引入 Supabase Auth/Storage 扩大范围 | 高 | CH-004 明确只采购数据库托管，认证与文件边界保持原设计 |
| 连接池或事务模式配置错误 | 高 | 在技术设计中明确 Supabase 连接方式、Prisma 连接池和 migration 直连通道并进行并发验证 |
| 托管平台凭据泄露 | 高 | 数据库密码和 service role 类凭据仅存后端 Secret；日志、前端包和仓库禁止出现 |
| 区域时延或数据驻留不合规 | 高 | 生产区域须经过真实用户网络延迟测试及隐私、数据驻留、跨境传输合规评审，完成前不得写入真实敏感数据 |
| 厂商绑定或恢复能力不足 | 中 | 使用标准 PostgreSQL/Prisma migration，明确备份、时间点恢复与导出恢复演练 |

### 6.5 变更决策

- **审批结果**：已批准。
- **批准人**：业务负责人/用户。
- **当前基线**：MVP/PRD v2.3、CH-001 至 CH-004。
- **执行限制**：本次只更新前期产品与技术设计；不得据此开始三端或后端业务代码开发，也不得创建生产数据库迁移。

批准条件：

- CH-001 至 CH-003 的业务决策和历史版本保持不变，CH-004 仅覆盖当前数据库选型。
- 技术设计中的 MySQL 专属类型、DDL、约束、事务说明和部署拓扑必须转换为 PostgreSQL/Supabase 口径并重新校验。
- Supabase 的认证、存储、Realtime 和客户端数据 API 不进入当前范围；如未来需要，必须提交新的需求变更。
- 技术契约通过一致性、Prisma、PostgreSQL migration 和安全边界检查后，方可另行申请进入开发。

### 6.6 执行计划与状态

| 步骤 | 交付物 | 状态 |
|---|---|---|
| 更新产品基线 | MVP v2.3、角色确认、PRD v2.3、原型方案基线 | 已完成 |
| 保持功能与原型范围 | 页面、FR、AC、US、流程及验收统计 | 无变化 |
| 更新变更记录 | CH-004 决策、边界、风险和门禁 | 已完成 |
| 复核技术设计 | 架构、API、ERD、数据库约束、Prisma 与部署说明改为 PostgreSQL/Supabase | 已完成并通过当前静态校验 |
| 数据库迁移与生产开发 | 首版 migration、后端和三端业务实现 | 未开始，需单独批准 |

### 6.7 验收条件

- 当前产品文档统一使用 v2.3/CH-004，v2.2/CH-003 仅作为历史业务基线保留。
- 当前数据库选型统一为 Supabase 托管 PostgreSQL，不再把 MySQL 作为当前实现目标。
- 文档明确 Supabase 只承担 PostgreSQL 托管，三端无直连，且不采用 Supabase Auth、Storage、Realtime 或客户端 Data API。
- 三端、三角色、业务规则、页面 ID、FR、AC、US 与原型验收统计无变化。
- 生产业务代码、数据库迁移和部署均未开始。

## 七、CH-005 变更详情

### 7.1 变更申请

- **申请日期**：2026-08-11
- **申请来源**：用户要求再次浏览全部产品、原型、接口和数据库设计，并明确批准解决准入审计发现的 P1/P2 缺口
- **变更类型**：新增需求与交易、售后、风控、状态、交互和验收规则修改
- **影响程度**：重大
- **执行时机**：开发前立即实施，形成 v2.4 产品基线；生产业务开发仍未获准开始

审计发现 v2.3/CH-004 仍存在以下不可留给开发临时决定的问题：单包裹与部分发货口径冲突；售后客户端金额边界不清；佣金百分比公式缺少 `/100` 与上限；支付 Provider 未知结果不可恢复；取消/超时/支付并发未串行；退款成功进度与失败处理混为一轴；退货地址、验货异常和已发货全退主状态未定义；绑定、转移、停用、完成/退款竞态未裁决；管理员二次验证未固定为 TOTP；高风险动作要求互相矛盾；商品生命周期、看板指标、单项上限和首个管理员恢复缺失；原型仍存在静态记录和不可执行异常样例。

用户将本次整改视为重大修改并批准立即执行，要求先修订需求文档和原型规范，再同步接口与数据库，仍不得进入生产代码开发。

### 7.2 已冻结业务决策

1. 首期严格单商户、单仓、单包裹。任一未发货订单项存在活动售后占用，整单不得创建或确认发货；全部占用释放或成功退款后，仅按最终剩余数量创建唯一包裹。
2. 商品售后客户端只提交订单项、数量、类型、原因、说明和凭证，不提交金额。服务端按订单项成交/优惠分摊快照、成功退款和活动占用计算金额；尾笔使用剩余可退金额清尾差。纯金额补偿使用总部专属 `AMOUNT_COMPENSATION`，不占数量、不回库但占金额并冲正佣金。
3. 佣金公式固定为 `基数 × 比例 / 100`，按订单项 `HALF_UP` 保留两位后汇总；比例使用 `DECIMAL(7,4)`，范围 `0.0000%-100.0000%`（含边界），超范围拒绝。
4. 支付意图先落库稳定 `intent_no + CREATING` 再调用 Provider；`CREATING/OPEN/CLOSE_PENDING` 视为活动状态且每单最多一个。创建/关闭结果未知按 intent_no 查询恢复，不得盲目新建或提前释放库存；对账任务负责补记、关闭、补偿和人工财务异常升级。
5. 用户主动取消、超时任务和支付确认使用同一订单锁/版本号串行。支付先提交则关单失败；关单先提交则后到成功为迟到支付并自动退款。活动意图未确认不可支付前不得释放库存。
6. 退款拆为 `refund_progress_status=NONE/PARTIAL/FULL` 与 `refund_processing_status=IDLE/REFUNDING/FAILED`。前者仅按成功退款派生，后者表达处理/异常；允许 `PARTIAL + FAILED` 并优先提示失败。
7. ADM-16 维护唯一当前总部退货地址，审核通过时保存不可变地址快照。总部验货请求一次精确覆盖全部售后项，逐项记录退货物流、实收、批准退款数量、验货结果 `PASS/ABNORMAL`、证据，以及回库、损坏、报废、退回客户四类处置；逐项满足“批准退款 + 退回客户 = 实收”“批准退款 = 回库 + 损坏 + 报废”。`PASS` 仅允许全量实收/全量批准且不退回客户，可零证据；少件、0 件或不符必须 `ABNORMAL` 且至少一份证据，两类证据清单均在提交时封存。异常只允许一次把类型化 `resolution` 写为 `CONTINUE_REFUND` 或 `REJECT_AFTER_RETURN`，继续退款原因必填且只能使用冻结批准数量，解决事实不可改写。已发货全额退款以 `COMPLETED + completion_reason=FULL_REFUND_AFTER_SHIPMENT` 结束，原包裹、物流和签收事实不回退。
8. 并发裁决固定为：两个绑定确认首个提交胜出；客户转移与订单提交按绑定版本先提交者决定候选；代理停用与支付按代理版本先提交者决定 `AGENT/DIRECT`；订单完成与退款按订单项佣金快照串行并保证两种顺序净结果一致。
9. 邀请码、代理或候选失效时只把归因降级为 `DIRECT`，不得阻断消费者浏览、下单或支付。撤权旧推广链接不再建候选，但可作为普通商品链接打开。
10. 账号删除除未完成订单/售后外，还阻断未结清支付、退款和财务异常；完成后撤销会话、结束绑定/候选，清空微信主体与资料，硬删除手机号验证、地址、收藏和购物车项的密文/HMAC/尾号/密钥标识，代理投影仅留不可逆代号；交易与审计事实按外部合规策略保留且不再通过当前资料接口关联原主体。
11. 品牌、分类、商品和 SKU 软删除。存在在售商品的品牌/分类禁止停用；商品/SKU 存在活动预占时禁止软删除。普通下架阻止新加购/结算，但下架前合法预占订单可在 `pay_expires_at` 前支付；库存始终满足 `实物 >= 锁定 >= 0`。
12. 代理端昵称只使用服务端脱敏值；当前城市取最近一笔本人归属已支付订单，无订单显示“暂无”。历史订单使用支付时订单城市快照，账号删除后显示不可逆客户代号且无手机号尾号。
13. 首个超级管理员只由受控部署命令创建。管理员使用 30 秒 TOTP，允许前后各一个时间步并防重放；连续失败 5 次锁定 15 分钟，恢复码一次一用且只保存哈希。密码/TOTP 重置撤销全部会话；全凭据丢失只允许双人审批离线恢复。
14. PRD 13.4 的 `HR-01` 至 `HR-15` 为唯一高风险操作矩阵，分别定义原因、影响预览/二次确认、TOTP/凭证、版本、幂等和审计；不再把同一组要求一刀切到所有动作。
15. 当前 MVP 删除消费者订单留言/买家备注；总部内部审计备注不受影响。
16. 每 SKU 单笔订单上限固定为 `min(99, 服务端可售库存)`。看板明确区分今日创建订单/今日有效支付订单、客户总数/新增注册/新增绑定和周期活跃代理，全部按 Asia/Shanghai 与事实流水复算。
17. 页面数量保持 `21 MP + 9 AGT + 22 ADM`。退货地址与 TOTP 放入 ADM-16，退货验货/金额补偿放入 ADM-13；不新增页面。代表性 Loading、Empty、网络错误、403、409、重复提交和 Success 必须连接真实操作。
18. v4 仅澄清既有范围的角色投影，不新增页面或功能：Store、Agent、Admin 的列表/详情使用角色专用 DTO，不以宽字段通用响应交由前端隐藏。Store 目录只含 `ACTIVE` 商品/SKU 和消费者字段；Store 订单列表含商品/SKU、售后摘要与服务端可用动作；Agent 客户仅当前有效绑定和当前归属期口径，订单仅支付成功时冻结本人快照并含商品/城市/售后/完整只读状态轴。
19. ADM-03 商品列表增加既有库存能力的 SPU 聚合展示，含 SKU 数和实物/锁定/可售摘要并下钻 SKU 流水；ADM-18/19 代理列表/详情展示当前客户、归属净销售、余额和创建时间，并可按目标代理下钻客户、支付归属订单、佣金流水、钱包流水、提现与目标审计，不增加新的经营权限。
20. `pay_expires_at` 由数据库固定为订单 `created_at + 30 分钟` 并在创建后不可修改；ADM-16 只读展示，任何角色或配置均不能延长/缩短既有订单期限。
21. 临时密码、TOTP 绑定 URI/恢复码、邀请码明文、银行卡查看授权、预览/确认能力令牌和签名文件 URL 均为一次展示的短时响应，统一 `Cache-Control: no-store`；刷新、离页、消费或到期后不得从缓存或普通详情接口恢复。
22. 消费者在本人已通过退货审核的售后详情读取总部退货地址快照，属于售后履约所需的授权地址例外，与消费者本人收货地址/订单和总部用途绑定履约地址并列；授权响应禁止缓存，代理端、列表、日志、错误、审计摘要和自动化截图仍不得出现详细地址。

### 7.3 状态与并发影响

| 领域 | CH-005 当前口径 |
|---|---|
| 支付意图 | `CREATING`、`OPEN`、`CLOSE_PENDING`、`CLOSED`、`SUCCEEDED`、`FAILED`、`CANCELLED`、`EXPIRED`；前三者活动且唯一 |
| 退款 | 成功进度 `NONE/PARTIAL/FULL` 与处理状态 `IDLE/REFUNDING/FAILED` 分离 |
| 售后 | 增加 `RETURN_EXCEPTION`、`REJECTED_AFTER_RETURN`，保留 `REFUND_FAILED` 占用；验货结果 `PASS/ABNORMAL` 与异常 `resolution=CONTINUE_REFUND/REJECT_AFTER_RETURN` 分离；金额补偿为独立类型 |
| 订单完成 | `completion_reason` 至少包含 `CUSTOMER_CONFIRMED`、`ADMIN_FORCED`、`FULL_REFUND_AFTER_SHIPMENT` |
| 履约 | 单包裹；活动售后占用整单阻断；发货后退款不回退履约事实 |
| 归因 | 代理失效降级 `DIRECT`；绑定/转移/停用/支付按明确版本串行 |
| 佣金 | 原快照和正向流水不可变；完成/退款并发的净结果顺序无关 |

### 7.4 影响评估

| 影响维度 | 级别 | 具体影响 |
|---|---|---|
| 产品规则 | 重大 | 重定义支付恢复、售后金额、单包裹、退货验货、退款双轴、并发和账号安全 |
| 页面与交互 | 重大 | 页面数不变，但 MP-09/13/14、AGT-01/04/07/08/09、ADM-03 至 08/10 至 13/16/19 至 22 均需真实交互升级 |
| 状态模型 | 重大 | 新增支付意图恢复状态、退款双轴、验货异常、完成原因和并发裁决 |
| 数据概念 | 重大 | 需要稳定支付意图号、退货地址版本/快照、验货处置、管理员 TOTP/恢复码、完成原因与并发版本 |
| API 边界 | 重大 | 订单取消、支付查询/关闭/对账、服务端售后试算、验货处置、TOTP、受控卡号和高风险预览/确认契约变化 |
| 数据库约束 | 重大 | 活动意图唯一、退款双字段、活动预占发货门禁、库存不变量、规则比例 CHECK、不可变版本/流水均需 PostgreSQL 约束 |
| 安全隐私 | 重大 | TOTP/恢复、全会话撤销、代理历史去标识化和卡号单次明文生命周期 |
| 报表 | 中 | 指标口径冻结并与注册、绑定、支付/退款事实分离 |
| 测试范围 | 重大 | 增加 Provider 未知结果、五类竞态、单包裹、验货异常、双状态轴、TOTP 和矩阵专项测试 |
| 数据迁移 | 无线上存量 | 生产迁移未开始，可直接调整首版 PostgreSQL 设计，无历史线上数据转换 |
| 开发进度 | 准入已解除 | 可点击原型和技术契约已同步并复验；可开始 Mock/开发/测试范围实施，生产上线仍受外部门禁约束 |

**综合影响程度**：重大。该变更同时影响产品、交互、支付、库存、履约、售后、佣金、隐私、管理员认证、API、数据库与测试，必须作为完整 v2.4 基线一次性同步。

### 7.5 风险与控制

| 风险 | 等级 | 控制措施 |
|---|---|---|
| Provider 未知结果产生双支付或提前释放 | 高 | 稳定 intent_no、活动意图唯一、`CLOSE_PENDING`、查询恢复与对账升级 |
| 售后金额被客户端篡改或并发超退 | 高 | 服务端快照计价、事务占用数量/金额、尾笔清差和幂等 |
| 单包裹订单在售后中被部分误发 | 高 | 任一活动占用整单门禁，发货前预览最终包裹项 |
| 退款失败覆盖已成功部分退款 | 高 | 成功进度与处理状态分离，退款失败不回退进度或改库存/佣金 |
| 退货地址变化或验货异常污染历史 | 高 | 审核地址快照、实收/验货/处置不可变记录，回库独立流水 |
| 完成与退款或归因操作竞态 | 高 | 指定串行化对象、版本控制、先提交者裁决、净结果一致验收 |
| 代理失效阻断正常消费 | 高 | 归因失败统一降级 DIRECT，支付与履约由总部继续 |
| TOTP 丢失被绕过或管理员永久锁死 | 高 | 一次性恢复码、全部会话撤销、双人离线恢复，不提供普通绕过接口 |
| 高风险页面要求互相矛盾 | 高 | PRD HR-01 至 HR-15 为唯一矩阵，页面和接口只引用不另写规则 |
| 旧 HTML/接口/数据库被误认为已同步 | 已关闭 | CH-005 原型、契约与迁移已重新生成和复验，文档只引用本轮实测统计 |

### 7.6 变更决策

- **审批结果**：已批准。
- **批准人**：业务负责人/用户。
- **CH-005 批准时产品版本**：v2.4 / CH-005。
- **执行范围**：MVP、三端角色确认、PRD、原型设计方案、可点击原型、接口/OpenAPI、Supabase PostgreSQL ERD/表设计、Prisma 草案和变更记录。
- **执行结果**：产品文档、可点击原型、文档生成报告和技术契约已由分离任务同步，并使用当前产物完成重新验收。
- **开发限制**：允许使用 Mock Provider、开发/测试数据库和脱敏数据进入业务开发；真实资金、生产 Supabase、完整银行账号与对外发布仍不允许。

### 7.7 执行计划与状态

| 步骤 | 交付物 | 当前状态 |
|---|---|---|
| 更新需求基线 | MVP v2.4、三端角色确认 | 已完成 CH-005 文字修订 |
| 更新产品基线 | PRD v2.4、FR/AC/US、状态、矩阵与测试 | 已完成 CH-005 文字修订；唯一 FR 142、AC 116、US 24 |
| 更新原型规范 | 三端页面/交互与 Figma 重建规范 | 已完成 CH-005 文字修订；页面仍为 21/9/22 |
| 同步可点击原型 | `product-materials/prototype/` 三端 HTML/JS 与自动验收 | 已完成；78 个渲染、21/9/22 页面契约与 14+16 关键流程连续 3 轮通过 |
| 更新生成报告 | 文档计数与整体一致性报告 | 已完成 v2.4/CH-005 统计与准入证据更新 |
| 同步接口与数据库 | API 文档、OpenAPI、ERD、PostgreSQL 表/约束、Prisma | 已完成；172/196 契约、76/59 模型和首迁移通过 |
| 一致性与准入验证 | 页面/FR/AC/US/状态/接口/模型、关键并发、敏感明文 | 已完成；P0/P1 准入阻断为 0 |
| 业务开发 | 三端工程、后端、迁移与部署 | CH-005 批准时状态：B0、B1 已通过，B2/B3 尚未开始；当前状态见第十节，B3.1 已完成本地验收并暂停 |

### 7.8 验收门禁

- 页面数保持 MP 21、AGT 9、ADM 22；唯一 FR 142、AC 116、US 24，无重复定义，页面/功能/验收引用一致。
- 同 SKU 上限、不同 SKU 快照、价格、库存、预占、订单、发货、售后与回库一致且并发不超卖。
- 数据库固定且不可改的 `pay_expires_at=created_at+30 分钟`、稳定 intent_no、活动意图唯一、创建/关闭未知恢复、主动取消/超时/支付串行、迟到支付退款与对账补偿全部可验证。
- 商品售后请求无金额，服务端计价/占用正确；金额补偿独立；任一活动占用整单阻断发货；退款失败不释放占用。
- 退货地址快照、退货物流、实收、PASS 零证据/ABNORMAL 至少一份且两类清单封存、类型化一次性异常解决、继续退款必填原因、回库和已发货全退完成原因全部可追溯。
- 退款进度与处理状态可组合，订单完成/退款、绑定确认、转移/订单、停用/支付竞态均按冻结裁决得到唯一且净值一致结果。
- 代理失效只降级 DIRECT；三端角色专用投影、Store `ACTIVE` 目录、Agent 当前绑定/已支付归属、Admin 库存与代理目标下钻均无越权或口径旁路。
- 服务端脱敏昵称、城市来源、手机号独立和删除后的历史去标识化均无隐私旁路；短时凭据/能力/签名 URL 均 no-store 且一次展示，本人售后总部退货地址例外禁止缓存并不进入列表、日志或截图。
- 比例边界、`/100` 公式、订单项 HALF_UP、规则版本、退款尾差和不可变佣金账本复算一致。
- 首个管理员、TOTP ±1 时间步/防重放、失败锁定、恢复码、会话撤销、双人离线恢复和银行卡 60 秒单次授权通过安全验收。
- HR-01 至 HR-15 在页面、API 与审计中逐项一致；不适用原因/TOTP/凭证不被错误强制。
- 看板指标按 Asia/Shanghai 和支付/退款/注册/绑定事实复算；买家备注无当前入口。
- 可点击原型重新通过关键点击、动态记录绑定、状态转换、高风险校验、Loading/Empty/Error/403/409/重复/Success、响应式、破图和敏感明文检查。
- API、OpenAPI、ERD、PostgreSQL 约束和 Prisma 与 v2.4 状态/字段/事务/幂等规则一致后，方可由负责人作出开发准入决定。

## 八、CH-006 变更详情

### 8.1 变更申请

- **申请日期**：2026-08-13
- **变更来源**：技术限制，B3 文件、品牌与分类在实现前发现冻结契约无法完整承载已批准需求
- **变更类型**：修改现有需求与接口契约，不增加页面、FR、AC、operation 或数据模型
- **影响程度**：中等，影响 B3 文件域、品牌/分类主数据域、总部后台交互与相应回归测试
- **审批级别**：产品负责人 + 技术负责人
- **审批结果**：已批准
- **执行时机**：立即执行，先完成 B3.0 契约解阻，验收后暂停

阻塞事实如下：品牌创建/更新契约缺少 `sort_order`，无法满足 ADM-05 排序；品牌/分类共用的 `LifecycleAction` 只有停用和软删除，无法表达恢复后的重新启用，且直接扩展该共享类型会错误改变 B4 商品/SKU 契约；创建请求允许直接 ACTIVE，绕过统一生命周期预览；文件上传意图没有预期 SHA-256，无法在上传完成时建立端到端内容完整性校验；对象公开范围、签名 TTL、清理时限和完成幂等缓存策略也未冻结。

### 8.2 已批准契约决策

1. `BrandCreateRequest.sort_order` 为必填非负整数；`BrandUpdateRequest.sort_order` 为可选非负整数，与一级分类对称。品牌和分类列表固定按 `sort_order ASC,id ASC`。
2. 品牌、分类创建继续携带 `initial_status`，但值固定为 `DRAFT`，不得创建即启用。
3. 新增品牌/分类专用的闭合 `MasterDataLifecycleAction`，动作仅为 `ACTIVATE/DEACTIVATE/SOFT_DELETE`，原因 2-500 字符；商品/SKU 继续使用原 `LifecycleAction`，不得获得 ACTIVATE。
4. 状态机固定为：非删除 `DRAFT/INACTIVE -> ACTIVATE -> ACTIVE`；非删除 `ACTIVE -> DEACTIVATE -> INACTIVE`；非删除 `DRAFT/INACTIVE -> SOFT_DELETE -> ARCHIVED + deleted_at`；`ACTIVE` 不得直接软删除。
5. ACTIVATE、DEACTIVATE、SOFT_DELETE 均先 preview，再以相同请求、未过期 token、新幂等键和 `If-Match` 确认。非法或同状态的新请求返回 409；同幂等键精确重放仍按公共幂等规则返回原结果。
6. restore 仅适用于软删除的 ARCHIVED 记录，保持现有“原因 + `If-Match` + 幂等”接口，不新增 restore-preview；成功后清除 `deleted_at`、状态固定回到 `DRAFT`、版本加一。重新启用必须另走 ACTIVATE preview/confirm。
7. 默认列表排除软删除记录；显式 `status=ARCHIVED` 仅返回软删除归档记录；按 ID 详情允许读取 ARCHIVED 以支持恢复。
8. 品牌/分类存在 ACTIVE 商品时，DEACTIVATE 和 SOFT_DELETE 的 preview 仍返回 200 并列出影响，confirm 以 `ACTIVE_PRODUCT_DEPENDENCY` 422 阻断。软删除业务键全局保留，再次创建返回 `SOFT_DELETED_KEY_RESERVED` 409。
9. `UploadIntentRequest` 增加必填 `sha256`，格式为 64 位小写十六进制；七类 purpose 统一仅接受 `image/jpeg`、`image/png`，单文件最大 5 MiB。
10. complete 请求的 SHA-256 和 size 必须同时匹配 intent 与服务端读取对象后的 MIME、魔数、大小和 SHA-256 实测值，不一致返回 `FILE_CONTENT_MISMATCH` 422。
11. 上传签名有效 15 分钟，私有下载签名有效 5 分钟；PENDING/staging 满 24 小时才进入清理候选。对象键固定使用 staging/public/private 分区且不包含原文件名。
12. bucket 默认私有，只允许 `public/*` 匿名 GET；售后、提现和推广 QR 始终使用 `private/*`，不得由客户端或 purpose 旁路改变可见性。
13. 不新增 `completed_at` 或 `updated_at` 数据库列。complete 成功事实由 `status=READY`、最终对象 key、服务端实测内容、审计与幂等记录表达；首次 `completed_at` 只保存在闭合 `FILE_UPLOAD_COMPLETE` 响应及其幂等正文，同键同请求精确重放，新键重复 complete 返回 409。
14. 上传意图和 lifecycle preview 使用 `HASH_ONLY`，不得把签名 URL、请求头或 preview token 保存到幂等响应正文；下载签名 GET 的幂等语义以 CH-008 勘误为准，不创建幂等记录。

### 8.3 影响评估

| 影响维度 | 级别 | 具体影响 |
|---|---|---|
| 产品范围 | 轻微 | 不新增能力，只把 ADM-05/06 与文件安全边界变为可实现契约 |
| API 契约 | 中等 | 请求字段、专用生命周期 DTO、错误码、状态说明和文件约束发生兼容性收紧 |
| 前端交互 | 中等 | 创建固定草稿、显示排序、启用也需预览、归档筛选与恢复后草稿状态需要同步 |
| 技术架构 | 中等 | 文件完成校验、对象分区、短时签名与 HASH_ONLY 幂等策略需冻结 |
| 数据模型/迁移 | 无 | 现有 Brand/Category/FileAsset/IdempotencyRecord 字段足够；Prisma 和首迁移逐字节不变 |
| 测试范围 | 中等 | 增加状态矩阵、依赖阻断、归档恢复、MIME/魔数/哈希、TTL、匿名访问和幂等回归 |
| 开发进度 | 中等 | 在 B3 业务实现前增加 B3.0 契约小批次，完成后暂停验收 |
| 生产影响 | 无当前存量 | 尚无 B3 业务客户端或生产数据，不提供旧请求兼容分支 |

### 8.4 风险与控制

| 风险 | 等级 | 控制措施 |
|---|---|---|
| 扩展共享 LifecycleAction 意外改变商品/SKU | 高 | 新建主数据专用 DTO，并用契约测试证明 Product/SKU 拒绝 ACTIVATE |
| ACTIVE 直接软删或恢复即启用绕过依赖检查 | 高 | 固定状态机；创建/恢复落 DRAFT；启用单独 preview/confirm |
| preview 后依赖或版本变化 | 高 | token 绑定 actor/session/action/target/request/version；confirm 重查依赖和 `If-Match` |
| 伪造 MIME、截断文件或同名内容替换 | 高 | intent 预期哈希 + complete 服务端 MIME/魔数/大小/SHA-256 四重校验 |
| 签名 URL 或 preview token 泄漏 | 高 | 15/5 分钟 TTL、no-store；有幂等键的 intent/preview 使用 HASH_ONLY；下载 GET 不建幂等记录；日志和敏感扫描兜底 |
| 公开 bucket 泄漏私有证据 | 高 | bucket 默认私有，仅 `public/*` 匿名 GET，私有 purpose 固定落 `private/*` |
| 清理任务误删已完成对象 | 高 | 仅扫描满 24 小时候选，删除前二次确认无 READY 记录或业务引用 |
| 局部实现被误认为 B3 已完成 | 高 | B3.1/B3.2 只登记本地/一次性环境验收，B3.3 只登记本地收口状态；明确 B2/B3 Supabase 专项与 GitHub 受保护证据仍未完成 |

### 8.5 变更决策与边界

- **批准人**：产品负责人、技术负责人。
- **CH-006 当时产品版本**：v2.4.1 / CH-006；OpenAPI 目标版本 `2.4.1-ch006`。现行版本见 CH-016。
- **执行范围**：变更记录、MVP、三端角色确认、PRD、原型规范、可点击原型、文档报告、API/OpenAPI、架构/数据库说明、生成 contracts 与 B3 开发管理文档。
- **不变范围**：52 页面（21/9/22）、唯一 FR 142、AC 116、US 24、172 paths、196 operations、76 models、59 enums。
- **数据库限制**：不新增迁移，不修改 Prisma 逻辑模型，不覆盖或重写 `0001_initial` 的 RLS、CHECK、触发器和条件唯一索引。
- **开发限制**：CH-006 原始决策要求 B3.0 完成并汇报后暂停；扩展后的 CH-007 放行 B3.1 至 B3.3 在本地和一次性环境逐段实施。B3.1 至 B3.3 现已通过各自本地/一次性环境验收并暂停；CH-007 不自动放宽远端风险边界。
- **最终验收限制**：B2 rollback-only 原定负责放行 B3.1，其时序已由 CH-007 延期，但证据要求未取消；B3.3 本地验收后还须在受保护的 Supabase development 对 B3 repository 执行独立 rollback-only smoke，并确认无测试数据、DDL、权限或 migration history 残留。缺少任一远端证据不得标记 B3 整体完成。

### 8.6 执行计划与当前状态

| 步骤 | 交付物 | 当前状态 |
|---|---|---|
| B3.0 文档基线 | 本变更单及产品、原型、API、架构、数据库、风险和开发管理文档 | 已完成 |
| B3.0 契约 | OpenAPI `2.4.1-ch006`、生成 contracts、错误码与闭合 schema | 已通过 172/196/196/306 与独立契约审计 |
| B3.0 原型同步 | 创建草稿、排序、启用预览、归档筛选、恢复草稿及移除无契约字段 | 已通过 84 个渲染与 30 条交互流 |
| 冻结数据库核对 | Prisma/schema/首迁移哈希、validate、空库回放、权限、migration diff | 已通过；冻结文件未改，diff 为零 |
| B2 云端前置门禁 | Supabase development repository rollback-only smoke | 已在 GitHub Actions Run `32678252828`、B3 实现基准 SHA `a03ed8475198e81708df4a8fd9f44995c53c86f9` 通过 |
| B3.1 文件基础 | NestJS/Worker/MinIO 文件实现与测试 | 本地/一次性环境验收通过并暂停；安全审计 P0/P1=0；Supabase rollback-only 已通过 |
| B3.2 品牌与分类 | CRUD、排序、状态机、预览确认、恢复和依赖保护 | 本地/一次性环境验收通过并暂停；安全审计 P0/P1=0 |
| B3.3 总部后台 | ADM-05/06 文件上传和主数据交互、五视口 E2E | 本地验收通过并暂停，`GO`；专用 E2E `26 passed / 4 designed skips` |

### 8.7 B3.0 验收条件

- OpenAPI 契约专项已实测为 172 paths、196 operations、196 个唯一 operationId、306 schemas、685 schema refs、2,561 local refs、0 dangling refs；该单项通过不替代其余 B3.0 门禁。
- Brand create/update 的排序、品牌/分类创建 DRAFT、专用 ACTIVATE 状态机、ARCHIVED 查询与 restore-to-DRAFT 在 PRD、原型、API 和生成类型中一致。
- Product/SKU 继续只接受 DEACTIVATE/SOFT_DELETE，ACTIVATE 契约请求必须失败。
- 文件契约完整覆盖 SHA-256、JPEG/PNG、5 MiB、15/5 分钟 TTL、24 小时清理、public/private 前缀、内容不匹配 422 和完成幂等策略。
- Redocly、生成漂移、闭合 schema、统计、Prisma validate、冻结文件检查和 migration diff 全部通过后，才能标记 B3.0 完成。
- B3.0 完成后已暂停并汇报；扩展后的 CH-007 随后对 B3.1 至 B3.3 调整了远端证据时序。本地 PostgreSQL、Redis、MinIO 仍不能替代 B2/B3 云端 rollback-only 或 GitHub 受保护运行证据。
- 后续状态：B2/B3 rollback-only 已由 B3 实现基准 SHA 的 GitHub Actions Run `32678252828` 补齐；CH-009 登记后续纯文档差异，不把未运行的新业务代码并入该证据。

## 九、版本与冻结说明

- PRD v1.0 作为两端历史版本归档，不再作为后续开发依据。
- MVP v2.4.12、PRD v2.4.12 与 CH-030 是 B14.0 已批准的当前产品/API 基线，OpenAPI 为 `2.4.12-ch030`；CH-028/v2.4.11 作为 B13 历史基线、CH-026/v2.4.10 作为 B12 历史基线、CH-024/v2.4.9 作为 B11 历史基线继续保留，CH-023/0004 作为数据库修复继续有效。页面编号仍沿用 21/9/22，唯一 FR、AC 与 US 统计以产品文档实测为准。
- CH-001 至 CH-005 作为历史批准记录保留；与 B3 品牌、分类和文件细节冲突时以 CH-006 为准，其他领域继续继承 CH-005。
- CH-006 不改变数据库结构；产品文档、可点击原型、OpenAPI、生成 contracts 和一致性门禁均已完成，B3 契约已重新冻结。
- CH-010 不改变数据库结构、path 或 operation；它以商品/SKU 专用生命周期 DTO 取代旧共享 `LifecycleAction`，并冻结 B4 创建、恢复、图集、价格投影和库存初值。Prisma 与 `0001_initial` 必须逐字节不变。
- CH-012 不改变数据库结构、path 或 operation；它闭合 Banner 创建/启停/归档恢复、库存公式、HR-07 preview-confirm、整数边界、流水类型和幂等策略。Prisma 与 `0001_initial` 必须逐字节不变。
- CH-014 不改变数据库结构、path 或 operation；它在现有 Store 读契约上闭合售罄投影、排序、搜索、首页分区降级和匿名限流。Prisma 与 `0001_initial` 必须逐字节不变。
- CH-016 在冻结数据库上新增法律文本 GET，并以注销 preview + 同步 confirm 取代旧 current GET；它闭合三份法律文本、Store 会话、单 AppID/openid 身份、资料手机号、归因候选、服务代理与同步注销。Prisma 与 `0001_initial` 必须逐字节不变。
- CH-018 在冻结数据库上新增单商品收藏状态 GET，并闭合收藏、服务端购物车、游客购物车合并、收货地址、个性化限流与 no-store 契约；Prisma 与两份 `0001_initial` 必须逐字节不变，不新增 migration。
- CH-020 在既有五个 Store 报价/订单 operation 上闭合报价 HMAC 绑定、库存预占、待付款订单、主动取消和超时释放；两份 `0001_initial` 继续逐字节不变，允许新增只包含预占/流水索引的 `0002_b9_inventory_fact_indexes`。
- B3.0 至 B3.3、B4.0 至 B4.4 与 B5.0 至 B5.4 development 已完成。B5 实现基准 `d97c43958142eaa0fa5a0a9954bb21d136944ba2` 的普通 CI Run `32822780209` 与 Supabase rollback-only Run `32823898006` 均成功且 `head_sha` 一致。CH-009 与 CH-011 均已耗尽；staging 和 production 继续关闭，生产上线另需微信、恢复演练、外部独立复核和合规评审。
- B6.0-B6.4 已完成：CH-013/CH-014 治理、产品/API/原型契约、冻结数据库、repository/API、MP-01 至 MP-06、真实纵向、全量回归、无残留及同 SHA 双绿均已闭合。CH-013 已自动失效，B6 development `GO`。
- B7.0-B7.5 已完成：最终实现 SHA `3f844bfb9866854ceedb975ad0dc4fd7cacfb04a` 的普通 CI Run `33055090596` 与 Supabase development rollback-only Run `33056078437` 均成功且 `head_sha` 一致；B7 development `GO`，CH-015 已自动失效。
- B8.0-B8.5 已完成：最终实现 SHA `0fc5a8d3d1f07d3b5c9fcadf7ea4ca9560a0911a` 的普通 CI Run `33141704459` 与 Supabase development rollback-only Run `33142971501` 均成功且 `head_sha` 一致；B8 development `GO`，CH-017 已自动失效。
- B9.0-B9.5 已完成：最终实现 SHA `19f9ad57190b28d11922db805b39af95b2f7ba3b` 的普通 CI Run `33230769777` 与 Supabase development rollback-only smoke Run `33233087710` 均为 `completed/success` 且 `head_sha` 一致；最终复审为 `P0=0/P1=0/P2=1`，B9 development `GO`，CH-019 已自动失效。唯一非阻断 P2 为大量正常到期订单存在时完整性扫描工作量仍未有界。
- B10.0-B10.6 已完成：最终实现 SHA `f5e59169b53a97704711c3aae3049e5b5d16a930` 的[普通 CI Run `33305811318`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33305811318)与[Supabase development rollback-only smoke Run `33306877575`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33306877575)均为 `completed/success` 且 `head_sha` 一致；最终复审为 `P0=0/P1=0/P2=0`，B10 development `GO`，CH-021 已自动失效。该结论只覆盖 Mock Provider 与脱敏 development。
- CH-024/CH-025 历史批准记录保留；B11.0-B11.5 与最终同 SHA 双绿均已完成。契约实测保持 `173/198/198/326/705/2,695/0`；B11.2-B11.4 历史批次证据保持。B11 development `GO`，CH-025 已自动失效。
- 任何多级代理、自动打款或代理独立经营需求都必须提交新的重大变更评估。

## 十、变更状态

CH-001 至 CH-031 均已批准。B0 至 B13 development 的验收事实继续有效；B13 最终 SHA `250cbb824ad11f4b0e11338c19494655ec384ff6` 的 development migration Run `33952031401` 与随后 rollback-only smoke Run `33952283771` 均为 `completed/success` 且 `head_sha` 一致，B13 development `GO`，CH-029 已自动失效。当前产品/API 基线为 `v2.4.12 / 2.4.12-ch030`；B14.0-B14.1 已完成，B14.2 的 Admin Web 实现与构建已完成但认证后手工核对尚未执行，B14.3 尚未准入；CH-031 仅覆盖 B14.1-B14.3 的单维护者脱敏 development。B12 orphan `P2=1` 与 B13.8 已登记 P2 继续阻断 staging/真实数据；第三方物流、自动确认、多包裹、真实支付/退款、真实客户数据、staging 和 production 继续 `NO-GO`；首次 staging 仍须外部独立复核。

## 十一、CH-007 交付门禁延期详情

### 11.1 变更申请与决策

- **申请日期**：2026-08-14。
- **申请来源**：开发负责人明确要求暂不使用 GitHub Actions，先继续整个 B3 的本地开发；用户随后明确批准该范围扩展。
- **变更类型**：交付门禁时序调整；不属于产品或接口需求变更。
- **影响程度**：中等。它调整 B3.1 至 B3.3 的远端证据时序，但不降低 B3 整体验收、上线或生产准备标准。
- **批准结论**：产品负责人和技术负责人批准立即执行。B3.1 至 B3.3 可使用本地 MinIO、Redis 与一次性 PostgreSQL 逐段完成实现和故障注入；每个子批次仍按原计划验收后暂停。

### 11.2 风险接受边界

1. GitHub `supabase-development` Environment、required reviewer、受保护 secrets 和 Actions run 证据延期管理，不删除现有 workflow，也不把失败运行改成通过。
2. B2 与 B3 的 Supabase rollback-only 状态继续如实标记为“无远端证据”；本地 PostgreSQL、MinIO 或 Redis 结果不得冒充远端结果。
3. B3.1 至 B3.3 已实施并通过各自独立本地/一次性环境验收并暂停，但 B3 整体不得标记完成，不得据此进入 B4、staging/production 或处理真实客户数据。
4. 在 B3 整体验收前必须补齐：B2 repository rollback-only、B3 repository rollback-only、head SHA、审批人与成功运行记录，并确认无测试数据、DDL、权限或 migration history 残留。
5. 若本地实现暴露冻结契约、Prisma 或首迁移缺口，仍须停止并另行走变更流程；CH-007 不授权绕过 CH-006 或数据库冻结门禁。

后续状态：CH-009 只替代第 1 项中的 development 独立 reviewer 条件。第 2 项“本地不得冒充远端”继续有效，但状态已由“无远端证据”更新为第十三节登记的成功运行；第 3 项只解除 B4 development 阻断，staging/production 与真实数据禁令继续有效；第 4 项的证据缺口已闭合；第 5 项继续有效。

## 十二、CH-008 下载签名幂等语义勘误

### 12.1 问题与决策

- **申请日期**：2026-08-14。
- **申请来源**：B3.1 实现与安全审计发现冻结说明不可执行。
- **问题**：既有文本把 `GET /files/{file_id}/download-url` 写为 `HASH_ONLY`，但该 operation 不接受 `Idempotency-Key`；公共幂等仓储也只接受会产生副作用的 mutation method。按原文既无法形成稳定幂等作用域，也不应为只读签名接口虚构请求键。
- **批准结论**：保持现有 GET 路径、参数和响应不变，明确该 operation 不创建 `idempotency_record`。签名 URL 的安全边界固定为逐次身份/归属校验、5 分钟 TTL、`Cache-Control: no-store, private`、`Pragma: no-cache`、后台敏感读取审计，以及禁止在日志、错误、审计摘要或数据库中保存 URL 明文。

### 12.2 影响与不变项

1. 上传意图和 lifecycle preview 继续使用 `HASH_ONLY`；文件 complete 继续使用闭合 `FILE_UPLOAD_COMPLETE`。
2. 仅更正 OpenAPI description 与技术文档，不新增 `Idempotency-Key`，不修改客户端请求或响应结构。
3. OpenAPI 继续保持 `2.4.1-ch006` 线协议版本、172 paths、196 operations、196 unique operationId 和 306 schemas。
4. Prisma、`0001_initial`、76 models / 59 enums 均不变，不创建迁移。
5. B3.1 测试已证明下载签名响应 no-store、签名 URL 不进入幂等记录/审计/日志，并为授权的私有文件读取写一条不含 URL 的 `READ_SENSITIVE` 审计事实。

### 12.3 B3.1 验证结果

- 真实 PostgreSQL + Redis + MinIO API 集成 `3/3`、Worker 清理集成 `3/3`；同键重放、新键重复完成、并发 complete、跨 owner 隔离、即时 + 延迟 staging 清理与失败重试收敛均有证据。
- API `118 passed / 10 skipped`、Worker `30 passed / 3 skipped`、storage 16、database `141 passed / 24 skipped`；全仓合计 `424 passed / 37 skipped`，安全审计 `P0/P1=0`。
- contracts、冻结数据库、MP-Weixin build、敏感扫描和 diff-check 全部通过；远端 Supabase/GitHub 证据仍按 CH-007 延期，不改变 B3 整体 NO-GO。

后续状态：上述为 CH-008 验收当时结论；远端证据后来由 CH-009 第 13.2 节登记的 B3 实现基准 SHA 成功运行补齐。

## 十三、CH-009 单人维护者开发门禁例外

### 13.1 变更申请与决策

- **申请日期**：2026-08-24。
- **申请来源**：当前项目只有一名开发维护者，无法提供与发起人不同的 GitHub Environment required reviewer；禁止创建虚假账号或把同一人包装为独立复核人。
- **变更类型**：修改交付门禁，不属于产品、接口、数据库或业务需求变更。
- **影响程度**：中等。独立复核缺失会降低缺陷发现的组织冗余，但不会改变自动化测试内容、远端数据库证据或上线标准。
- **批准结论**：产品负责人和技术负责人（当前由同一项目所有者承担）已明确批准立即执行。本例外只适用于 `supabase-development` 的脱敏 development 验收。

### 13.2 已登记技术证据

| 证据 | 已登记事实 |
|---|---|
| GitHub Actions 运行 | [`Supabase development smoke` Run 32678252828](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/32678252828)，`completed/success` |
| 分支与提交 | `main` / `a03ed8475198e81708df4a8fd9f44995c53c86f9`，为 B3 可执行实现及 smoke workflow 的验收基准 SHA |
| 触发与环境 | 手动 `workflow_dispatch`，确认值 `DEVELOPMENT_SMOKE_APPROVED`；验收时另行查询 Environment 配置，结果为 secrets 与普通 CI 隔离、分支策略只允许 `main`、无 required reviewer |
| 只读前置 | Prisma validate 与 Supabase 结构、权限、TLS、migration history 只读检查通过 |
| rollback-only | B1 `1 passed / 13 skipped`、B2 `1 / 6`、B3 文件 `1 / 2`、B3 主数据 `1 / 5`；四组远端步骤均成功 |
| 无残留 | 每组 smoke 均在顶层事务最终 `ROLLBACK`，并以事务外查询断言本组 synthetic DML 标识计数为零；rollback specs 不含 DDL、权限或 migration history 写路径，结构/权限/history 检查是运行前只读前置，不表述为运行后全库漂移复核 |

该运行只证明 Supabase development repository 的 rollback-only 行为，不证明 B2 API + Redis 云端联调，也不证明真实浏览器到 NestJS 的纵向链路。

### 13.3 补偿控制与适用边界

1. Environment secrets 继续与普通 CI 隔离；工作流只能手动触发，必须显式确认开发项目，且只允许 `main` 分支。
2. 每次阶段验收必须登记最终 head SHA、成功 run 链接和各 rollback step 结果；失败运行不得删除、改写或冒充成功证据。
3. smoke 仅使用 `mall_runtime`、脱敏 synthetic 标识和顶层回滚；本地 PostgreSQL、Redis 或 MinIO 结果不能替代远端记录。
4. CH-009 不要求虚构 required reviewer，也不把发起人的自我确认描述为独立复核。缺少独立 reviewer 的剩余风险只被接受到 B4 development 结束，现已耗尽；后续 development 若仍无独立复核，必须另行提交并批准新的变更。
5. CH-009 允许将 B3 开发验收标记为 `GO` 并进入 B4 开发；它不允许部署 staging/production、接入真实客户数据、真实微信支付或真实银行卡付款。
6. CH-009 已在 B4 development 结束时自动失效，不得续用于任何后续 development、staging 或 production。第一次进入 staging 前必须取得外部独立人员对代码、安全、数据库权限/迁移和验收证据的复核记录，并配置真实 required reviewer 或保存可追溯的外部签字报告。

### 13.4 实施与回退

- **实施范围**：仅同步变更记录、产品/风险/技术状态摘要、B0-B3 开发管理文档和资料/原型索引；OpenAPI、生成 contracts、Prisma、`0001_initial`、应用代码与现有 workflow 均不修改。CH-009 文档差异位于上述实现基准 SHA 之后，不扩张该 run 的技术证明范围；下一批业务实现仍须登记自己的基准 SHA 和运行证据。
- **当前状态**：已实施并到达适用边界。B2/B3/B4 远端 repository 门禁已闭合，B4 development 为 `GO`；进入 staging 前必须恢复外部独立复核。
- **回退触发**：若成功 run 与最终 SHA 不匹配、证据不可验证、发现测试残留或出现安全/权限异常，立即撤销 B4 后续写入准入并重新执行受控 smoke；不执行数据库降级。

## 十四、CH-010 B4 商品与 SKU 契约解阻

### 14.1 变更申请与决策

- **申请日期**：2026-08-24。
- **申请来源**：技术限制。B4 启动审计发现旧 Product/SKU `LifecycleAction` 只有 `DEACTIVATE/SOFT_DELETE`，已下架记录无法重新启用；同时创建状态、空 SKU 商品的最低活动价、归档 SKU 返回、图集上限和 SKU 库存初值未闭合。
- **变更类型**：修改需求与接口契约，不增加页面、path、operation 或数据库模型。
- **影响程度**：中等，影响 ADM-03/04、Product/SKU 12 个现有 operation、生成 contracts、Catalog 事务与 B4 测试。
- **审批级别**：产品负责人 + 技术负责人。
- **审批结果**：已批准，立即执行。
- **CH-010 当时基线**：产品/PRD `v2.4.2 / CH-010`，OpenAPI `2.4.2-ch010`；现行版本见 CH-016。

### 14.2 已批准契约决策

1. 删除旧共享 `LifecycleAction`，新增闭合 `ProductLifecycleAction` 和 `SkuLifecycleAction`；二者均只接受 `ACTIVATE/DEACTIVATE/SOFT_DELETE` 与 2-500 字符 `reason`。
2. Product 创建必须显式提交 `initial_status=DRAFT`；SKU 创建必须显式提交 `initial_status=INACTIVE`。普通更新接口均不得修改生命周期状态。
3. Product 状态机固定为 `DRAFT/INACTIVE -> ACTIVATE -> ACTIVE`、`ACTIVE -> DEACTIVATE -> INACTIVE`、`DRAFT/INACTIVE -> SOFT_DELETE -> ARCHIVED + deleted_at`；恢复固定为 `DRAFT + deleted_at=null`。
4. SKU 状态机固定为 `INACTIVE -> ACTIVATE -> ACTIVE`、`ACTIVE -> DEACTIVATE -> INACTIVE`、`INACTIVE -> SOFT_DELETE -> ARCHIVED + deleted_at`；恢复固定为 `INACTIVE + deleted_at=null`。SKU 可在非归档 Product 下先启用，但只有 Product 与 SKU 同时 ACTIVE 才进入消费者目录。
5. ACTIVE Product/SKU 不得直接软删除。Product 软删除前不得存在 ACTIVE SKU 或其下任一活动库存预占；SKU 软删除前不得存在活动库存预占。Product 状态变化不级联改变 SKU，恢复 Product 也不级联恢复 SKU。
6. Product ACTIVATE 必须同时满足品牌 ACTIVE、分类 ACTIVE、至少一张 `READY/PUBLIC/PRODUCT_IMAGE` 活动图和至少一个 ACTIVE SKU。`published_at` 只在首次 ACTIVATE 时写入，重新启用不得覆盖。
7. Product 与 SKU 的 ACTIVATE、DEACTIVATE、SOFT_DELETE 均须先 preview，再以相同业务字段、未过期 token/确认哈希、新幂等键和 `If-Match` confirm；`reason` 写入既有 `audit_log.reason`。restore 不新增 preview，仍要求原因、新幂等键和 `If-Match`。
8. Product 列表固定 `published_at DESC NULLS LAST,id DESC`；没有 ACTIVE SKU 时 `minimum_active_price=null`。默认列表排除 ARCHIVED，显式归档筛选和按 ID 详情允许读取归档 Product。
9. Product 详情返回全部 SKU，包括 ARCHIVED，固定按 `created_at ASC,id ASC`；图集最多 8 张，活动关系固定按 `sort_order ASC,id ASC` 返回。
10. Product 的 `spu_code` 和 SKU 的 `code` 创建后永不可修改，软删除后继续全局保留，只能恢复原记录。SKU 创建响应为 HTTP 201，并在同一事务创建 `physical_qty=0,locked_qty=0` 的 `inventory_balance`，可售库存派生为 0。
11. 新增四个 422 业务码：`PRODUCT_PRIMARY_IMAGE_REQUIRED`、`PRODUCT_ACTIVE_SKU_REQUIRED`、`ACTIVE_SKU_DEPENDENCY`、`ACTIVE_INVENTORY_RESERVATION`。preview 可返回依赖影响，confirm 才执行阻断并且不得消费失败的 preview。
12. 状态、`published_at`、`deleted_at`、图集关系、SKU 与库存余额均使用现有字段；不新增迁移，Prisma 与 `0001_initial` 必须逐字节不变。

**CH-010 勘误（2026-08-24）**：B4.1 实现审计发现共享 `PositiveMoney` 正则未限制整数位数，与已批准且冻结的 `DECIMAL(18,2)` 存储边界不一致；后台商品 `keyword` 也漏写了实现采用的 200 字符查询上限。现将金额收紧为大于 0、固定 2 位小数、整数部分最多 16 位，并补齐 `keyword.maxLength=200`。这些调整只排除数据库无法存储或超出受控查询边界的输入，不新增需求或数据库变更，不另行登记 CH。

### 14.3 影响与风险控制

| 影响维度 | 级别 | 决策与控制 |
|---|---|---|
| 产品范围 | 中等 | 闭合 ADM-03/04 最小商品/SKU 管理；Banner、库存调整/流水和其他 B4 外能力不在本批次 |
| API 契约 | 中等 | 保持 172 paths / 196 operations / 196 unique operationId；schemas 目标 307，以解析门禁实测为准 |
| 数据库 | 无结构影响 | 复用 Product、Sku、ProductImage、InventoryBalance、预览、幂等与审计表；migration diff 必须为 0 |
| 并发 | 高风险 | lifecycle confirm 按 preview/idempotency → brand → category → product → SKU → reservation → audit/outbox 锁序，事务内重查全部前置 |
| 用户体验 | 中等 | 创建固定草稿/停用，发布独立预览确认；409 强制刷新重做，422 展示准确依赖，不复用旧 token |
| 兼容性 | 可控 | 当前无生产 Product/SKU 客户端与真实数据，不保留旧创建即 ACTIVE 或无 ACTIVATE 的兼容分支 |

### 14.4 实施批次与门禁

| 批次 | 交付 | 进入条件 | 退出条件 |
|---|---|---|---|
| B4.0 | CH-010 文档、OpenAPI、contracts、原型说明与普通 CI 修复 | CH-010 已批准 | 已通过并暂停 |
| B4.1 | Product/SKU CRUD、图集、稳定投影、库存初值和精确幂等 | B4.0 全部退出条件通过 | 已通过并暂停，P0=0/P1=0 |
| B4.2 | Product/SKU preview-confirm、restore、锁序和依赖保护 | B4.1 已验收暂停 | 已通过并暂停，P0=0/P1=0 |
| B4.3 | ADM-03/04 商品列表与编辑 | B4.2 已验收暂停 | 已通过并暂停，P0=0/P1=0 |
| B4.4 | 普通 CI 全量、真实纵向链路与 Supabase rollback-only | B4.3 已验收暂停 | 已通过并暂停，最终 SHA、成功 run、无残留齐全，P0=0/P1=0 |

B4 已按上述门禁严格串行完成。CH-009 的独立 reviewer 风险接受已在 B4 development 结束时耗尽；后续 development 若仍无独立复核须新变更批准，staging 前必须取得外部独立复核。B4 development 不包含 Banner、库存人工调整/流水、真实客户数据、真实微信资金链路、staging 或 production。

### 14.5 回退边界

- B4.1 已进入业务实现，不再采用整体契约回滚；通过关闭 Product/SKU 模块路由回退，保留已写审计、幂等和历史记录，不重写冻结迁移。
- ADM-03/04 已进入工程实现；回退时同步关闭商品后台导航，不删除历史数据。
- 若普通 CI、契约统计、冻结文件、并发状态矩阵或 Supabase rollback-only 任一证据失效，撤销相应 development 准入并重新执行，不得用本地结果代替远端记录。

### 14.6 B4 development 收口证据

- 实现基准固定为 `main` / `0929f2435e7f5b9ad745fd9cab60b066378e502e`；[普通 CI Run 32721588213](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/32721588213) 与 [Supabase rollback-only Run 32722510890](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/32722510890) 均为 `completed/success` 且 `head_sha` 一致。
- 本地工程 `641 passed / 60 designed environment skips`；B2/B3/B4 浏览器回归和 `1/1` 真实 browser → Nest → PostgreSQL/Redis/MinIO 纵向链路通过，数据库、对象和 Redis 无测试残留。
- 最终结论为 B4 development `GO`、`P0=0/P1=0`；后续仅登记证据的 docs-only 提交不扩张实现 SHA 的技术证明范围。

## 十五、CH-011 B5 单人维护者开发门禁例外

### 15.1 变更申请与决策

- **申请日期**：2026-08-25。
- **申请来源**：项目仍只有一名开发维护者，无法提供与发起人不同的 development required reviewer。
- **变更类型**：修改交付门禁，不属于产品、接口、数据库或业务需求变更。
- **影响程度**：中等。独立复核缺失降低缺陷发现的组织冗余，但不改变自动化、远端数据库、冻结契约或上线标准。
- **批准结论**：用户已明确批准，产品负责人和技术负责人（当前由同一项目所有者承担）批准立即执行。
- **适用范围**：仅 B5 Banner/库存的脱敏 development，从 B5.0 开始，到 B5.4 development 验收结束自动失效。

### 15.2 补偿控制

1. GitHub `supabase-development` Environment 继续隔离 secrets、只允许 `main` 手动触发，并要求 `DEVELOPMENT_SMOKE_APPROVED` 显式确认。
2. 每个 B5 子批次只有 `P0=0/P1=0` 才能暂停并进入下一批；B5.4 必须登记同一实现 SHA 的普通 CI 与 Supabase rollback-only 成功 run。
3. Supabase smoke 继续使用 `mall_runtime`、synthetic 标识和顶层回滚，并在事务外断言无残留；本地结果不得冒充远端证据。
4. 第三方 Actions 按核验 commit 固定，Supabase CA 下载后校验 SHA-256，根级纵向 runner 纳入 lint；准确 SHA 上必须实际运行完整门禁。
5. 不虚构第二 reviewer，也不把项目所有者的自我确认表述为独立复核。第一次进入 staging 前仍须外部独立人员复核代码、安全、数据库权限/迁移和验收证据。

### 15.3 不授权事项与失效

- CH-011 不修改 v2.4.2/CH-010 产品/API 基线，不批准任何 Banner 或库存契约选择，也不允许修改 Prisma 或首迁移。
- CH-011 不放行 staging、production、真实客户数据、真实微信或资金链路。
- 若准确 SHA 的远端证据失败、出现残留或 P0/P1 未清零，立即撤销相应子批次准入。
- B5.4 development 验收结束时本例外自动失效；后续 development 若仍缺独立复核，必须重新申请，不能自动续期。

### 15.4 最终验收与失效记录

- 实现基准固定为 `main` / `d97c43958142eaa0fa5a0a9954bb21d136944ba2`；[普通 CI Run 32822780209](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/32822780209) 与 [Supabase rollback-only Run 32823898006](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/32823898006) 均为 `completed/success` 且 `head_sha` 一致。
- 普通 CI 实际执行 B5 browser -> Nest -> PostgreSQL/Redis/MinIO 纵向链路、Banner/Inventory PostgreSQL full 和 B5 浏览器回归；Supabase workflow 实际执行 Banner 与 Inventory rollback-only，事务外无残留。
- B5.4 最终结论为 development `GO`、`P0=0/P1=0`。CH-011 随验收完成自动失效；该结论不适用于后续 development、staging 或 production，进入 staging 前仍须外部独立人员复核。

## 十六、CH-012 B5 Banner 与库存契约解阻

### 16.1 申请与当前决策

- **申请日期**：2026-08-25。
- **申请来源**：技术限制。B5.0 审计发现现有 Banner DTO 可绕过软删除/恢复、`low_stock` 无存储口径、库存 HR-07 preview 未在公共仓储注册，且库存边界/流水响应和原型存在漂移。
- **变更类型**：修改需求与接口契约，不新增页面、path、operation 或数据库模型。
- **影响程度**：中等，影响 ADM-07/08、9 个现有 operation、生成 contracts、幂等/preview 公共内核和静态原型。
- **审批状态**：用户已明确批准，产品负责人和技术负责人批准立即执行。
- **执行边界**：先完成 B5.0 产品、OpenAPI、生成 contracts、公共幂等/preview 契约、原型和冻结数据库门禁；B5.0 验收暂停前不得实施 B5.1 repository、controller、service 或工程前端。

### 16.2 推荐的最小方案

1. Banner 创建固定 DRAFT；资料更新不接收状态；同一 PATCH operation 以独立闭合 action 分支处理 ACTIVATE/DEACTIVATE。DELETE 是唯一软归档入口，ACTIVE 不得直接归档，restore 固定回 DRAFT。
2. Banner 默认排除 ARCHIVED，稳定排序 `sort_order ASC,id ASC`；公开窗口为 `(starts_at IS NULL OR starts_at <= now) AND (ends_at IS NULL OR now < ends_at)`，起止均非空时 `ends_at > starts_at`。启用、ACTIVE 资料更新与公开投影重查 READY/PUBLIC/BANNER 文件和 ACTIVE PRODUCT/CATEGORY target；HTTPS URL 最长 500 字符且须命中服务端 origin allowlist。
3. Banner 写响应新增代码级闭合 `BANNER_RESOURCE_RESPONSE` 精确重放策略，不新增数据库列。
4. 无迁移删除 `low_stock`、原型预警值、售后占用和独立备注；库存只展示/复算 `physical/locked/available=physical-locked/active_reservation`。
5. `physical_delta` 固定为非零 int32；归档 SKU 只读。preview 使用 `HASH_ONLY` 并注册 `INVENTORY.ADJUST/INVENTORY`，越过 locked 下界仍返回 200 warning，confirm 以 `STOCK_INSUFFICIENT` 422 阻断且不消费 preview；超 PostgreSQL INTEGER 使用 `INVENTORY_QUANTITY_OUT_OF_RANGE` 422。
6. confirm 使用 `COMMAND_RESPONSE`，固定 `resource_type=inventory/resource_id=sku_id/status=SUCCEEDED/version=new balance version`；余额、version、单条 MANUAL 流水、preview、审计、幂等和 outbox 在同一 Serializable 事务完成。
7. 流水响应使用闭合 enum，稳定排序 `occurred_at DESC,id DESC`；B4 零余额不回填 INITIAL 流水。
8. 产品/API 升级至 `v2.4.3 / 2.4.3-ch012`；实测保持 172 paths / 196 operations / 196 unique operationId，并得到 312 schemas / 692 schema refs / 2,578 local refs / 0 dangling refs。Prisma、`0001_initial` 和冻结哈希逐字节不变。

完整状态矩阵、批次、锁序、验收与回退方案见 `../05-开发管理/B5-Banner与库存.md`。

### 16.3 实施与暂停边界

- B5.0 可以修改：产品/技术说明、OpenAPI、生成 contracts、公共幂等与 preview 白名单、静态原型及专项检查。
- B5.0 不可以创建：Banner/Inventory 业务 repository、controller、service、工程路由或导航；这些只属于后续 B5.1-B5.3。
- B5.0 必须证明：统计与闭合 schema 通过、原型回归通过、Prisma/首迁移逐字节不变、migration diff=0，且 `P0=0/P1=0` 后暂停。
- **实施结果**：B5.0 已完成上述允许项并通过本地契约、生成漂移、公共内核、原型、冻结数据库及独立复核门禁；随后经用户逐批明确继续，B5.1 已交付 Banner repository/API，B5.2 已交付 Inventory repository/API、HR-07 preview-confirm 与流水，B5.3 已交付 ADM-07/08 后台与五视口验收，均在 `P0=0/P1=0` 后暂停。B5.4 已完成真实纵向链路、全量回归和同一实现 SHA 的远端双绿，B5 development `GO`。B5 收口当时消费者 `/store/home` 尚未实施；现行状态见 CH-014 的 B6.1 实施结果。

## 十七、CH-013 B6 单人维护者开发门禁例外

### 17.1 变更申请与决策

- **申请日期**：2026-08-25。
- **申请来源**：团队资源约束；当前仅一名维护者，无法实现同一 development 批次内的第二人 reviewer。
- **变更类型**：修改交付门禁，不修改产品、接口、数据库或安全要求。
- **影响程度**：中等；组织独立性减少，以自动化、真实纵向测试、准确 SHA 远端双绿和无残留断言补偿。
- **审批状态**：产品负责人和技术负责人已批准，立即执行。
- **适用边界**：只允许 B6.0-B6.4 使用脱敏 development 数据实施和验收；B6.4 验收结束时自动失效。

### 17.2 补偿控制与失效条件

1. B6.0-B6.4 严格串行，每批只有 `P0=0/P1=0` 才能暂停并由用户明确批准下一批。
2. B6.4 必须在同一最终实现 SHA 上取得完整普通 CI 和 Supabase development rollback-only 成功证据；任一失败 run 不得替代成功证据。
3. 不虚构第二 reviewer，不把项目所有者的自我确认表述为独立复核。
4. 发现残留数据/对象、契约漂移、限流绕过或公开目录泄露管理字段时，立即撤销当前批次准入。
5. CH-013 不适用 staging、production、真实客户数据、真实微信身份或资金链路；第一次进入 staging 前仍须外部独立人员复核代码、安全、数据库和验收证据。

## 十八、CH-014 B6 消费者匿名商城目录契约

### 18.1 变更申请与决策

- **申请日期**：2026-08-25。
- **申请来源**：技术限制。现有公开目录契约将 `is_salable` 固定为 true，未表达零库存可浏览；搜索、综合排序、首页分区降级和匿名限流也未闭合。
- **变更类型**：修改需求与 API 契约，不新增 path、operation、页面、FR、AC、US、HR 或数据库模型。
- **影响程度**：中等；影响 5 个现有 Store 公开 GET、MP-01 至 MP-06、生成 contracts、静态原型和 CI 契约门禁。
- **审批状态**：用户已明确批准，产品负责人和技术负责人批准立即执行。
- **基线**：产品/PRD `v2.4.4 / CH-014`，OpenAPI `2.4.4-ch014`。

### 18.2 已批准契约

1. 仅返回 ACTIVE 商品、品牌、分类和 ACTIVE SKU；所有 ACTIVE SKU 都公开，`StoreSkuView.is_salable` 为布尔值，零库存返回 false。商品摘要同步返回 `is_salable`，全部 SKU 售罄时商品仍可浏览。
2. 新增默认 `COMPREHENSIVE`，固定为 `is_hot DESC,is_new DESC,sales_count DESC,published_at DESC NULLS LAST,product_id ASC`；HOT 按全生命周期净销量，NEWEST 按首次发布时间，价格排序使用 ACTIVE SKU 最低零售价，并以 product ID 打破平局。
3. 搜索只对 trim 后的商品名执行大小写不敏感匹配，长度 1-200；品牌/分类公开接口无 pagination，返回全部 ACTIVE 记录并按 `sort_order,id` 排序。
4. 首页返回 Banner/分类/热销/新品四个数组和各自 `READY|UNAVAILABLE` 分区状态；单区失败返回空数组且总体 200，四区全失败才返回 500。上限分别为 10/8/4/4，无效 PRODUCT Banner target 自动排除。
5. 匿名目录使用 Redis 固定窗口限流，每个 HMAC 来源 IP 每 60 秒 120 次；不保存原始 IP，超限返回 429 和准确 `Retry-After`，Redis 异常不得绕过限流。
6. 仅允许 READY/PUBLIC 且 purpose 正确的图片进入公开投影；不返回管理状态、version、file ID、实物/锁定库存或其他后台字段。
7. 契约统计保持 172 paths / 196 operations / 196 unique operationId / 312 schemas。Prisma、`0001_initial` 与冻结文件逐字节不变，本批已实测 `pnpm db:check-frozen`、`pnpm prisma:validate` 和 `pnpm db:diff` 通过，diff 为 `No difference detected`。

### 18.3 批次、暂停与回退

- B6.0 只修改治理、产品/API/架构说明、OpenAPI、生成 contracts、契约检查和静态原型；B6.1 明确批准前不创建 Store repository、controller/service 或工程页面。
- B6.1 至 B6.4 分别交付公开 API、MP-01 至 MP-05、MP-06 游客本地购物车和总验收；每批 `P0=0/P1=0` 后暂停。
- CH-014 尚未进入业务实现时，可整体回退文档、OpenAPI、生成类型与原型；进入 B6.1 后通过关闭 Store Catalog 模块路由与小程序导航回退，不执行数据库降级。
- 微信登录、收藏、服务端购物车、结算和订单不在 B6 内；非匿名功能不得伪造登录或业务成功。

### 18.4 B6.1 实施结果

- 用户在 B6.0 `P0=0/P1=0` 后明确批准进入 B6.1。现已实现独立 `StoreCatalogRepository`、Nest Store Catalog controller/service/module，以及 `/store/home`、`/store/categories`、`/store/brands`、`/store/products`、`/store/products/{product_id}` 5 个匿名 GET；MP-01 至 MP-05 的后续实施结果见 18.5。
- repository 使用 Repeatable Read、参数化 SQL 与按 ID 批量装配，覆盖公开状态/素材过滤、最低 ACTIVE SKU 价格、五种稳定排序、库存缺失 fail-safe、Banner 有效窗口/target/origin allowlist 与无 N+1。
- 5 路由共享 Redis 服务端时间固定窗口，每个来源 IP HMAC 120 次/60 秒；只保存 HMAC，超限返回 1-60 秒动态 `Retry-After`，Redis 未就绪、断连、脚本异常或非法结果均 fail closed。B2 既有登录/MFA 900 秒 `RATE_LIMITED/REAUTH_LOCKED` 保持不变。
- `API_TRUSTED_PROXY_CIDRS` 默认空且不信任代理头，只接受显式数字 IP/CIDR；拒绝全网段、带 host bits、hostname 与语义重复项。IPv4-mapped IPv6 在可信代理配置和请求来源处均归一化为 IPv4，未经信任的 `X-Forwarded-For` 不影响来源 IP。
- 实测为 repository unit `9 passed`、database 全包 `332 passed / 52 环境模式跳过`、API 全包 `426 passed / 26 环境模式跳过`、config `67 passed`；隔离 PostgreSQL full 和受控 Supabase development rollback-only 均为 `1 passed / 1 mode skip`，事务外 fixture 计数均为 0；Store/API 聚焦回归（含可信代理与 HTTP 限流边界）为 `86 passed`。
- Prisma、两份 `0001_initial` 与冻结哈希未修改，migration diff 仍为 0。本节保留 B6.1 批次结束时的历史状态；B6.4 随后已在最终 SHA `9d8911934a5aa2b09ece2e12935bcafc9ccdcba6` 完成真实纵向及 workflow 双绿，B6 development `GO`，CH-013 已失效。

### 18.5 B6.2 实施结果

- 用户在 B6.1 `P0=0/P1=0` 后明确批准进入 B6.2。现已实现基于 `uni.request` 与 generated contracts 的 Store API 客户端，以及 MP-01 首页、MP-02 分类、MP-03 搜索、MP-04 商品详情和 MP-05 SKU 选择层。
- 首页覆盖四区独立降级；分类/搜索覆盖五种服务端排序、筛选、稳定分页、请求取消与下一页局部失败；详情和选择层覆盖售罄规格、数量上限、图片解码、404/429/500 与慢请求。收藏、立即购买和“我的”只提示登录。
- 实测为 miniapp unit `4 files / 58 passed`，typecheck、scoped lint、H5 build 与 MP-Weixin build 均通过；B6 UI Playwright 为 `19 passed / 36 designed skips`，MP-01 至 MP-05 成功路径和无横向溢出覆盖 375/390/414/1024/1440，详细状态矩阵在 390 视口执行，未发现横向溢出、文字裁切或控件重叠。
- B6.2 暂停时 SKU 确认只更新页面内选择，不持久化购物车，不调用服务端 Cart/订单，也不伪造登录或交易成功；后续 B6.3 实施结果见 18.6。
- B6.2 未修改 OpenAPI、Prisma 或 `0001_initial`，本地退出审查为 `P0=0/P1=0`。本节保留 B6.2 批次结束时的历史状态；B6.4 最终证据现已闭合，B6 development `GO`。

### 18.6 B6.3 实施结果

- 用户在 B6.2 `P0=0/P1=0` 后明确批准进入 B6.3。现已实现 MP-06 版本化游客本地购物车与详情页游客加购：同 `sku_id` 合并、不同 SKU 分行，保存数量、选择状态和最小展示快照。
- 每次进入购物车按 `product_id` 去重并顺序回源公开详情，刷新价格、库存和状态。404 或详情中缺少目标 SKU 时判失效，售罄/失效项排除合计；429、网络失败及其他临时错误保留本地项和快照，不误判下架。
- 数量上限为 `min(99, available_stock)`；选择、全选、数量变更和删除已覆盖。结算只显示登录提示并保留购物车，没有调用服务端 Cart 或订单接口。
- 当前累计实测为 miniapp unit `6 files / 84 passed`，miniapp typecheck、scoped lint、H5 build、MP-Weixin build、全仓 `pnpm check`、`git diff --check` 和 `db:check-frozen` 逐字节检查均通过；B6 UI Playwright `34 passed / 56 designed skips`，覆盖 375/390/414/1024/1440 五个视口；lint 为 0 errors（保留既有 warnings），本地退出审查为 `P0=0/P1=0`。
- B6.3 未修改 OpenAPI、Prisma 或 `0001_initial`。本节保留 B6.3 批次结束时的历史状态；B6.4 已取得真实 H5 纵向与同 SHA 双绿，CH-013 已失效，B6 development `GO`；staging/production 继续 `NO-GO`。

## 十九、CH-015 B7 单人维护者开发门禁例外

### 19.1 变更申请与决策

- **申请日期**：2026-08-26。
- **申请来源**：团队资源约束；当前仅一名维护者，无法在同一 development 批次配置第二 reviewer。
- **变更类型**：修改交付门禁，不修改产品、接口、数据库、权限、隐私或安全要求。
- **影响程度**：中等；组织独立性减少，以自动化、真实纵向测试、准确 SHA 远端双绿和无残留断言补偿。
- **审批状态**：用户已明确批准，产品负责人和技术负责人批准立即执行。
- **适用边界**：只允许 B7.0-B7.5 使用脱敏 development 数据和 Mock Provider；B7.5 验收结束时自动失效。

### 19.2 补偿控制与失效条件

1. B7.0-B7.5 严格串行，每批只有 `P0=0/P1=0` 才能暂停并由用户明确批准下一批。
2. B7.5 必须在同一最终实现 SHA 上取得完整普通 CI 与 Supabase development rollback-only 成功证据；失败 run 不得替代成功证据。
3. 不虚构第二 reviewer，不把项目所有者的自我确认表述为独立复核。
4. 发现真实个人信息、token/手机号泄露、跨 audience 接受、会话撤销失效、部分注销、归因覆盖、数据库残留或契约漂移时，立即撤销当前批次准入。
5. CH-015 不适用 staging、production、真实微信身份、真实客户数据或资金链路；第一次进入 staging 前仍须外部独立人员复核代码、安全、数据库和验收证据。

### 19.3 最终验收与失效记录

- B7 最终实现 SHA 为 `3f844bfb9866854ceedb975ad0dc4fd7cacfb04a`。
- [普通 CI Run `33055090596`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33055090596) 与 [Supabase development rollback-only Run `33056078437`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33056078437) 均成功，且 `head_sha` 相同。
- B7 development `GO`；CH-015 已按批准条件自动失效，不得用于 B8 或任何 staging/production 准入。

## 二十、CH-016 B7 消费者身份、会话、服务代理与隐私契约

### 20.1 变更申请与决策

- **申请日期**：2026-08-26。
- **申请来源**：技术限制。既有消费者身份契约没有服务端权威法律文本入口，登录同意可重复/使用任意版本，Store 会话复用宽认证响应，手机号允许客户端选择 Provider，候选目标可由客户端提交，服务代理复用内部绑定投影，账号删除仍为未闭合的异步申请形状。
- **变更类型**：修改需求与 API 契约；新增一个匿名法律文本 GET，以 preview + 同步 confirm 替换旧删除状态查询。
- **影响程度**：中等；影响消费者法律文本/登录/会话、profile/手机号、归因候选/服务代理、账号注销、MP-07/18/19/20/21、生成 contracts、静态原型和 CI 门禁。
- **审批状态**：用户已明确批准，产品负责人和技术负责人批准立即执行。
- **基线**：产品/PRD `v2.4.5 / CH-016`，OpenAPI `2.4.5-ch016`。

### 20.2 已批准契约

1. 新增匿名 `GET /store/legal-documents`，返回当前 `USER_AGREEMENT`、`PRIVACY_POLICY` 与 `PHONE_AUTHORIZATION` 三份必填权威版本快照；登录只提交前两份，手机号授权单独匹配第三份。响应 no-store，独立使用每 HMAC 来源 IP 每分钟 120 次 Redis 固定窗口，Redis 异常 fail closed。
2. 微信登录同意必须是两种文档各一次、`accepted=true`、版本与服务端当前版本精确匹配。code 1-512、candidate token 32-512 且均 write-only；Store token 固定校验 `role=CUSTOMER`、`assurance=WECHAT`、`audience=qingxu-store`，不得与 `role=SUPER_ADMIN`、`assurance=MFA`、`audience=qingxu-admin-web` 混用。Provider 不属于 token claim，只用于服务端 Adapter 选择和持久化来源。
3. token 响应统一 no-store；登录、refresh/logout 与注销 confirm 等敏感命令使用 `HASH_ONLY` 幂等，不缓存或重放 token/完成响应。refresh 轮换及旧 token 重放继续使用公共 session-family 安全规则。
4. profile PATCH、手机号授权/撤回均要求 `If-Match` 和新幂等键。手机号请求移除客户端 `provider`，只接受 provider credential 与 `PHONE_AUTHORIZATION` 同意；Provider 由服务端环境选择，Mock 仅允许 development/test。
5. 候选创建只接受 `invite_code + promotion_asset_id`，邀请码长度 1-128，推广物料 ID 为 26 字符 ULID；目标由服务端从有效推广物料解析。candidate token 仅首次返回，服务端仅存与幂等/IP 不同 domain 的用途隔离 HMAC，固定 30 分钟有效；GET 查询不消费，替换或登录迁移原子失效 token hash，确认/拒绝消费迁移后的候选事实；不匹配返回 409。服务代理专用投影只返回 `agent_id/display_name/bound_at`。
6. 新增 `POST /store/privacy/deletion-requests/preview`，删除旧 `GET /store/privacy/deletion-requests/current`，保留 `POST /store/privacy/deletion-requests` 作为同步 confirm。preview 始终 200；`eligible=false` 返回 blockers/impacts 且 token/hash/expiry 为 null，只有 eligible 才签发绑定 CUSTOMER、session、账户版本和预览结果的 5 分钟能力。confirm 携带 If-Match、新幂等键并重新检查阻断。
7. 无阻断时，同一事务完成身份/资料去标识化、全部 session/refresh 撤销、手机号和允许硬删个人数据清理、候选失效、绑定结束、代理隐私投影匿名化及审计/幂等事实；存在业务阻断或事务失败时不得出现部分注销。成功后重复请求只返回认证失效或已完成冲突，不提供 current 查询或完成响应重放。
8. 新增 `CONSENT_VERSION_MISMATCH` 409、`ATTRIBUTION_CANDIDATE_MISMATCH` 409、`ACCOUNT_DELETION_BLOCKED` 422。CH-016 专项实测为 173 paths / 197 operations / 197 unique operationId / 320 schemas / 699 schema refs / 2,617 local refs / 0 dangling refs，Redocly 0 warning。
9. Prisma、两份 `0001_initial`、76 models / 59 enums 和冻结哈希逐字节不变，不新增 migration。
10. B7 固定一个消费者微信 AppID，以 `(AppID, openid)` 作为身份语义；现有 `account.wechat_open_id` 唯一约束只在该 AppID 范围内有效。`wechat_union_id` 仅作可空元数据，不是唯一登录键且不得用于自动合并；未来多 AppID 必须另提变更并执行迁移。
11. 候选创建响应闭合为有效 candidate、已有 service agent 和不可归因公开 fallback 三个互斥分支；有效候选必返公开目标。匿名或 candidate-token 请求返回 candidate 时签发新 token，bearer 返回 candidate 时 token 为 null；绑定/fallback 分支始终为 null。匿名替换旧候选必须携带可选旧 `X-Candidate-Token`，否则旧事实只按 TTL 过期。
12. B7 Store 身份、资料、归因和注销操作的成功及全部 JSON 错误响应改用专用必返 no-store/no-cache Header，敏感错误 wire 将 `rejected_value` 收紧为只能是 null；保留 Agent/Admin/Files 原共享 Header 的 optional wire，并锁定手机号掩码 pattern。不改非 Store 响应体、权限、角色、audience 或数据库。
13. 候选创建与查询的可选认证严格 fail closed：双凭据返回 400；只要存在 bearer 或候选 token 就必须按固定优先级验证，失败返回 401 且不得降级；仅创建接口的无凭据请求进入匿名分支，查询无凭据返回 401。
14. 登录、手机号授权、候选查询/确认/拒绝及删除 confirm 的 CH-016 409/422 使用专用闭合错误响应；固定错误码进入 wire schema，不再仅靠 description 约束，并保持 schemas/schema refs/local refs 统计不变。

### 20.3 批次、暂停与回退

- B7.0 只修改治理、产品/API/架构/数据库/风险说明、OpenAPI、生成 contracts、契约检查和静态原型；B7.1 明确批准前不得创建身份域 repository、controller/service 或工程页面。
- B7.1-B7.5 依次交付角色感知 Auth/协议、profile/手机号、归因/服务代理、同步注销、MP-07/18/19/20/21 与总验收；每批 `P0=0/P1=0` 后暂停。
- 收藏、服务端购物车、地址、结算、订单、支付、退款、真实微信和 staging/production 不在 B7 内，不得显示伪业务成功。
- CH-016 尚未进入 B7.1 业务实现时，可整体回退文档、OpenAPI、生成类型与原型；进入 B7.1 后通过关闭对应 Store 模块路由和小程序导航回退，不执行数据库降级。

完整契约、锁序、验收和回退边界见 `../05-开发管理/B7-消费者身份会话与隐私.md`。B7.0-B7.5 已完成；最终实现 SHA `3f844bfb9866854ceedb975ad0dc4fd7cacfb04a` 的普通 CI Run `33055090596` 与 Supabase development rollback-only Run `33056078437` 同 SHA 双绿，最终只读复审 `P0=0/P1=0`，B7 development `GO`。

## 二十一、CH-017 B8 单人维护者开发门禁例外

### 21.1 变更申请与决策

- **申请日期**：2026-08-27。
- **申请来源**：团队资源约束；当前仅一名维护者，无法在同一 development 批次配置第二 reviewer。
- **变更类型**：修改交付门禁，不修改产品、接口、数据库、权限、隐私或安全要求。
- **影响程度**：中等；组织独立性减少，以自动化、真实纵向测试、准确 SHA 远端双绿和无残留断言补偿。
- **审批状态**：用户已明确批准，产品负责人和技术负责人批准立即执行。
- **适用边界**：只允许 B8.0-B8.5 使用脱敏 development 数据；B8.5 完成时自动失效。

### 21.2 补偿控制与失效条件

1. B8.0-B8.5 严格串行，每批只有 `P0=0/P1=0` 才能暂停并由用户明确批准下一批。
2. B8.5 必须在同一最终实现 SHA 上取得完整普通 CI 与 Supabase development rollback-only 成功证据；失败 run 不得替代成功证据。
3. 不虚构第二 reviewer，不把项目所有者的自我确认表述为独立复核。
4. 必须保留真实浏览器到 Nest、PostgreSQL、Redis、MinIO 的纵向证据，并精确清理数据库 fixture、Redis key 和测试对象。
5. 发现 PII 泄露、跨客户读取、地址密文/AAD 失效、购物车重复累加、幂等漂移、限流 fail-open、数据库残留或契约漂移时，立即撤销当前批次准入。
6. CH-017 不适用 staging、production、真实微信身份、真实客户数据或资金链路；第一次进入 staging 前仍须外部独立人员复核代码、安全、数据库和验收证据。

## 二十二、CH-018 B8 登录后购物基础契约

### 22.1 变更申请与决策

- **申请日期**：2026-08-27。
- **申请来源**：技术限制。既有契约缺少单商品收藏状态、可权威投影的服务端购物车、可重试的游客合并、严格所有权地址访问及闭合的个性化缓存/限流语义。
- **变更类型**：修改需求与 API 契约；新增一个收藏状态 GET，等量替换购物车项写 DTO，并闭合登录后购物基础域。
- **影响程度**：中等；影响 Store Favorites/Cart/Address API、生成 contracts、小程序 MP 页面、配置、加密、幂等、审计和 CI 门禁。
- **审批状态**：用户已明确批准，产品负责人和技术负责人批准立即执行。
- **基线**：产品/PRD `v2.4.6 / CH-018`，OpenAPI `2.4.6-ch018`。

### 22.2 已批准契约

1. 新增 `GET /store/favorites/{product_id}`。CH-018 冻结统计为 173 paths / 198 operations / 198 unique operationId / 323 schemas / 701 schema refs / 2,653 local refs / 0 dangling refs。
2. 新增 `FavoriteProductView`、`FavoriteStateResponse` 和 `CartMergeItemInput`；以必填 `quantity` 与 `selected` 的 `CartItemWriteRequest` 等量替换 `CartQuantityRequest`。所有 Product、SKU、Address 路径参数统一按 ULID 校验。
3. 收藏列表按 `created_at DESC,id DESC`；搜索只匹配 trim 后商品名称，大小写不敏感，长度 1-200。收藏投影状态闭合为 `SALEABLE | OUT_OF_STOCK | UNAVAILABLE`；商品失效后收藏仍可见，价格和图片可为 null，`is_salable=false`。
4. 收藏 PUT 只允许当前公开商品，重复收藏成功且不产生重复记录；DELETE 在记录不存在或商品失效时仍幂等成功；状态 GET 返回当前客户对指定商品的收藏布尔值。收藏写操作使用 `HASH_ONLY`，不得缓存响应正文。
5. 购物车空 GET 不写数据库，固定返回 `cart_id=null,items=[]`；仅在首次写入或合并时懒创建。删除无数据库来源的 `CartResponse.version` 和无法权威计算的 `change_flags`。
6. 购物车项包含 `selected` 与可空 `primary_image_url`，状态闭合为 `SALEABLE | INSUFFICIENT_STOCK | OUT_OF_STOCK | INACTIVE | DELETED`；合计只包含已选且 `SALEABLE` 的项目。每项数量 1-99，最多 100 个不同 SKU，GET 固定按 `created_at ASC,id ASC`。
7. 合并最多接受 100 个不重复 SKU，数量按 99 封顶，选择状态采用 `existing OR incoming`；事务全成全败。未知 SKU 阻断整个合并，已失效但存在的 SKU 可保存并返回当前状态。客户端比较本地展示快照和服务端投影，不新增价格或库存快照数据库字段。
8. 购物车写使用 Serializable 与统一重试，锁序固定为幂等事实、客户、购物车、升序 SKU/购物车项、审计；同一幂等键不得重复应用命令，重放时重新鉴权并返回当前投影，无法安全重放时返回 409 并要求刷新。
9. 地址手机号为 11 位 ASCII 数字；收件人、省、市、区各 1-80 字符，详细地址 1-300 字符；trim 后不得为空且不得包含控制字符。
10. 手机号与详细地址使用 AES-256-GCM 加密，AAD 分别绑定 `customer_address:<id>:phone_ciphertext` 和 `customer_address:<id>:detail_ciphertext`；手机号哈希复用 Store 手机密钥环，但使用独立域 `qingxu:store-address-phone:v1`。
11. 地址列表只返回脱敏摘要；详情、创建、更新仅向当前所有者返回完整值，跨客户访问统一返回 404。PATCH/DELETE 必须携带 `If-Match`，版本冲突返回 409；审计严禁记录姓名、手机号或详细地址。
12. 第一条地址自动成为默认地址；切换默认时原默认地址原子清除；删除默认地址后按 `created_at ASC,id ASC` 自动提升下一条；存在其他地址时禁止将当前默认地址直接改为非默认。
13. 新增 `CART_ITEM_LIMIT_EXCEEDED` 422 与 `DEFAULT_ADDRESS_REQUIRED` 422。全部 B8 个性化响应必须设置 `Cache-Control: no-store, private` 与 `Pragma: no-cache`；B8 写操作使用 `HASH_ONLY`，不缓存响应正文，也不新增 Outbox 事件。
14. B8 登录接口共享 CUSTOMER 与来源 IP 组合的 Redis 固定窗口限流，每 60 秒 120 次；Redis 只保存域隔离 HMAC 摘要，禁止保存原始 IP 或客户标识，Redis 异常必须 fail closed。新增 `STORE_CUSTOMER_RATE_LIMIT_MAX=120` 和 `STORE_CUSTOMER_RATE_LIMIT_WINDOW_SECONDS=60` 环境契约。
15. Prisma、两份 `0001_initial`、76 models / 59 enums 和冻结哈希保持逐字节不变，不新增 migration，`migration diff=0`。

### 22.3 批次、暂停与回退

- B8.0-B8.5 本地实现、专项验收、全仓回归和最终只读复审已完成；最终 SHA `0fc5a8d3d1f07d3b5c9fcadf7ea4ca9560a0911a` 的普通 CI Run `33141704459` 与 Supabase rollback-only Run `33142971501` 同 SHA 双绿，B8 development `GO`，CH-017 已自动失效。
- B8.1-B8.5 依次交付收藏、服务端购物车与游客合并、收货地址、小程序、真实纵向与总验收；每批 `P0=0/P1=0` 后暂停。
- B8.4 中未登录继续使用版本化本地购物车；登录后服务端购物车为权威，不得在服务异常时静默回退。合并 journal 固定幂等键并只在服务端确认后删除对应本地条目。
- 订单、库存预占、结算、支付、履约、售后、真实微信和 staging/production 不在 B8 内；订单报价与 B5-C09 预占索引风险进入 B9，支付进入 B10。
- B8.1 与 B8.2 已进入业务实现；通过关闭 Store Favorites/Cart 模块路由回退，不回退 CH-018 契约，不执行数据库降级。

完整契约、批次、验收和回退边界见 `../05-开发管理/B8-登录后购物基础.md`。B8.0-B8.5 已完成，B8 development `GO`，CH-017 已自动失效。

## 二十三、CH-019 B9 单人维护者开发门禁例外

### 23.1 变更申请与决策

- **申请日期**：2026-08-28。
- **变更来源**：团队资源约束；当前仅一名维护者，无法在同一 development 批次配置第二 reviewer。
- **变更类型**：修改交付门禁，不修改产品、API、数据库、权限、隐私或安全要求。
- **影响程度**：中等；组织独立性减少，以自动化、真实纵向测试、准确 SHA 远端双绿和无残留断言补偿。
- **审批状态**：用户已明确批准，产品负责人和技术负责人批准立即执行。
- **适用边界**：只允许 B9.0-B9.5 使用脱敏 development 数据；已随 B9.5 完成自动失效。

### 23.2 补偿控制与失效条件

1. B9.0-B9.5 严格串行，每批只有 `P0=0/P1=0` 才能暂停并由用户明确批准下一批。
2. B9.0 完成受控 development 前向迁移后，才能执行 B9 rollback-only repository smoke；不得用本地 PostgreSQL 替代 Supabase development 证据。
3. B9.5 必须在同一最终实现 SHA 上取得完整普通 CI 与 Supabase development rollback-only 成功证据；失败 run 不得替代成功证据。
4. 必须验证报价防篡改、最后库存并发、幂等重放、取消/超时竞争、流水唯一和测试事实零残留。
5. 不虚构第二 reviewer，不把项目所有者的自我确认表述为独立复核。
6. CH-019 不适用 staging、production、真实微信身份、真实客户数据或资金链路；第一次进入 staging 前仍须外部独立人员复核代码、安全、迁移和验收证据。

### 23.3 最终失效证据

- B9 最终实现 SHA `19f9ad57190b28d11922db805b39af95b2f7ba3b` 的 [普通 CI Run `33230769777`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33230769777) 与 [Supabase development rollback-only smoke Run `33233087710`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33233087710) 均为 `completed/success`，两项 `head_sha` 均精确等于该最终 SHA。
- B9.5 已完成，最终复审为 `P0=0/P1=0/P2=1`；唯一非阻断 P2 为大量正常到期订单存在时完整性扫描工作量仍未有界。B9 development `GO`，CH-019 已自动失效；该例外不得继续用于 staging、production、真实微信身份、真实客户数据或真实支付/资金链路。

## 二十四、CH-020 B9 订单、报价与库存预占契约

### 24.1 变更申请与决策

- **申请日期**：2026-08-28。
- **变更来源**：技术限制。既有结算与订单契约未将报价绑定到客户、会话、地址版本、购物项与库存/价格事实；B5-C09 还已确认预占项缺少 SKU-first 索引且库存流水业务事实缺少唯一保护。
- **变更类型**：修改需求。
- **影响程度**：中等；影响 Store Checkout/Order API、库存事实索引、Worker、小程序 MP-08/10/11、幂等/审计/Outbox 和 CI/Supabase 门禁，不改变表、列或枚举。
- **审批级别**：产品负责人 + 技术负责人。
- **审批结果**：已批准，立即执行。
- **基线**：产品/PRD `v2.4.7 / CH-020`，OpenAPI `2.4.7-ch020`。

### 24.2 已批准契约与迁移决策

1. CH-020 专项实测为 173 paths / 198 operations / 198 unique operationId / 325 schemas / 703 schema refs / 2,665 local refs / 0 dangling refs，Redocly 0 warning。
2. `CheckoutQuoteRequest` 闭合 source、本人 ULID 地址和 1-100 个不重复 SKU；`BUY_NOW` 恰好一项，`CART` 精确匹配当前已选服务端购物车项。
3. 可提交报价签发 5 分钟无状态 HMAC 凭证，绑定 CUSTOMER、会话、规范请求、地址版本、商品/SKU/价格/库存事实、quote ID 和过期时间；使用现有幂等密钥环按 `qingxu:store-checkout-quote:v1` 域派生，不落数据库、Redis、日志或审计。
4. 报价 blocker 闭合为 `CART_SELECTION_CHANGED | ITEM_UNAVAILABLE | INSUFFICIENT_STOCK`；阻断报价仍返回 200，但不签发 token/hash/expiry。B9 运费固定 `0.00`。
5. `OrderSubmitRequest` 必须重复精确 source/address/items，并提交 `quote_id`、`quote_token` 和 `confirmation_hash`。创建订单返回 201，使用 `HASH_ONLY`；完整幂等重放先于 token 过期校验并返回同一订单当前投影。用户明确确认后允许唯一受控客户端例外：H5 `sessionStorage` 或小程序沙箱保存 CUSTOMER 绑定的精确请求、固定幂等键与创建时间，最长 24 小时。不得保存地址明文、会话 token、响应正文或错误正文；成功、确定失败、退出、注销、客户不匹配或 TTL 到期时清理。
6. 新增 409：`CHECKOUT_QUOTE_EXPIRED`、`CHECKOUT_QUOTE_MISMATCH`、`CHECKOUT_REQUOTE_REQUIRED`、`ORDER_NOT_CANCELLABLE`。取消使用 `If-Match` 与 `HASH_ONLY`，不再携带原因请求体。
7. 创建订单在 Serializable 事务中保存订单/商品/地址快照与归因候选，创建 30 分钟 ACTIVE 预占、原子增加 `locked_qty` 并写 `ORDER_RESERVE` 流水。CART 成功后只删除本次提交的已选项；BUY_NOW 不修改购物车。
8. 用户取消将预占置为 `RELEASED`，超时 Worker 置为 `EXPIRED`；两者原子减少 `locked_qty`、写唯一 `ORDER_RELEASE` 流水、关闭订单并写审计/Outbox。发现任何 payment intent 时 fail closed，留给 B10 处理。
9. 首次下单没有既有 order 且 B9 不创建 payment intent，锁序为 `idempotency -> account/customer -> CART/cart items（仅 CART）-> address -> binding/agent -> brand -> category -> product -> SKU ID ASC -> inventory_balance ID ASC -> insert order/reservation/snapshots -> ledger -> audit/outbox`。主动取消/Worker 才使用 `idempotency（Worker 跳过） -> order -> payment_intent -> SKU ID ASC -> inventory_balance ID ASC -> inventory_reservation ID ASC -> ledger -> audit/outbox`。候选 reservation/SKU ID 可无锁预读但只用于定位，锁内必须重验；禁止混用两条路径或先锁 reservation 再锁 SKU/balance。Product/SKU lifecycle 与库存调整在共享库存尾部同样遵循 SKU → balance → reservation。
10. 两份 `0001_initial` 保持逐字节不变，新增 `0002_b9_inventory_fact_indexes`：`inventory_reservation_item(sku_id,reservation_id)` 普通索引与 `inventory_ledger(business_id,sku_id,ledger_type) WHERE business_id IS NOT NULL` 条件唯一索引。迁移先检测历史重复事实，全部使用事务型 DDL。
11. 五个 B9 operation 都使用 CUSTOMER 鉴权、CUSTOMER+IP 120/60 fail-closed 限流、`Cache-Control: no-store, private` 和 `Pragma: no-cache`。支付、退款、物流、售后、Admin/Agent 订单不在 B9 内。

### 24.3 批次、迁移门禁与回退

- B9.0 仅修改治理、产品/API/架构/数据库/风险/原型、OpenAPI、generated contracts、契约门禁与 0002 前向索引迁移；B9.0 全部验收前不开始 Checkout/Order repository、API 或小程序工程页面。
- B9.1-B9.5 依次交付报价、下单/预占、查询/取消/超时 Worker、小程序 MP-08/10/11 和总验收；每批 `P0=0/P1=0` 后暂停。
- B9 数据库/API 验收必须并发覆盖下单、取消和超时释放与 Product/SKU lifecycle confirm、库存人工调整；不得出现 PostgreSQL `40P01`、反向锁序、重复释放或库存不变式破坏。
- 新增 main-only 受保护 Supabase development migration workflow，验证项目 ref、精确 SHA、main、TLS 和迁移历史后才执行 deploy；与 rollback-only smoke 共用数据库 concurrency group。
- B9.0 以前可整体回退 CH-020 文档、OpenAPI、contracts 和原型；业务实现开始后只关闭 Store Checkout/Order 路由和小程序入口。`0002` 是加法索引迁移，代码回退时保留，不执行数据库降级。

完整契约、锁序、批次、验收和回退边界见 `../05-开发管理/B9-订单报价与库存预占.md`。B9.0-B9.5 已完成，最终实现 SHA `19f9ad57190b28d11922db805b39af95b2f7ba3b` 的普通 CI Run `33230769777` 与 Supabase development rollback-only smoke Run `33233087710` 均为 `completed/success` 且 `head_sha` 一致；最终复审为 `P0=0/P1=0/P2=1`，B9 development `GO`，CH-019 已自动失效。唯一非阻断 P2 为大量正常到期订单存在时完整性扫描工作量仍未有界；staging、production、真实微信身份、真实客户数据和真实支付/资金链路继续 `NO-GO`。

## 二十五、CH-021 B10 单人维护者开发门禁例外

### 25.1 变更申请与决策

- **申请日期**：2026-08-29。
- **变更来源**：团队资源约束；当前只有一名维护者，无法为同一 development 变更配置第二 reviewer。
- **变更类型**：修改交付门禁，不修改产品、API、数据库、权限、隐私、支付或安全要求。
- **影响程度**：中等。
- **审批结果**：产品负责人和技术负责人已批准，立即执行。
- **适用边界**：仅 B10.0-B10.6、Mock Provider、脱敏 development；B10.6 完成后自动失效。

### 25.2 补偿控制与失效条件

1. B10.0-B10.6 严格串行，每批只有 `P0=0/P1=0` 才可暂停并由用户批准下一批。
2. 以契约生成、迁移回放、真实纵向、故障注入、敏感信息扫描和最终同 SHA 远端双绿补偿第二 reviewer 缺失；不得虚构独立复核。
3. `0003` 只能通过受控 main-only Supabase development migration 部署；最终实现还必须在同一 SHA 取得普通 CI 与 rollback-only smoke 成功证据。
4. CH-021 不适用 staging、production、真实微信身份、真实客户数据或真实资金链路；首次 staging 仍须外部独立人员复核。
5. B10.6 完成后，无论是否立即进入后续阶段，本例外自动失效，不得沿用。

### 25.3 最终失效记录

- 最终实现 SHA `f5e59169b53a97704711c3aae3049e5b5d16a930` 的[普通 CI Run `33305811318`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33305811318)与[Supabase development rollback-only smoke Run `33306877575`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33306877575)均为 `completed/success`，两项 `head_sha` 精确一致。
- B10.6 最终门禁已闭合，B10 development `GO`；CH-021 随即自动失效，不得用于后续 development、staging 或 production。

## 二十六、CH-022 B10 支付、对账与迟到支付退款契约

### 26.1 变更申请与决策

- **申请日期**：2026-08-29。
- **变更来源**：技术限制。B9 只允许订单在无支付意图时本地关闭，尚缺可恢复支付意图、支付结果唯一事实、关单对账和迟到支付退款闭环。
- **变更类型**：修改需求与接口契约。
- **影响程度**：中等；影响支付 API、订单/库存/佣金事务、Worker、MP-09、ADM-10、环境契约和前向索引迁移。
- **审批级别**：产品负责人 + 技术负责人。
- **审批结果**：已批准，立即执行。
- **基线**：产品/PRD `v2.4.8 / CH-022`，OpenAPI `2.4.8-ch022`。

### 26.2 已批准边界

1. B10 development 只注册 Mock Provider；Mock result 仅在非 production 的 `development|test + MOCK` 注册，`test` 只用于 B10.6 真实 HTTP 纵向；production 配置 Mock 必须拒绝启动，production/WECHAT 不注册 Mock result。真实微信回调保留契约但不注册路由。
2. 支付写操作使用 `HASH_ONLY` 幂等策略；Provider 调用全部位于数据库事务外，未知状态、超时和 Redis 故障均 fail closed。
3. 支付成功必须原子结算订单、消耗预占、同时扣减 `physical_qty`/`locked_qty`、写唯一流水，并在支付时复核归因和冻结佣金规则快照。可重建的 `product.sales_count` 只在结算收敛时递增；首次 `MANUAL_REQUIRED` 不递增、补偿成功只递增一次，达到 PostgreSQL `integer` 上限后饱和，缓存异常不得否认 Provider 成功事实。
4. 用户取消和超时遇到活动意图时先进入 `CLOSE_PENDING`，由 Worker 在事务外 query/close；Provider 未确认前保持库存锁定，消费者取消返回 `202 + CLOSE_PENDING` 当前投影。
5. 已关闭订单收到支付成功时订单保持关闭，不恢复库存、履约、归因或佣金；只写唯一迟到成功事实并创建唯一全额 `LATE_PAYMENT` 退款，退款 attempt 从 `INITIATED` 开始。退款失败或未知进入 `MANUAL_REQUIRED`。
6. ADM-10 待办只允许 `PAYMENT_INTENT | PAYMENT_SETTLEMENT | LATE_PAYMENT_REFUND`，提供只读列表和幂等 reconcile 触发；200/202 安全响应均不得携带 capability，不允许管理员直接修改订单、意图或支付尝试状态。MP-09 不信任客户端成功状态。
7. 新增事务型 `0003_b10_payment_fact_indexes`，只增加“每支付意图最多一个成功事实”和“每订单最多一个迟到退款”两个条件唯一索引；创建前先检测历史重复事实，既有迁移保持逐字节不变。
8. B10 不包含普通售后退款、物流、履约、真实微信验签、证书轮换、Provider ACK、staging 或 production。

### 26.3 当前实施状态与回退

- B10.0-B10.6 实施与验收已完成；契约为 173/198/198/326，全仓 2,067 passed/130 designed skips，B10 数据库 85/85、Miniapp 12/28、ADM-10 15/15、真实纵向 1 passed，完整 `0001→0004`、20 个 partial indexes 与 `migration diff=0` 通过。数据库与纵向独立复审为 `P0=0/P1=0/P2=0`，TR-026、QA-005、`PAYMENT_TIMEOUT` 和跨提交纵向风险已关闭。最终 SHA `f5e59169b53a97704711c3aae3049e5b5d16a930` 已取得同 SHA 普通 CI 与 Supabase development rollback-only 双绿，B10 development `GO`，CH-021 已自动失效。
- B10.1 固定采用 7 天 Mock Redis 状态 TTL 与 60 秒初始 `next_reconcile_at`；Provider 网络调用全部在数据库事务外，超时、Redis 故障或 `UNKNOWN` 均 fail closed。既有 `CREATING` 一律先 query，只有 `NOT_FOUND` 才 create；`OPEN` 与订单 `PROCESSING/version+1` 原子提交，明确终态与订单 `UNPAID` 原子恢复，陈旧 `If-Match` 新命令返回 409。Mock result 仅在非 production 的 `development|test + MOCK` 注册且只写 Callback Inbox，`test` 只用于 B10.6 真实 HTTP 纵向，production/WECHAT 不注册；入口不直接修改支付或订单业务状态。
- B10.0-B10.6 依次交付治理/契约/迁移、Provider 与支付意图、支付结算、取消超时与对账、迟到退款、小程序与后台、总验收。
- B10.1 已完成，不能再整体回滚 CH-022 基线；`0003` 作为加法索引保留，不执行数据库降级。
- 当前可通过关闭支付意图路由、Mock 结果入口、支付结果 Worker handler、小程序支付入口以及 ADM-10 导航/路由回退；既有 0003/0004 保留，不执行数据库降级。

完整契约、批次、验收和回退边界见 `../05-开发管理/B10-支付对账与迟到支付退款.md`。

## 二十七、CH-023 B10 佣金头寸触发器最小权限修复

### 27.1 变更申请与决策

- **申请日期**：2026-08-30。
- **变更来源**：技术限制。B10.2 真实 PostgreSQL 集成在 `mall_runtime` 写入正佣金 position 时返回 `42501 permission denied for table order_item_commission_snapshot`；原因是冻结 `0001_initial` 的 `enforce_commission_position_snapshot()` 通过 `SELECT ... FOR SHARE` 读取不可变快照，而运行角色按最小权限只拥有该表 `SELECT/INSERT`，明确没有 `UPDATE`。
- **变更类型**：修改需求，不改变消费者、管理员或 Provider 的在线接口与业务状态机。
- **影响范围**：开发进度、技术架构、测试范围、数据迁移；不影响功能范围、用户体验、第三方接口或预算。
- **影响程度**：中等，影响 B10.2 支付结算、佣金数据库触发器和 development 迁移/回滚验证。
- **审批级别**：产品负责人 + 技术负责人。
- **审批结果**：已批准，立即执行。
- **产品/API 基线**：继续使用 `v2.4.8 / CH-022` 与 OpenAPI `2.4.8-ch022`，paths、operations、schemas 和 generated contracts 均不因 CH-023 改变。

### 27.2 已批准修复

1. 新增事务型前向迁移 `0004_b10_commission_position_trigger_fix`；两份迁移镜像必须一致，既有 `0001_initial`、`0002_b9_inventory_fact_indexes` 和 `0003_b10_payment_fact_indexes` 逐字节不变。
2. `0004` 只以 `CREATE OR REPLACE FUNCTION` 更新 `public.enforce_commission_position_snapshot()`：保留 snapshot ID 不可变与 `original_commission` 必须等于不可变快照的校验，移除读取快照时不必要的 `FOR SHARE`。
3. 函数显式使用 `SECURITY INVOKER`；不得改为 `SECURITY DEFINER`，不得向 `mall_runtime` 授予 `order_item_commission_snapshot` 的 `UPDATE` 或 `DELETE`，也不得扩大 Data API 角色权限。
4. 该修复不新增表、列、枚举、索引、RLS policy 或在线 API；不可变快照仍由权限、外键和触发器校验共同保护。
5. `0004` 部署仍使用 main-only 受控 Supabase development migration Workflow，与 rollback-only smoke 共用数据库 concurrency group；代码回退保留已部署函数修复，不执行数据库降级。

### 27.3 影响、风险与验收

| 维度 | 影响与控制 |
|---|---|
| 开发进度 | B10.3/B10.4 实现、本地验收及准确 SHA 三项远端证据已完成；B10.5/B10.6 实施、总验收与最终 SHA 同 SHA 远端双绿均已完成 |
| 技术架构 | 只调整数据库触发器的锁语义；不改变 Payment、Order、Inventory 或 Commission 模块边界 |
| 测试范围 | 增加 0001→0002→0003→0004 空库/升级回放、函数定义与权限指纹、真实 `mall_runtime` 正佣金和 0% 佣金路径回归 |
| 数据迁移 | 仅替换函数定义，不重写业务数据；受控 development deploy 前后均验证迁移历史与 `migration diff=0` |

验收必须同时满足：本地完整迁移链与冻结检查通过；`mall_runtime` 能创建正佣金 snapshot/position/ledger 且仍不能更新不可变 snapshot；0% AGENT 路径保留快照和 `NONE` position、DIRECT 路径不创建佣金事实；准确实现 SHA 的普通 CI 通过；同一 SHA 的 Supabase development migration 成功后再执行 rollback-only smoke，并确认正佣金与 0% 路径全部通过且事务外无残留。CH-023 与 B10.2 已完成，B10.3/B10.4 后续准确 SHA 三项远端门禁也已闭合；B10.5/B10.6 验收已完成。最终 SHA `f5e59169b53a97704711c3aae3049e5b5d16a930` 的普通 CI 与 Supabase development rollback-only smoke 同 SHA 双绿，B10 development `GO`。

### 27.4 当前状态

CH-023 已批准并立即执行。`0004` 与 B10.2 既有结论保持；B10.4 既有准确 SHA 三项远端证据有效，B10.5/B10.6 验收及最终同 SHA 远端双绿均已完成。B10 development `GO`，CH-021 已自动失效；CH-023 仍不改变 v2.4.8/CH-022 在线契约，staging、production 与真实资金链路仍为 `NO-GO`。

## 二十八、B10.3 取消、超时与对账实施状态

- B10.3 已完成，采用 `claim → Provider query/close（事务外）→ finalize` 三段流程。活动 `CREATING/OPEN` intent 先标记 `CLOSE_PENDING`；Provider 明确不可支付的终态才允许订单关闭并释放预占，`UNKNOWN` 保留订单和库存锁定。
- 超时 Worker 使用 `pay_expires_at ASC, id ASC` 稳定候选游标，健康候选同样推进游标；对账复用现有 poll/batch/retry 配置，不使用 Redis lease。
- 本地定向验收：`@qingxu/database` `store-order.repository.spec.ts` 为 `14 passed`；`@qingxu/worker` `order-timeout.service.spec.ts` 为 `14 passed`。覆盖稳定游标、CLOSE_PENDING claim、Provider 调用在事务外、明确终态关单和 UNKNOWN 不释放预占。
- 准确实现 SHA `524ffe84cc6e88d0e0b6b1fb9c132a9dc752317f` 的普通 CI Run `33288346901`、Supabase development migration Run `33290094491` 与 rollback-only smoke Run `33290244592` 均为 `completed/success`。本行保留 B10.3 退出时“后续批次和 B10.6 总验收闭合前保持 `NO-GO`”的历史结论；当前状态见第三十一节。

## 二十九、B10.4 迟到支付与全额自动退款实施状态

- 已关闭订单收到可信成功事件时，只为符合关闭/释放/金额闭包的订单原子写唯一 `SUCCEEDED_LATE` attempt 与唯一 `LATE_PAYMENT` 全额退款；订单保持 `CLOSED/NOT_STARTED`，不恢复库存、归因、佣金或销量。
- Worker 采用 `prepare → claim → Provider refund（事务外）→ finalize`；固定 refund number 允许响应丢失后同事实重试。成功收敛为 `LATE_SUCCESS_REFUNDED`，Provider 失败、未知或异常收敛为 `MANUAL_REQUIRED`。
- 稳定支付交易身份允许不同 event ID/时间戳的重复事件无害收敛；另一个 Worker 已完成后，陈旧 claim/finalize 返回 `TERMINAL/REPLAY`，不会再次调用 Provider 或重复退款。
- 本地验收包括数据库仓储 61 passed、Worker callback/module 31 passed，以及真实 `mall_runtime` rollback 7 passed；两路只读复审均为 `P0=0/P1=0`。Prisma、0001-0004、OpenAPI 与 generated contracts 未修改。
- B10.4 已在准确实现 SHA `8998e4a51605e97a952f13f4538811c19de14b63` 取得普通 CI Run `33293709228`、Supabase development migration Run `33294573472` 与 rollback-only smoke Run `33294644725` 三项成功证据，批次已闭合。本行保留 B10.4 当时的阶段证据；B10 整体最终状态见第三十一节。

## 三十、B10.5 小程序支付结果与 ADM-10 实施状态

- MP-09 只从服务端订单投影确认支付结果；订单详情按服务端动作开放 PAY/CANCEL。支付 journal 仅保存订单 ID、版本和幂等键，Provider capability 只保存在内存中。
- ADM-10 只展示 `PAYMENT_INTENT | PAYMENT_SETTLEMENT | LATE_PAYMENT_REFUND` 三类待办并提供幂等 reconcile；200/202 精确表达已收敛/仍待确认，不提供人工改状态、人工退款或履约能力。
- reconcile 的 payment-intent owner lock 使用独立 `coordinationPool(max=1)`，覆盖重读、事务外 Provider I/O 和最终提交；Callback Inbox 回排与 Worker 消费按同一 Inbox 键互斥。锁超时、连接丢失或无法安全解锁均 fail closed。
- Provider 配置或 intent locator 在外部副作用前校验。终态非成功 intent 与订单/预占分叉时，以 `PAYMENT_INTENT/CLOSE_PENDING + ORDER_CLOSE_INCOMPLETE` 发现并仅执行本地 Serializable 关单补偿，不再次调用 Provider。
- CH-022 契约勘误确认迟到退款已收敛分支允许当前 intent 为 `SUCCEEDED`，同时兼容既有 `CLOSED/EXPIRED`；OpenAPI 统计仍为 173/198/198/326，不新增迁移。
- 本行保留 B10.5 退出时的历史证据：本地全仓单元/服务测试为 **2,052 passed / 127 designed skips**；B10 Miniapp 12 passed/28 skipped、ADM-10 15 passed/15 skipped，覆盖 375/390/414/1024/1440 五视口，退出复审为 `P0=0/P1=0`。B10 整体最终状态见下节。

## 三十一、B10.6 总验收与最终远端门禁状态

- B10.6 本地实现与总验收已完成：全仓单元/服务测试 **2,067 passed / 130 designed skips**；B10 数据库 **85/85 passed**；B10 Miniapp **12 passed / 28 skipped**、ADM-10 **15 passed / 15 skipped**；真实浏览器 → Nest → PostgreSQL/Redis/MinIO/Worker/Mock Provider 纵向 **1 passed**。
- 契约保持 **173 paths / 198 operations / 198 unique operationId / 326 schemas**；完整 `0001→0002→0003→0004` 回放、20 个 partial indexes、冻结哈希、Prisma validate 与 `migration diff=0` 均通过。
- TR-026 畸形历史成功事实、QA-005 多商品销量原子回滚、`PAYMENT_TIMEOUT` 显式迟到退款及跨提交纵向风险已关闭；数据库与纵向独立复审均完成，合计 `P0=0/P1=0/P2=0`。
- 普通 CI 已接入 `db:test-b10-payments`、`e2e:b10` 与 `e2e:b10:vertical`；Supabase development rollback-only workflow 已接入 B10 支付/关单/库存/归因/佣金/迟到退款/对账仓储 smoke。
- Mock result 路由只在非 production 的 `development|test + MOCK` 注册，`test` 仅用于 B10.6 真实 HTTP 纵向；production/WECHAT 始终不注册。真实微信路由仍未实现验签、证书轮换或 ACK。
- 最终实现 SHA `f5e59169b53a97704711c3aae3049e5b5d16a930` 的[普通 CI Run `33305811318`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33305811318)与[Supabase development rollback-only smoke Run `33306877575`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33306877575)均为 `completed/success`，两项 `head_sha` 精确一致。B10.6 最终门禁已闭合，B10 development `GO`，CH-021 已自动失效；本结论仅覆盖 Mock Provider 与脱敏 development，staging、production、真实客户数据、真实微信和真实支付/资金链路继续 `NO-GO`。

## 三十二、CH-024 B11 订单履约与物流契约

### 32.1 变更申请与决策

- **申请日期**：2026-08-30。
- **变更来源**：C，业务调整。
- **变更类型**：B，修改需求。
- **影响范围**：A、B、D、E，即功能范围、开发进度、测试范围和用户体验；不涉及数据迁移。
- **影响程度**：C，重大。
- **审批级别**：C，CCB。
- **审批结果**：A，已批准。
- **执行时机**：A，立即执行。
- **基线**：产品/PRD `v2.4.9 / CH-024`，OpenAPI `2.4.9-ch024`；B11.0 本地全量门禁通过后已冻结。

### 32.2 已批准业务与状态边界

1. B11 只交付已支付订单的一单一包裹履约。发货前必须同时满足 `payment_status=PAID`、`payment_resolution=NORMAL`、`order_status=PENDING_SHIPMENT`、`fulfillment_status=READY_TO_SHIP`，不存在活动售后，且请求项精确等于每行 `quantity - pre_shipment_refunded_qty - shipped_qty`。
2. 总部录入承运商、运单号和全部剩余可发数量后，原子创建唯一 `SHIPPED` 包裹及包裹项，并把订单推进为 `SHIPPING/SHIPPED`。重复或并发发货不得产生第二个包裹；包裹创建后不允许取消或替换。
3. 人工物流 `STATUS` 只允许 `SHIPPED -> IN_TRANSIT -> DELIVERED` 单向推进。`TRACKING_CORRECTION` 只追加不可变纠错事实并更新当前承运信息，不改写历史，也不改变物流状态；消费者时间线按 `occurred_at ASC,event_id ASC` 稳定排序。
4. 消费者确认收货和总部填写原因后的兜底完成，只允许本人订单或授权总部人员操作已支付、支付裁决正常、处于 `SHIPPING` 且无活动售后/未解决支付异常的唯一包裹。若包裹尚未 `DELIVERED`，完成事务只封存包裹/履约轴为 `DELIVERED`，不得伪造跳过的人工物流节点。
5. 两条完成路径分别写 `COMPLETED/CUSTOMER_CONFIRMED` 与 `COMPLETED/ADMIN_FORCED`，并在同一 Serializable 事务中冻结当前 `PUBLISHED` 业务规则、写售后截止时间、把每个 `EXPECTED` position 的当前 `expected_remaining` 精确一次结转为 `AVAILABLE`、写 `AVAILABLE_CREDIT` 和钱包/审计/Outbox。消费者与总部并发完成只能成功一次。
6. 总部订单列表/详情只返回经营所需掩码投影。完整履约地址仅由专用接口在 `SUPER_ADMIN + ORDER_FULFILLMENT_PII_READ`、固定用途和必填原因下读取，成功、失败和拒绝均逐次审计；地址明文不得进入普通详情、日志、截图或导出。
7. B11 复用 Store 本人物流/确认收货两个 operation，以及 Admin 订单列表、详情、履约地址、发货、物流事件、兜底完成六个 operation，不新增 path 或 operation。全部个性化响应禁止缓存，写操作使用 ULID、`If-Match`、`Idempotency-Key` 和 `HASH_ONLY`。

### 32.3 契约与数据库门禁

- 契约实测为 `173 paths / 198 operations / 198 unique operationId / 326 schemas / 705 schema refs / 2,695 local refs / 0 dangling refs`；CH-024 专项检查、OpenAPI 解析、Redocly、闭合 Schema、generated contracts 二次生成/构建与漂移检查均已通过。
- B11.4 将三项实现期可执行性缺口登记为 CH-024 实施勘误：Admin complete 必须精确返回冻结的 `AdminOrderResponse`；`OrderPackageDetailView` 必须返回 `version>=1` 供包裹 `If-Match` 使用；浏览器发送非 ASCII `X-Access-Reason` 时使用 `UTF-8''` 加 percent-encoded UTF-8，服务端严格解码后再校验。勘误不增加 path、operation、schema、权限或业务状态，也不触发数据库变更。
- B11 零迁移。两份 Prisma schema、两份 `0001_initial` 及既有 `0002→0004` 逐字节不变；冻结检查、Prisma validate 与 remote `migration diff=0` 均已通过。B11.0 未运行 B11 最终普通 CI 或 Supabase rollback-only smoke，也不宣称这些远端门禁已通过。
- 现有 `shipment.order_id UNIQUE` 保证一单最多一个包裹；现有订单/订单项、包裹/包裹项、物流事件、佣金头寸/流水与钱包模型足够承载 B11。数据库未直接强制跨表状态机、包裹项归属和精确发货集合，这些事实必须在 Serializable 事务中按稳定锁序重验。
- B11.0 只允许治理、产品/API/架构/数据库/风险说明、OpenAPI、generated contracts、契约检查和静态原型说明同步，不得创建 B11 repository、controller/service、Worker handler 或工程前端业务代码。
- CH-025 曾仅允许 B11.1-B11.5 的脱敏 development 以自动化、逐批暂停、最终同 SHA 双绿和无残留证据补偿第二 reviewer 缺失。B11.0-B11.5 与最终同 SHA 双绿均已完成，B11 development `GO`，CH-025 已自动失效。不得虚构第二 reviewer，也不得把该例外用于后续阶段、staging、production、真实客户、真实支付或真实物流。

### 32.4 实施批次

| 批次 | 交付范围 | 退出条件 | 当前状态 |
|---|---|---|---|
| B11.0 | CH-024 治理、产品/技术契约、OpenAPI、generated contracts、冻结数据库与静态说明 | Redocly、专项契约、生成漂移、冻结数据库与 `migration diff=0` 全部通过，`P0=0/P1=0` | **已完成；CH-025 已批准并进入 B11.1** |
| B11.1 | Admin 订单列表/详情、Store 当前投影、受控履约地址与全路径审计 | 权限、跨主体 404、PII/no-store、审计与查询稳定性通过，`P0=0/P1=0` | **已完成本地验收** |
| B11.2 | 一单一包裹发货、人工物流状态/纠错、Store 本人物流 | 精确可发集合、活动售后阻断、唯一包裹、幂等、版本与并发矩阵通过 | **已完成本地验收；`P0=0/P1=0/P2=1`** |
| B11.3 | Store 确认收货、Admin 兜底完成、规则/售后期限冻结和佣金一次入账 | 双完成/退款竞争、状态封存、position/ledger/wallet 唯一及审计/Outbox 通过 | **已完成本地工程门禁；`P0=0/P1=0/P2=2`** |
| B11.4 | MP-10/11/12 与 ADM-09/10/11 工程交互 | 五视口及 loading/empty/401/403/404/409/422/429/500/慢请求/重复点击通过 | **已完成本地工程门禁；`P0=0/P1=0`；暂停在 B11.5 前** |
| B11.5 | 数据库、API、前端、真实纵向和全仓总验收 | `db:test-b11-fulfillment`、`e2e:b11`、`e2e:b11:vertical` 接入 CI；最终同 SHA CI+Supabase rollback-only 双绿 | **已完成；最终 SHA `c8e42c4…` 同 SHA 双绿；B11 development `GO`；CH-025 已自动失效** |

每批均在 `P0=0/P1=0` 后暂停并取得下一批明确准入。B11.5 实现、无残留、复审和最终同 SHA 双绿均已完成；B11 development `GO`，CH-025 已自动失效。

### 32.5 排除、回退与剩余条件

- B11 明确排除普通售后/消费者退款、人工退款、退货验货、第三方物流 Provider/真实轨迹、多仓、多包裹、拆单、部分先发、自动确认收货、创建后取消/替换包裹、完整 Agent 订单扩展、真实微信、真实资金、staging 和 production。
- Admin 不开放人工修改订单、支付、退款、履约或佣金状态，也不得绕过状态机；当前静态画板不是 repository、API、数据库或真实纵向证据。
- B11.1-B11.5 已完成本地实现与门禁；通过关闭 Store/Admin 履约查询、发货、物流与订单完成写路由和前端导航回退，不执行数据库降级。
- CH-025 只覆盖 B11.1-B11.5 的脱敏 development，并须在 B11.5 完成后自动失效；它不适用于 staging、production、真实客户数据、真实支付或真实物流。首次 staging 准入仍须外部独立复核。

完整准入、锁序、测试和回退计划见 `../05-开发管理/B11-订单履约与物流.md`。B11.0-B11.5 与最终远端同 SHA 双绿均已完成；B11 development `GO`，CH-025 已自动失效。

## 三十三、CH-025 B11 单人维护者开发门禁例外

### 33.1 变更申请与决策

- **申请日期**：2026-08-31。
- **变更来源**：团队资源约束；当前只有一名维护者，无法为同一 B11 development 变更配置第二 reviewer。
- **变更类型**：修改交付门禁，不修改产品、API、数据库、权限、PII、履约、佣金或安全要求。
- **影响程度**：中等；组织独立性减少，以自动化、逐批暂停、真实纵向、准确 SHA 远端双绿和无残留断言补偿。
- **审批级别**：产品负责人 + 技术负责人。
- **审批结果**：用户以“进入下一阶段”明确批准，立即执行。
- **适用边界**：仅 B11.1-B11.5、Mock Provider、脱敏 development；B11.5 完成后自动失效。
- **当前状态**：B11.0-B11.5 与最终远端同 SHA 双绿均已完成；B11 development `GO`，CH-025 已自动失效。
- **B11.1 证据**：独立 `FulfillmentRepository` Admin/Store 查询投影、Admin 订单列表/详情/受控地址、Store 物流投影、共享 `display_status`、PII 权限、五分钟地址展示与成功/失败/拒绝审计已完成；database `553 passed / 83 skipped`、API `1029 passed / 44 skipped`、platform-core `135 passed`，全仓 test/build/typecheck/lint、契约、冻结数据库、敏感扫描和差异检查通过，且无 Prisma/migration 改动。
- **B11.2 证据**：唯一包裹、精确剩余可发集合、活动售后阻断、手工 STATUS/纠错、Store 本人物流、HASH_ONLY、乐观锁和锁内授权重验已完成；API focused `136 passed`、DB focused `114 passed / 3 environment-mode skipped`、隔离 PostgreSQL 18 full `3/3`、Supabase rollback-only `1 passed / 2 full-only skipped` 且 fixture=0，全仓 `2254 passed / 133 skipped`。Prisma/migration 零改动，复审 `P0=0/P1=0/P2=1`；P2 仅记录 CH-024 未冻结相邻 STATUS 事件时间单调性，本批保持现行 `DELIVERED >= shipped_at` 约束。
- **B11.3 证据**：Store 确认收货、Admin 兜底完成、状态封存、规则/售后期限冻结、佣金/钱包精确一次、审计/Outbox/幂等原子事务已完成；API `81 files passed / 13 skipped；1,093 passed / 44 skipped`，DB `32 files passed / 16 skipped；611 passed / 96 skipped`，隔离 PostgreSQL full `10/10`，Supabase development rollback-only `9 passed / 1 full-only skipped`。lint `0 errors / 437 existing warnings`，顺序 typecheck/build、契约 `173/198/198/326/705/2,695/0`、冻结数据库、敏感扫描和差异检查通过；Prisma/migration 零改动。复审 `P0=0/P1=0/P2=2`：P2 为 HASH_ONLY 完成重放在未来普通全额退款把 `completion_reason` 改为 `FULL_REFUND_AFTER_SHIPMENT` 后将对原键 fail-closed 500；普通退款不属于 B11。full 集成具备完整 cleanup SQL，但自动无残留断言仅覆盖核心 fixture，全面枚举留在 B11.5。普通退款仅验证活动退款阻断和历史部分退款剩余 4.50 结转；正佣金钱包缺失 fail-closed，钱包预创建是上游前置条件，当前无生产自动建钱包路径。
- **B11.4 历史证据**：MP-10/11/12 与 ADM-09/10/11 工程交互完成；Miniapp `21 files / 288 passed`，Admin `3 files / 20 passed`；两端 Mock API Playwright 均为 `13 passed / 17 designed skips`。当批复审 `P0=0/P1=0/P2=2`，两个 designed vertical 项已在 B11.5 本地纵向验收中闭合。
- **B11.5 本地证据**：DB full `5 files / 76 passed`；受控 Supabase rollback 本地执行 `2 files / 10 passed / 3 full-only skips`（非远端 workflow 证据）；Miniapp/Admin Mock API 各 `13 passed / 17 skips`；vertical `1 passed`，覆盖真实五分钟、clipboard denied 实际 copy 失败、Worker/MinIO/Redis/佣金与零残留。`pnpm check` 通过，lint `0 errors / 437 existing warnings`；本地复审 `P0=0/P1=0/P2=1`。唯一 P2 为普通 CI 固定 60 分钟总时限的裕量风险，须由最终远端运行证明。
- **B11.5 远端证据**：最终实现 SHA `c8e42c44a6a31099c00070294a85f628913165df`；[普通 CI Run `33379627199`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33379627199) 与 [Supabase development rollback-only smoke Run `33385726528`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33385726528) 均为 `completed/success` 且 `head_sha` 一致。普通 CI 60 分钟裕量 P2 已由成功运行关闭；B11 development `GO`，CH-025 已自动失效。

### 33.2 补偿控制

1. B11.1-B11.5 严格串行；每批必须完成自动化、敏感信息扫描、故障/并发验证、无残留检查与只读复审，在 `P0=0/P1=0` 后暂停，并取得用户对下一批的明确准入。
2. 不虚构第二 reviewer，不把项目所有者自我确认表述为独立复核；文档中的“复审”只能如实标明自动化、静态检查或实际参与者。
3. B11.1 必须证明 Admin/Store 数据范围、跨主体 404、履约地址用途/原因/权限、PII no-store 与成功/失败/拒绝审计；任一 PII 泄露或越权立即撤销当前批次准入。
4. B11.2/B11.3 必须覆盖唯一包裹、精确可发集合、活动售后阻断、物流单向性、消费者/Admin 双完成竞争、佣金/钱包精确一次和事务回滚；不得以唯一约束替代 Serializable 锁内重验。
5. B11.4 必须覆盖五视口、严格响应解码、服务端动作权威、409 刷新重试、慢请求/丢包/重复点击和前端敏感持久化扫描。
6. B11.5 必须在同一最终实现 SHA 上取得完整普通 CI 与 Supabase development rollback-only smoke 成功证据，并证明数据库 fixture、审计、幂等、Outbox、Redis key 和对象无残留；失败 run 或 B10 历史 run 不得替代。
7. CH-025 不放宽 CH-024 契约、零迁移、冻结数据库、RBAC、PII、幂等、乐观锁、审计、Outbox、限流或测试标准，也不授权普通售后、第三方物流、自动确认、多包裹、真实支付或真实物流。

### 33.3 失效与外部边界

- CH-025 在 B11.5 完成时自动失效，无论是否立即进入后续阶段；不得沿用到 B12 或其他 development 批次。
- CH-025 从不适用于 staging、production、真实客户数据、真实微信身份、真实支付或真实物流。
- 首次进入 staging 前必须由外部独立人员复核代码、安全、数据库、PII、履约、佣金和验收证据；单人维护者的自我确认不能替代该门禁。
- 若 B11.5 最终同 SHA 双绿、无残留或 `P0=0/P1=0` 未闭合，B11 必须保持非 `GO`，CH-025 也不得被描述为已完成或已失效。

## 三十四、CH-026 B12 售后与普通退款契约解阻

### 34.1 变更申请与决策

- **申请日期**：2026-09-01。
- **变更来源**：D，技术限制。
- **变更类型**：B，修改需求。
- **影响范围**：A、B、C、D、E、F，即功能范围、开发进度、技术架构、测试范围、用户体验和数据迁移。
- **影响程度**：C，重大。
- **审批级别**：C，变更控制委员会（CCB）。
- **审批结果**：A，批准变更。
- **执行时机**：A，立即执行。
- **基线**：产品/PRD 升级为 `v2.4.10 / CH-026`，OpenAPI 升级为 `2.4.10-ch026`；契约实测为 `173 paths / 198 operations / 198 unique operationId / 326 schemas / 706 schema refs / 2,726 local refs / 0 dangling refs`。

### 34.2 变更原因与范围

1. B11 已明确排除普通售后、消费者/人工退款和退货验货；B12 按既有产品路线图交付消费者售后申请/查询/取消/退货物流、总部审核/验货/异常处置、普通退款/失败重试、库存回库、佣金冲正和金额补偿。
2. 既有 18 个售后/退款 operation 已预留，但资源 ID、乐观锁、限流、no-store、错误码和输入集合仍未达到 B8-B11 的冻结标准；订单详情还把 `aftersales` 固定为空，和 B12 开放售后直接冲突。CH-026 不新增 path 或 operation，只闭合现有契约及其订单投影依赖。
3. 既有 `enforce_refund_item_return_limit()` 在不可变验货项上使用锁定读取，而 `mall_runtime` 被最小权限撤销该表 UPDATE 权限；按调用者权限执行时会阻断真实退货退款写入。
4. `refund_attempt` 缺少“每个退款最多一个活动 attempt、最多一个成功 attempt”的数据库级并发兜底；退款、售后、订单与订单项的跨表 envelope 也需要补强，防止不同幂等键或交叉绑定产生重复资金/库存事实。

### 34.3 已批准契约和数据库决策

1. 保持既有 173 paths / 198 operations / 198 unique operationId；实测为 326 schemas / 706 schema refs / 2,726 local refs / 0 dangling refs，Redocly 0 warning。
2. 售后、订单、订单项、文件、退款等资源 ID 统一 ULID；Store 个性化响应使用 CUSTOMER+来源 IP 120/60 fail-closed 限流，并统一 `Cache-Control: no-store, private`、`Pragma: no-cache`。
3. Store 售后创建在同一 operation 内使用闭合 `PREVIEW -> CONFIRM`，两阶段携带相同业务字段并使用不同幂等键；PREVIEW 从现有幂等密钥环经独立 HKDF 域签发 5 分钟服务端试算凭证，CONFIRM 锁内重验后才占用。创建、取消、填写退货物流及 Admin 审核、拒绝、验货、异常处置、退款与重试写操作均使用 `HASH_ONLY` 幂等策略；已有资源 mutation 必须携带 `If-Match`，冲突返回 409 并要求刷新。
4. 消费者动作不得包含不存在的退款重试能力；退款失败重试只属于 Admin。审核通过只推进售后状态并冻结必要快照，实际退款必须另走 preview-confirm。
5. 增加并闭合 `AFTERSALE_QUOTA_EXCEEDED`、`RETURN_ADDRESS_NOT_CONFIGURED` 等售后业务错误；申请原因、逐项数量、证据文件、承运商和运单号均设置闭合枚举、数量、去重、长度与控制字符边界。申请和验货证据均最多 9 个不重复 ULID；`carrier_code` 固定为 1-40 位 ASCII 字母、数字及 `._-`。
6. Store 订单列表/详情的 `available_actions` 增加服务端权威 `APPLY_AFTERSALE`：必须为 PAID/NORMAL，完成前 `PENDING_SHIPMENT|SHIPPING` 允许期限为空，完成后 `COMPLETED` 必须未超过非空期限，`CLOSED` 永不允许，并至少一个订单项仍有数量和金额额度；活动售后只扣减逐项额度。订单详情 `aftersales` 不再固定为空，并可返回本人普通退款和金额补偿尝试的安全投影。客户端不得自行推导售后资格，消费者动作仍不得出现 Admin 专属 `RETRY_REFUND`。
7. 新增事务型前向迁移 `0005_b12_aftersale_refund_guards`。迁移必须先检查历史重复/交叉事实，修复退款项触发器的最小权限兼容，增加活动/成功退款 attempt 条件唯一索引，并强化 refund/order/aftersale/customer/order-item envelope；不得修改既有 `0001` 至 `0004`。
8. Provider refund 必须使用 `prepare -> transaction 外 Provider -> finalize`。UNKNOWN、失败、响应丢失、重复/乱序回调和 Worker 崩溃必须以稳定退款号和幂等事实收敛；B12 development 只允许 Mock Provider，真实微信退款 callback 不注册。
9. 退款成功事务必须原子更新退款双状态轴、售后占用、订单/订单项退款累计、适用库存回库/销量、佣金 position/ledger、钱包、审计和 Outbox；原支付、佣金快照与正向流水不可改写。

### 34.4 实施批次与准入

| 批次 | 交付范围 | 退出条件 |
|---|---|---|
| B12.0 | CH-026、产品/API/架构/数据库/风险/原型同步、OpenAPI/contracts、0005 与迁移链门禁 | 契约、Redocly、生成漂移、完整迁移回放、权限、冻结、Prisma validate 和 migration diff 通过；准确 SHA 的普通 CI、development migration、rollback-only smoke 三项证据补齐后暂停 |
| B12.1 | Store 售后申请、列表、详情和取消 | 服务端计价、额度并发占用、跨客户 404、证据权限、取消释放与幂等通过 |
| B12.2 | Admin 审核、拒绝 preview-confirm、退货地址快照与消费者退货物流 | 地址版本冻结、状态矩阵、If-Match、preview 篡改/过期/重放和 PII 通过 |
| B12.3 | PASS/ABNORMAL 验货、证据封存和一次性异常处置 | 精确覆盖、数量等式、不可变结论、回库边界和并发通过 |
| B12.4 | 稳定普通退款、失败重试、金额补偿、库存/销量、订单与佣金/钱包收敛 | Provider 故障、重复/乱序、双状态轴、唯一流水、full/partial refund、完成/退款并发、尾差和累计上限通过 |
| B12.5 | MP-13/14 与 ADM-12/13/16 工程前端 | generated contracts、严格解码、服务端动作权威、五视口、错误/慢请求/丢包与敏感持久化扫描通过 |
| B12.6 | DB/API/前端/真实纵向总验收 | 无残留；最终实现同 SHA 的普通 CI 与 rollback-only smoke 双绿，并确认 `0005` 已在 development 迁移历史中 |

B12.0 当时只授权治理、契约、迁移和门禁，不授权 repository、controller/service、Worker 售后处理器或工程前端业务代码。CH-025 已失效且不得沿用；随后 CH-027 已另行批准，B12.1 在其约束下完成并暂停。

### 34.5 回退与外部边界

- B12.1 开始前，CH-026 的文档、OpenAPI、generated contracts 与尚未部署的 0005 可整体回滚；0005 一旦部署到 development，则作为加法/约束型前向迁移保留，不执行数据库降级。
- B12 仅允许 Mock Provider 和脱敏 development。真实微信退款验签/证书/ACK、真实客户数据、真实资金、第三方物流、staging 和 production 均继续 `NO-GO`。
- 第一次进入 staging 前仍须由外部独立人员复核代码、安全、数据库、资金、库存、PII、佣金和验收证据；单人维护者自我确认不能替代该门禁。

## 三十五、CH-027 B12 单人 development reviewer 例外

### 35.1 变更申请与决策

- **申请日期**：2026-09-01。
- **变更来源**：团队资源约束。
- **变更类型**：修改交付门禁。
- **影响范围**：开发进度、测试范围。
- **影响程度**：中等。
- **审批级别**：产品负责人 + 技术负责人。
- **审批结果**：已批准。
- **执行时机**：立即执行。

当前项目只有一名维护者，无法在 B12.1-B12.6 脱敏 development 配置第二 reviewer。CH-027 只替代这一组织条件，不把项目所有者的自我确认伪装为独立复核，也不降低 CH-026 契约、数据库、权限、PII、资金、库存、佣金、并发、幂等、审计或远端证据标准。

### 35.2 B12.0 前置证据

B12.0 最终准入 SHA 为 `03a1e9f98bea0926887fb68354ca602566148f6a`。以下三项运行均为该精确 SHA，且均已 `completed/success`：

| 门禁 | 成功证据 |
|---|---|
| 普通 CI | [Run `33462227180`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33462227180) |
| Supabase development migration | [Run `33464158444`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33464158444) |
| Supabase development rollback-only smoke | [Run `33464267703`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33464267703) |

上述证据关闭 B12.0 的准确 SHA 远端门禁；结合本地契约、迁移、权限、冻结、Prisma、`migration diff=0` 和 `P0=0/P1=0` 结果，B12.0 已完成。CH-027 批准后，B12.1 的治理前置条件闭合并获得准入，但不等于 B12 development `GO`。

### 35.3 补偿控制与执行范围

1. B12.1-B12.6 必须严格串行，每批完成约定的领域、并发、故障、权限、隐私和无残留测试，并在 `P0=0/P1=0` 后暂停；未通过不得进入下一批。
2. 代码、契约、数据库和测试必须继续经过自动化检查；高风险 preview-confirm、退款、库存回库、佣金冲正和 Provider 故障路径不得以单人例外降级或跳过。
3. B12.6 必须在同一最终实现 SHA 上取得完整普通 CI 与 Supabase development rollback-only smoke 双绿，并确认 `0005` 保留在 development 迁移历史中；失败或不同 SHA 的 Run 不得拼接替代。
4. 所有数据库 fixture、售后/退款/库存/佣金/审计/幂等/Inbox/Outbox 事实、Redis key 和对象存储测试对象必须精确清理并保留无残留断言。
5. 审查记录必须如实标注为单人 development 补偿控制，不得虚构第二 reviewer、独立批准或外部复核。

### 35.4 失效与外部边界

- CH-027 仅适用于 B12.1-B12.6 的 Mock Provider、脱敏 development；不追溯替代 B12.0 已完成的技术门禁，也不扩张 B12 产品范围。
- CH-027 在 B12.6 完成并满足最终同 SHA 双绿、无残留和 `P0=0/P1=0` 后自动失效，不得沿用到后续 development 阶段。
- CH-027 不适用于 staging、production、真实客户数据、真实身份、真实微信支付/退款、真实资金或真实物流。
- 第一次进入 staging 前仍必须由外部独立人员复核代码、安全、数据库、PII、资金、库存、佣金和全部验收证据；项目所有者自我确认不能替代该门禁。

### 35.5 B12.1 退出记录

B12.1 已实现 Store 售后 PREVIEW/CONFIRM、本人列表/详情、允许阶段取消、逐项数量/金额占用、订单售后摘要与 `APPLY_AFTERSALE`。CUSTOMER 仅可使用本人 `READY/PRIVATE/AFTERSALE_EVIDENCE` 文件，上传意图、完成和下载均复用 CUSTOMER+来源 IP `120/60` fail-closed 限流；SUPER_ADMIN 保持原文件权限语义。

本批全仓测试为 `2455 passed / 148 environment or designed skips`；临时 PostgreSQL 18.3 完整回放 `0001-0005` 后 full `5/5`，覆盖额度竞争、同证据跨订单、确认与发货、取消与发货四类真实双连接竞态；Supabase development rollback-only 为 `1/1`。lint `0 errors`、typecheck、build、Redocly、生成漂移、敏感扫描、冻结数据库、Prisma validate 和 `migration diff=0` 均通过，且无 Prisma/迁移修改或测试残留。

只读复审发现的售后单号长度边界、CUSTOMER 文件限流和 `aftersale_item` 锁序问题均已闭合；同毫秒审计事件按售后版本保持 CREATE 到后续状态的因果顺序。最终结论为 `P0=0/P1=0`。B12.1 按批次规则暂停后，经再次明确准入进入 B12.2；该结论不构成 B12 development `GO`，也不扩大 CH-027 或 staging/production 权限。

### 35.6 B12.2 退出记录

B12.2 已实现 Admin 售后列表/详情、初审通过、初审拒绝 preview-confirm、总部退货地址版本发布，以及 Store 消费者退货物流提交。`RETURN_REFUND` 初审在同一串行化事务锁定唯一当前 PUBLISHED 地址，将版本密文解密后以独立快照 AAD 重新加密；地址更新保留不可变历史版本，HASH_ONLY 重放读取命令当时的准确版本，既有售后快照不回写。

本批全仓测试为 `2530 passed / 151 environment or designed skips`；database 为 `677/104`，API 为 `1231/44`，platform-core 为 `139/0`。临时 PostgreSQL 18.3 完整回放 `0001-0005` 后 full `8/8`，覆盖真实 Admin 拒绝与发货、退货物流提交与取消、独立管理员地址换版与审批竞争；Supabase development rollback-only 为 `1/1`，且地址、售后、物流、审计和幂等 fixture 无残留。lint `0 errors`、typecheck、test、build、MP-Weixin build、Redocly、确定性生成、敏感扫描、冻结数据库、Prisma validate 和 `migration diff=0` 均通过；当前契约为 `173/198/198/326/706/2,726/0`，没有修改 Prisma schema 或迁移。

只读复审发现并闭合了 Admin 详情申请证据投影、地址 preview/PATCH HASH_ONLY 声明、首次地址强 ETag、准确历史地址重放、完整当前验货投影、单字符可选审批备注、Admin 成功响应 no-store 契约和两类真实双连接竞态边界。地址/物流明文不进入审计或幂等正文，错误响应也保持 no-store。最终结论为 `P0=0/P1=0/P2=0`。B12.2 当批按规则暂停；其后 B12.3 已另批完成。该历史结论不构成 B12 development `GO`，也不扩大 CH-027 或 staging/production 权限。

### 35.7 B12.3 退出记录

B12.3 已实现 Admin 实收验货、`PASS | ABNORMAL` 判别与 canonical 证据封存、异常继续退款及退货后拒绝 preview-confirm。验货精确覆盖全部售后项，逐项满足冻结数量等式；`ABNORMAL` 必须存在真实数量异常、原因和证据。证据限定当前管理员创建、`READY/PRIVATE/AFTERSALE_EVIDENCE/private/{file_id}` 且全局未绑定，封存后由不可变与 manifest 触发器保护。继续退款仅保留批准额度，退货后拒绝释放全部剩余额度，两条路径都不创建退款、不调用 Provider、不写库存或佣金。

本批全仓测试为 `2567 passed / 158 environment or designed skips`；database 为 `692/111`，API 为 `1253/44`，platform-core 为 `139/0`。临时 PostgreSQL 18.3 完整回放 `0001-0005` 后 full `51/51`，扩展后的 Admin return 7 个真实 DB 用例覆盖 manifest 精确错误、PASS/ABNORMAL、零件/少件、多项精确覆盖、不可变、非法 purpose、deterministic reject、文件完成/验货和双 resolution 竞态；Supabase development rollback-only 为 `6/6`。隔离 PostgreSQL/Redis/MinIO 的文件 API 与清理 Worker 为 `6/6`，签名对象、no-store、审计和跨账户 404 均通过，所有临时资源和 fixture 无残留。

lint `0 errors`、typecheck、test、build、MP-Weixin build、Redocly、确定性生成、敏感扫描、冻结数据库、Prisma validate 和 `migration diff=0` 均通过；契约为 `173/198/198/326/706/2,726/0`，没有修改 Prisma schema 或迁移。复审发现并修复 SUPER_ADMIN 本人文件授权回归、无数量异常伪装 ABNORMAL、manifest 断言假绿及 Store 验货证据包络，最终为 `P0=0/P1=0`。保留一个 P2：跨所有者已绑定证据的真实下载正向纵向用例在 B12.6 补齐。B12.3 按批次规则暂停；其后 B12.4 已另批完成。该历史结论不构成 B12 development `GO`。

### 35.8 B12.4 退出记录

B12.4 已实现 Admin 普通退款和总部纯金额补偿 preview-confirm、稳定 refund 生命周期上的失败重试，以及 Mock Provider -> Inbox -> Worker 收敛链路。确认操作使用 `If-Match`、新 preview token 和 HASH_ONLY 幂等；同键重放重新鉴权并返回当前投影。Provider 网络调用保持数据库事务外，refund attempt 作为外部请求身份，确保同 attempt 精确重放、新 attempt 可重试且旧 attempt/旧执行事件不能推进当前 attempt。

退款成功事务原子更新 refund/attempt、售后与订单退款双轴、订单项净退款、适用的库存回库与净销量、全额退货后完成原因、佣金头寸/流水及 AVAILABLE 代理钱包。部分退货按最终批准数量和金额执行；纯金额补偿只占金额、不占数量且绝不回库。Worker 对普通退款执行和 callback 使用有界 durable retry，覆盖创建响应丢失、Provider UNKNOWN/失败、重复/乱序事件、业务提交后崩溃和超过通用重试上限后的继续收敛；单活动/单成功 attempt、库存流水、佣金冲正和钱包扣减均保持精确一次。

本批 B12 full DB 为 `6 files / 64 passed`，数据库 unit `8/8`、真实 integration `5/5`；refund API `16/16`、Admin order `19/19`、fulfillment `6/6`，focused 合计 `41/41`；Worker `93 passed / 3 designed skips`。全仓为 `2629 passed / 165 environment or designed skips`，lint `0 errors / 437 existing warnings`，typecheck、test、build、MP-Weixin build、Redocly、contracts 确定性生成且无漂移、敏感扫描、冻结数据库、Prisma validate 和 `migration diff=0` 均通过；契约保持 `173/198/198/326/706/2,726/0`。本批没有修改 OpenAPI、generated contracts、Prisma schema、既有迁移或静态前端。

复审新增并闭合两个 P1：`RETURN_DAMAGED` 使用独立 disposition event，不再误发 restocked；历史 attempt 的精确延迟回调可幂等吸收且冲突 fail-closed。非 Mock runtime 也已从 preview 起 fail-closed。连同历史失败 attempt 误开放重试、旧 attempt/旧 Outbox 事件驱动新 attempt、Provider 成功重放冲突、部分退货按原申请而非批准数量退款及完成幂等键在全额退款后失效等风险，最终结论为 `P0=0/P1=0/P2=0`。

原两个非阻断 P2 已在 B12.6 收口：文档现已准确记录公开 preview/confirm 的 Serializable 边界与 confirm 固定锁序；多行退货中一行批准 0 的真实数据库用例已补齐并在 admin-refund integration `6/6`、B12 full DB `65/65` 中通过，证明该行不生成 refund item、不回库、不改销量且不冲正佣金。B12.4 因此为 `P2=0`。B12.3 既有“跨所有者已绑定证据真实下载正向纵向”P2 已由此前纵向基线闭合；整体仅保留 B12.5 orphan `P2=1`。

### 35.9 B12.5 退出记录

B12.5 已实现消费者 MP-13/14 与总部 ADM-12/13/16 的售后工程界面，覆盖申请与详情、初审、总部退货地址、消费者退货物流、验货、异常解决、普通退款/失败重试和金额补偿。两端均复用 generated contracts 和严格响应解码器，所有金额、可用动作、状态与配额以服务端投影为准；Admin 验货证据上传严格绑定 intent/complete 的用途、文件 ID、状态、时间戳、MIME、SHA-256 及 PRIVATE `public_url=null`，对象存储拒绝时禁止 complete 和验货提交。移动导航、409 后配额 clamp 和精确金额求和问题已修复。登录态请求绑定账户/客户与会话代次；消费者确认和总部退款命令使用分账户、最长 24 小时、只保存加盐完整请求哈希的 durable journal，刷新、响应丢失和重新认证继续使用同一幂等键，且不持久化自由文本原因或原始身份。退款失败重试统一由订单详情恢复原命令，售后详情不形成第二执行入口。

本批 miniapp E2E 为 `21 passed / 29 designed skips`，admin E2E 为 `24 passed / 21 designed skips`，合计 `45 passed / 50 designed skips`，覆盖 375/390/414/1024/1440 五视口；全仓单测为 `2735 passed / 165 designed or environment skips`，Admin 文件服务为 `24/24`，B3/B4/B5 历史上传回归分别为 `26/4`、`27/8`、`30/20`。lint 为 `0 errors / 437 existing warnings`，全仓 typecheck/build、MP-Weixin build、敏感扫描、契约与冻结门禁均通过。批次复审结论为 `P0=0/P1=0/P2=1`；残余 P2 为沿用 B3 已登记策略的未绑定 READY/PRIVATE 证据回收缺口，首次 staging/真实数据前必须另行变更并闭合。OpenAPI 继续保持 `173 paths / 198 operations / 198 unique operationId / 326 schemas`，两份 Prisma schema 与既有 `0001` 至 `0005` 迁移继续冻结。

B12.5 按批次规则完成，历史全仓结果保持 `2735 passed / 165 skips`；B12.6 本地实现与本地验收也已完成。当前 B12 full DB `65/65`、两端 E2E `45 passed`、全仓单测 `2738 passed / 166 skips`、完整 `RETURN_REFUND` 真实纵向 `1/1` 及精确清理通过。最终实现 SHA `8c3589afcf7bb0dd5a4b8711d418e4c61b1ad09c` 的普通 CI Run `33592754575` 与 Supabase development rollback-only smoke Run `33594513127` 均为 `completed/success` 且 `head_sha` 一致；B12 development `GO`，CH-027 已自动失效。当前唯一残余为不阻断 development 的 B12.5 未绑定 `READY/PRIVATE` evidence orphan `P2=1`，首次 staging/真实数据前须闭合回收策略。

## 三十六、CH-028 B13 一级代理经营与资金闭环

### 36.1 变更申请与决策

| 项目 | 内容 |
|---|---|
| 申请日期 | 2026-09-02 |
| 变更来源 | 产品路线图未实现域与现有预埋契约之间的交付缺口 |
| 变更类型 | 新增一级代理经营、归属和资金闭环能力 |
| 影响范围 | 功能范围、开发进度、技术架构、测试范围、用户体验、数据迁移 |
| 影响程度 | 重大 |
| 审批级别 | 变更控制委员会（CCB） |
| 审批结果 | 已批准 |
| 执行时机 | 立即执行 |
| 目标基线 | `v2.4.11 / CH-028`、OpenAPI `2.4.11-ch028` |

CH-028 准入 B13.0 的文档、OpenAPI/generated contracts、配置、契约门禁和前向 `0006_b13_agent_finance_guards`。`0006` 只允许历史预检和数据库守卫，不修改 `0001` 至 `0005`，不删除或改写既有交易、佣金、钱包、审计和提现事实。B13 覆盖一级代理生命周期、独立 Agent JWT realm、商品授权、邀请码/推广、归属客户与历史订单投影、佣金规则/钱包/银行卡/提现及总部审核；不包含多级代理、独立店铺、代理专属佣金、自动打款、真实微信、真实资金、staging 或 production。

### 36.2 B13 串行批次

| 批次 | 范围 | 门禁 |
|---|---|---|
| B13.0 | B12 收口、B13 主记录、CH-028/029、契约/配置/0006 实现 | 契约、生成、迁移回放/权限/diff、审计与风险同步；同 SHA CI→migration→rollback-only smoke |
| B13.1 | 总部代理生命周期与 Agent 认证/账户安全 | 临时密码、refresh、停用会话、限流、枚举和 replay |
| B13.2 | 商品授权、邀请码、推广链接/二维码/素材 | 撤权、下架、失效安全退化，历史事实不变 |
| B13.3 | Agent 客户/订单只读、Admin 归属转移/转直营 | 当前/历史快照隔离、跨代理 404、转移/下单竞态 |
| B13.4 | 佣金规则版本、Agent 看板/佣金/钱包和 Admin 账本下钻 | `null` 继承与 `0` 分离、规则/支付并发、流水精确一次 |
| B13.5 | 银行卡、最低提现设置、提现申请 | 加密/掩码、快照、冻结、单进行中申请、幂等 |
| B13.6 | 提现审核、TOTP reauth、单次卡号查看、凭证、PAID | 拒绝解冻、PAID 扣减、凭证必需、TOTP 失败锁定 |
| B13.7 | Agent Web AGT-01 至 AGT-09 | generated contracts、严格解码、五视口与完整错误状态 |
| B13.8 | Admin Web ADM-14/15/17/18-22、ADM-16 | 高风险 preview-confirm、敏感值清除、下钻与五视口 |
| B13.9 | 真实基础设施纵向、全仓回归、最终复审 | `P0=0/P1=0`、零残留、同 SHA 双绿，随后 B13 development `GO` |

每批完成后必须暂停，未经当前批 `P0=0/P1=0` 和下一批治理准入不得继续；失败或不同 SHA 的远端证据不得拼接。

### 36.3 B13.0 本地候选记录

B13.0 已形成绿色本地候选：CH-028 契约保持 `173 paths / 198 operations / 198 unique operationId`，Agent 子集为 `21 paths / 23 operations`，完整 operation manifest SHA-256 为 `128509f76f2e62ebe78cd0465205a94236f87f70904bf614664dd1b02548dc80`；generated contracts 两次生成稳定。`0006` 正式/冻结副本 SHA-256 为 `355311f6a5091f03bcb879f927ca78c984ec2cb26efb7f14bb4133161ccc2ea0`，完整回放、历史异常原子失败、权限、B10-B12 回归、原生定义指纹、`migration diff=0`、76 表零残留和连接清理均通过；全仓 lint/typecheck/test/build、MP-Weixin build、配置合同、Prisma validate、敏感扫描和 workflow YAML 解析也已通过。

B13.0 远端退出证据已闭合：最终 SHA `250f4c8fabb08e170c012889c392461794ed8875` 的普通 CI Run `33615918343`、development migration Run `33618959837` 与 rollback-only smoke Run `33619152221` 均为 `completed/success`，且按 CI -> migration -> smoke 串行完成。migration attestation `supabase-migration-250f4c8fabb08e170c012889c392461794ed8875-gphyhqryivtpicuqpdkq` 与批准的 development project ref 匹配。B13.0 已退出，B13.1 正式准入；该结论不构成 B13 development `GO`，也不开放 staging、production 或真实资金。

### 36.4 B13.1 最终退出记录

截至 2026-09-03，B13.1 已完成总部代理列表/详情/创建/编辑/停用 preview-confirm/重启/密码重置 preview-confirm，以及 Agent 独立 JWT realm 下的登录、refresh、登出、临时改密、普通改密、全端登出和当前账号 7 个接口。临时会话固定 `CHANGE_PASSWORD_ONLY` 且无 refresh；一次性秘密只在首次响应披露，幂等重放脱敏，10 分钟交接窗口从成功事务完成后起算。preview 动态数量仅为时点估算，confirm 绑定 actor/session/action/target/request/version 并在共享 Agent 锁内重验。

契约实测为 `173 paths / 198 operations / 198 unique operationId / 327 schemas / 716 schema refs / 2,746 local refs / 0 dangling refs`，generated contracts SHA-256 为 `2207286938dd174c0d6b36879407131253ef1ad4ffe6dc3e5726a83827852448`，operation manifest SHA-256 保持 `128509f76f2e62ebe78cd0465205a94236f87f70904bf614664dd1b02548dc80`。全仓 `2,878 passed / 168 skipped`，typecheck、lint、完整 build、MP-Weixin、Redocly、CH-028、冻结与敏感扫描通过；全新 PostgreSQL 18.3 + Redis 7.4.10 runner 覆盖认证、会话撤销、停用/支付双序竞态、迁移回放、`diff=0` 与精确零残留并通过。本地复审为 `P0=0/P1=0`。

最终 SHA `a565a3c406ad2f1af498f9f58b556315d30262d6` 的普通 CI Run `33704252016`、development migration Run `33706364405` 与 rollback-only smoke Run `33706498758` 均为 `completed/success` 且按 CI -> migration -> smoke 严格串行；migration artifact `supabase-migration-a565a3c406ad2f1af498f9f58b556315d30262d6-gphyhqryivtpicuqpdkq` 与 development project ref 匹配。B13.1 已退出，B13.2 正式准入；B13 development 未标记 `GO`，staging、production 与真实资金继续关闭。

## 三十七、CH-029 B13 单维护者 development 例外

### 37.1 变更申请与决策

| 项目 | 内容 |
|---|---|
| 申请日期 | 2026-09-02 |
| 变更来源 | 团队资源约束 |
| 变更类型 | 修改 development 交付门禁的 reviewer 组织条件 |
| 影响范围 | 开发进度、测试范围 |
| 影响程度 | 中等 |
| 审批级别 | 产品负责人 + 技术负责人 |
| 审批结果 | 已批准 |
| 执行时机 | 立即执行 |
| 适用范围 | B13.1-B13.9、Mock Provider、脱敏 development |

CH-029 仅补偿单维护者无法配置第二 reviewer 的组织条件，不虚构独立复核。2026-09-05 用户明确取消普通 CI，B13.9 远端证据调整为同 SHA development migration attestation 后的 rollback-only smoke。最终 SHA `250cbb824ad11f4b0e11338c19494655ec384ff6` 的 development migration Run `33952031401` 与 rollback-only smoke Run `33952283771` 已按顺序同 SHA 成功；B13 development `GO`，CH-029 已自动失效，不得沿用到 B14、staging 或 production。首次 staging 仍须外部独立人员复核。

## 三十八、CH-030 B14 ADM-02 经营分析契约最小修正

### 38.1 变更申请与决策

| 项目 | 内容 |
|---|---|
| 申请日期 | 2026-09-05 |
| 变更来源 | ADM-02 既有经营分析契约与工程实现之间的最小闭合需要 |
| 变更类型 | 修改需求与接口契约 |
| 影响范围 | 产品/API 基线、OpenAPI/generated contracts、Admin Web 与对应 API 读模型 |
| 影响程度 | 重大 |
| 审批级别 | 变更控制委员会（CCB） |
| 审批结果 | 已批准 |
| 执行时机 | 立即执行 |
| 目标基线 | `v2.4.12`、OpenAPI `2.4.12-ch030` |

### 38.2 最小范围与边界

CH-030 仅准入 B14 ADM-02 经营分析契约的最小修正：保留既有 `/admin/dashboard`、日/月销售报表、商品销量排行和客户消费排行路径及 operationId，修正服务端权威口径、查询参数、响应 Schema 与 generated contracts，使 B14.1-B14.3 可以按现有交易、退款、客户和代理事实实现并展示。不得借本变更新增其他页面、营销能力、Excel 导出、自定义报表或经营写操作。

本变更不新增或修改 Prisma schema、数据库迁移、Worker handler、定时聚合任务或图表依赖；Admin Web 使用既有组件完成指标、表格和状态展示。用户已取消 GitHub 普通 CI，本变更不新建或恢复 GitHub CI workflow。B12 orphan `P2=1` 与 B13.8 已登记 P2 保持不变并继续阻断 staging/真实数据；CH-030 不授权 staging、production、真实客户数据、真实微信、真实支付、真实退款、真实物流或真实资金。

### 38.3 当前状态

B13 最终 SHA `250cbb824ad11f4b0e11338c19494655ec384ff6` 的 development migration Run `33952031401` 与 rollback-only smoke Run `33952283771` 已按顺序同 SHA 成功，B13 development `GO`，CH-029 已自动失效。CH-030 已批准并立即执行，当前产品/API 基线升级为 `v2.4.12 / 2.4.12-ch030`，B14.0 已准入。

## 三十九、CH-031 B14 单维护者 development 例外

### 39.1 变更申请与决策

| 项目 | 内容 |
|---|---|
| 申请日期 | 2026-09-05 |
| 变更来源 | 团队资源约束 |
| 变更类型 | 修改 development 交付门禁的 reviewer 与验证条件 |
| 影响范围 | B14.1-B14.3 开发进度与验证范围 |
| 影响程度 | 中等 |
| 审批级别 | 产品负责人 + 技术负责人 |
| 审批结果 | 已批准 |
| 执行时机 | 立即执行 |
| 适用范围 | B14.1-B14.3、单维护者、脱敏 development |

### 39.2 补偿控制

CH-031 仅补偿 B14.1-B14.3 无法配置第二 reviewer 的组织条件，不虚构独立复核。B14.1 只新增 2 个定向基础测试文件：一个覆盖权威事实查询仓储，另一个覆盖 5 个 Admin Analytics API 的范围、参数和严格响应；只构建受影响的 database/API 模块。B14.2 不新增前端测试文件，只执行一次 Admin Web build 与最小手工页面核对。每批完成后必须暂停并汇报，未经当前批结果确认不得进入下一批；不得扩展为全仓测试或重复运行已通过且不能提供新信息的测试。

用户已取消 GitHub 普通 CI，B14 不建立或恢复 GitHub CI workflow。B14.3 最终候选仅以同一精确 SHA 的 development migration attestation 与随后 Supabase rollback-only smoke 作为远端退出证据；两者成功后 CH-031 自动失效，不得沿用到后续阶段。

### 39.3 不适用边界与失效条件

CH-031 不适用于 staging、production、真实客户数据、真实微信、真实支付、真实退款、真实物流或真实资金，也不降低首次 staging 的外部独立复核要求。B12 orphan `P2=1` 与 B13.8 已登记 P2 继续阻断 staging/真实数据。任一批出现权限越界、指标口径不一致、敏感信息暴露或受影响构建失败时，必须暂停并修复；只有 B14.1-B14.3 完成且最终同 SHA migration attestation、rollback-only smoke 依次成功后，例外才自动失效。
