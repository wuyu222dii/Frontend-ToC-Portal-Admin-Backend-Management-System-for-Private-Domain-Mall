# 洗化产品私域商城风险管控方案

## 1. 管理基线

| 项目 | 口径 |
|---|---|
| 文档版本 | v2.0 / CH-022 |
| 更新日期 | 2026-08-29 |
| 适用阶段 | 设计冻结、研发准入、迭代开发与生产上线准备 |
| 风险范围 | 技术、质量、需求、进度、资源、外部、合规与运营 |
| 评估方法 | 概率（1-3）× 影响（1-3） |
| 应对策略 | 按风险采用规避、缓解、转移或接受 |
| 复核频率 | 每个研发迭代评审；高风险触发信号出现时立即复核 |

本方案区分“研发准入”和“生产上线”两个门禁。研发准入只允许在 Mock Provider、开发或测试数据库和脱敏数据条件下实施；真实微信支付、生产 Supabase、银行卡付款和佣金税务不因研发准入自动获批。

## 2. 评级规则

- 概率：低 1（小于 20%）、中 2（20%-50%）、高 3（大于 50%）。
- 影响：低 1（局部且可快速恢复）、中 2（影响迭代或部分业务）、高 3（资金、库存、隐私、合规或主链路失真）。
- 分数：1-2 为低，3-4 为中，6-9 为高。
- 高风险未落实预防控制和验证证据时，不得进入对应开发或生产阶段。

## 3. 风险登记表

| ID | 风险与触发信号 | 概率 | 影响 | 分数 | 应对与责任边界 | 验证或监控证据 |
|---|---|---:|---:|---:|---|---|
| TR-001 | 支付服务已创建交易但本地未保存 Provider ID，出现无法关单或库存误释放 | 2 | 3 | 6 | 缓解：以稳定 `intent_no` 查询/关闭，采用可恢复支付意图状态、对账任务和迟到支付退款；后端负责人 | 支付崩溃点故障注入、关单重试、对账集成测试 |
| TR-002 | 支付、订单、商品生命周期、库存与退款锁顺序不一致，尤其 reservation 先于 SKU/balance 或 B10 先插 payment intent 后锁订单，导致死锁或状态分叉 | 2 | 3 | 6 | 规避：候选 ID 只允许无锁定位；首次下单按 customer/cart/address/catalog→SKU/balance→insert order/reservation，取消/Worker 按 order→payment intent→SKU→balance→reservation，Product/SKU lifecycle 和库存调整共享 SKU→balance→reservation 尾部；B10 创建意图必须先锁定并重验订单后再插入；事务内不调用外部 Provider；后端负责人 | 下单/取消/超时分别与 lifecycle confirm、库存调整并发，B10 意图创建与关单并发，PostgreSQL `40P01` 和死锁日志为零，静态锁序审查 |
| TR-003 | 售后数量和金额由客户端分别提交导致超退、少发或佣金冲正错误 | 2 | 3 | 6 | 规避：普通售后仅接收数量，服务端按订单项快照计价；金额补偿使用独立实体；交易域负责人 | 数量、尾差、重复申请和并发占用测试 |
| TR-004 | OpenAPI、文本接口和数据库状态枚举漂移 | 2 | 3 | 6 | 缓解：OpenAPI 作为可生成契约，CI 校验路径覆盖、引用、operationId、枚举和 Schema；架构负责人 | 契约覆盖脚本、生成客户端编译、契约测试 |
| TR-005 | Prisma Schema 可解析但 PostgreSQL 专属约束未进入迁移 | 2 | 3 | 6 | 规避：固定 Node/Prisma 版本，生成迁移后人工补审条件唯一、CHECK、角色、授权和 RLS，并在空库回放；数据库负责人 | `format`、`validate`、`migrate diff`、空库迁移回放 |
| TR-006 | 单包裹模型与售后占用后的部分发货冲突 | 2 | 3 | 6 | 规避：MVP 任一有效售后占用阻断整单发货，释放或退款完成后重新评估；订单域负责人 | 发货门禁状态矩阵与并发验收 |
| TR-007 | 退款部分成功、部分失败被单一状态覆盖 | 2 | 3 | 6 | 规避：退款进度与处理状态分轴，退款尝试和回调不可变留痕；支付域负责人 | 多笔退款组合、失败重试和回调乱序测试 |
| TR-008 | 文件仅信任客户端 MIME/大小，或 complete 重试产生多个最终对象 | 2 | 3 | 6 | 规避：intent 预期 SHA-256，服务端复核 MIME/魔数/大小/hash；owner lease、幂等 copy 和闭合 `FILE_UPLOAD_COMPLETE`；文件域负责人 | 伪 MIME、截断/替换、并发 complete、copy/DB 故障注入 |
| TR-009 | 品牌/分类直接 ACTIVE、直接软删或恢复即启用绕过商品依赖检查 | 2 | 3 | 6 | 规避：创建/恢复固定 DRAFT，专用状态机，三动作 preview-confirm，确认事务重查 ACTIVE 商品；商品域负责人 | 完整状态矩阵、依赖变化、版本冲突与 token 重放测试 |
| TR-010 | Product/SKU 无 ACTIVATE、创建即启用或级联状态变化造成目录不可恢复/绕过发布校验 | 2 | 3 | 6 | 规避：CH-010 专用三动作 DTO；Product/SKU 固定 DRAFT/INACTIVE 创建，恢复分别回 DRAFT/INACTIVE且不级联；商品域负责人 | 状态矩阵、创建契约、重新启用、父子状态组合测试 |
| TR-011 | 商品发布缺图/活动 SKU，或软删时仍有 ACTIVE SKU/活动预占 | 2 | 3 | 6 | 规避：preview 显示影响，confirm 按统一锁序重查并用四个闭合 422 阻断；商品域负责人 | 依赖变化、并发确认、预占竞争和失败不消费 preview 测试 |
| TR-012 | Banner 普通写接口绕过归档状态机，或库存调整因低库存口径/preview 绑定不完整造成状态与流水失真 | 2 | 3 | 6 | 规避：CH-012 已闭合 DTO、库存公式与高风险 preview；B5.1 Banner 与 B5.2 Inventory 的状态/边界/幂等/权限、一次性 PostgreSQL 和 rollback-only 门禁已通过；商品与库存负责人 | Banner 状态/target/time matrix、库存边界/并发/篡改/过期/重放、契约与生成漂移 |
| TR-013 | 公开目录隐藏零库存 ACTIVE SKU、泄露管理字段、失真分页/排序，Redis 异常绕过匿名限流，或盲目信任代理头导致来源伪造 | 2 | 3 | 6 | 规避：CH-014 闭合 salable 投影、稳定排序、首页分区降级和 fail-closed 公共限流；`API_TRUSTED_PROXY_CIDRS` 默认空并仅显式信任直接代理数字 CIDR，IPv4-mapped 地址归一化；Store 目录负责人 | 公开/隐藏状态矩阵、快照一致性、字段泄露扫描、分页稳定性、Redis 故障、429/Retry-After、伪造 X-Forwarded-For 与 CIDR 配置测试 |
| TR-014 | 客户端伪造法律版本/Provider/推广目标，Store/Admin token 跨 audience 接受，或多 AppID/union_id 被误作当前唯一登录键 | 2 | 3 | 6 | 规避：服务端三份权威文档、Provider 环境选择、目标解析、role/assurance/audience 联合守卫，以及固定单 AppID + openid 身份规则；身份域负责人 | consent 缺失/重复/过期、跨端 token、production 配置失败、目标篡改、union_id 合并拒绝和多 AppID 迁移门禁测试 |
| TR-015 | 游客购物车合并因丢包重复累加、未知 SKU 部分写入，或服务端购物车投影使用陈旧客户端价格/库存 | 2 | 3 | 6 | 规避：固定 merge journal/幂等键、Serializable 全成全败事务、升序 SKU 锁序、服务端实时投影；购物域负责人 | 网络丢包重放、未知/失效 SKU、并发懒创建、99/100 上限与合计测试 |
| TR-016 | 报价凭证可被篡改、跨客户/会话重放，或价格、地址、购物车和库存变化后仍能提交 | 2 | 3 | 6 | 规避：5 分钟用途隔离 HMAC 绑定 CUSTOMER、session、规范请求与全部版本/价格/库存事实，下单事务再检查；订单域负责人 | 篡改、过期、跨主体/会话、密钥轮换、各类事实漂移测试 |
| TR-017 | 下单、主动取消、超时 Worker、Product/SKU 生命周期与库存调整并发造成死锁、超售、重复预占/释放或 `locked_qty` 失真 | 2 | 3 | 6 | 规避：Serializable、统一 order/payment/SKU/balance/reservation 锁序、流水事实唯一索引、CAS 不变式与 SKIP LOCKED；订单/商品/库存域负责人 | 最后库存、取消/超时/生命周期/库存调整竞争、多 Worker、重放、无 `40P01` 和 `physical_qty >= locked_qty >= 0` 验收 |
| TR-018 | 0002 索引迁移遇到历史重复事实、错误 Supabase 项目/SHA 或与 rollback-only smoke 并发执行 | 2 | 3 | 6 | 规避：迁移内历史重复预检、main-only 受保护 workflow、项目/SHA/TLS/历史验证与共享 DB concurrency group；数据库负责人 | 空库/0001 升级回放、重复 fixture 失败、迁移历史、权限和 diff=0 |
| TR-019 | 到期订单的 order item、ACTIVE reservation/item 或 inventory balance 事实损坏，候选查询将其 fail-closed 排除却无专用告警，可造成库存长期锁定 | 1 | 3 | 3 | **B9.5 已闭环**：新增独立只读完整性扫描与闭合、无 PII 的结构化告警；继续禁止自动修复损坏事实，异常候选保持 fail-closed、不释放库存；订单/运维负责人 | 真实数据库回滚 fixture 证明损坏预占不释放、扫描发出仅含 order ID/问题类型的告警且事务外无残留 |
| TR-020 | 完整性扫描只按问题数量 `LIMIT`，大量正常到期订单存在时扫描工作量仍未有界 | 1 | 2 | 2 | 接受为 B9.5 唯一非阻断 P2；后续改为候选页游标加页内问题列表，使每轮候选读取与问题输出分别有界；订单/运维负责人 | 大量正常候选与少量损坏候选混合 fixture、游标推进、页内上限和无漏报回归 |
| TR-021 | Provider create/query/close/refund 返回超时或 UNKNOWN，订单被错误关单、重复支付或库存提前释放 | 2 | 3 | 6 | 规避：Provider 调用全部在事务外；UNKNOWN 保持 intent/退款待处理与库存锁定，由稳定 intent_no/refund_no 对账；支付负责人 | 创建响应丢失、关单 UNKNOWN、退款超时及进程崩溃故障注入 |
| TR-022 | Mock Provider 的 Redis 状态不可用或键域混用，导致绕过 fail-closed 或泄露原始业务标识 | 2 | 3 | 6 | 规避：域隔离 HMAC key、无原始客户/订单/IP；Redis 异常统一 503，不降级为成功或本地猜测；平台负责人 | Redis 断连/延迟/键冲突测试与键内容扫描 |
| TR-023 | 回调重复、乱序、延迟或 Worker 提交后崩溃，产生两个成功 attempt、重复扣库存或重复佣金 | 2 | 3 | 6 | 规避：Inbox 幂等、0003 intent 成功条件唯一索引、唯一库存流水与事务性结算；支付/库存负责人 | 重复/乱序事件、Inbox 完成前崩溃、支付/取消/超时并发测试 |
| TR-024 | 已关闭订单迟到收款后自动退款失败或重复，造成资金悬挂或重复退款 | 2 | 3 | 6 | 规避：每订单唯一 LATE_PAYMENT 退款、稳定退款事实和可重试 Provider 调用；失败/UNKNOWN 转 MANUAL_REQUIRED 对账；支付/财务负责人 | 退款失败、超时、重试、乱序事件及唯一索引测试 |
| SE-001 | 管理员高风险操作缺少强认证或确认令牌可被重放 | 2 | 3 | 6 | 缓解：TOTP、恢复码、短期 challenge、绑定会话/动作/目标/请求哈希的预览令牌、乐观锁与幂等键；安全负责人 | 重放、越权、过期、并发和会话撤销安全测试 |
| SE-002 | 完整银行卡号泄露到日志、接口缓存、前端源码或截图 | 2 | 3 | 6 | 规避：仅 APPROVED 提现可二次验证后 60 秒单次查看；响应 `no-store`，明文离页清除，全链路审计；安全负责人 | 静态扫描、网络响应检查、日志检查、5 次失败锁定测试 |
| SE-003 | 三端绕过 NestJS 直连 Supabase 导致权限失控 | 1 | 3 | 3 | 规避：关闭 Data API，应用表不授予前端角色，runtime/migration 分权，RLS 默认拒绝；平台负责人 | 权限脚本审计、匿名连接拒绝测试 |
| SE-004 | 对象存储公开策略或签名/preview 缓存泄露售后、提现或推广 QR | 2 | 3 | 6 | 规避：bucket 默认私有，仅 `public/*` 匿名 GET；私有前缀、5 分钟下载签名和 no-store；intent/preview 使用 HASH_ONLY，下载 GET 不建幂等记录；安全负责人 | 匿名访问矩阵、缓存/日志/构建产物敏感扫描 |
| SE-005 | 候选 token、登录 code、手机号凭证或注销能力进入日志/缓存或被跨会话重放 | 2 | 3 | 6 | 规避：write-only、no-store、HASH_ONLY、候选 token 使用与幂等/IP 不同 domain 的用途隔离 HMAC，候选 30 分钟/注销 5 分钟并绑定主体会话版本；安全负责人 | 日志/追踪扫描、domain separation、篡改/过期/重放/跨会话测试 |
| SE-006 | 地址 PII 或用户偏好进入日志、审计、缓存/幂等正文，密文被跨字段替换，或登录后限流因 Redis 故障被绕过 | 2 | 3 | 6 | 规避：AES-256-GCM + 地址/字段 AAD、独立手机号 HMAC 域、全响应 no-store、HASH_ONLY、最小审计和 CUSTOMER+IP HMAC fail-closed 限流；安全负责人 | 密文篡改/轮换、PII 扫描、跨客户 404、Redis 故障与 429 测试 |
| SE-007 | 报价 token/hash 或订单地址明文进入日志、审计、Outbox、幂等响应，或进入受控下单 journal 之外的本地持久化 | 2 | 3 | 6 | 规避：报价凭证默认无状态；唯一例外是用户明确确认后，H5 `sessionStorage`/小程序沙箱保存 CUSTOMER 绑定的精确提交请求与幂等键，最长 24 小时；禁止进入 URL、日志、埋点、通用缓存或响应持久化。订单地址按独立 AAD 重新加密，服务端幂等 HASH_ONLY，全响应 no-store/private；安全负责人 | 敏感扫描、AAD 替换、日志/审计/Outbox/幂等抽检、journal 精确字段/TTL/跨客户零请求/退出清理和丢包同键重放检查 |
| SE-008 | 支付 capability、签名密钥或 Provider payload 进入日志、审计、Outbox、幂等正文或前端持久化 | 2 | 3 | 6 | 规避：capability 仅当前内存，HASH_ONLY/no-store，服务端字段白名单和支付域敏感扫描；安全负责人 | 浏览器存储、日志/审计/Outbox/Redis/幂等抽检及构建产物扫描 |
| SE-009 | development 未配置真实微信时仍注册回调，或 production 意外启用 Mock 结果入口 | 1 | 3 | 3 | 规避：路由按 Provider 配置条件注册；production 配置 Mock 必须启动失败，真实微信路由需独立 staging 凭据/验签门禁；安全负责人 | 环境矩阵启动测试、路由枚举、未配置回调 404 与 production fail-start |
| QA-001 | 原型静态数据与当前选择记录不一致，掩盖真实交互缺口 | 2 | 2 | 4 | 缓解：原型所有详情、编辑、筛选和高风险动作绑定当前记录，并扩展点击路径验证；前端负责人 | 三端验证脚本与桌面/移动截图 |
| QA-002 | 幂等键、回调去重或版本冲突未覆盖造成重复扣减 | 2 | 3 | 6 | 缓解：关键写入统一幂等记录、回调收件箱、Outbox 与 `If-Match`；质量负责人 | 重复提交、回调重放、409 冲突集成测试 |
| QA-003 | 空 SKU 商品的最低价被伪装为 0，或归档 SKU 被详情过滤导致误操作 | 2 | 2 | 4 | 规避：最低活动价 nullable；Product 详情固定返回全部 SKU 并稳定排序；质量负责人 | 无 SKU/无 ACTIVE SKU、归档筛选和分页稳定性测试 |
| QA-004 | 收藏商品失效后被隐藏、空购物车 GET 产生写入，或地址默认项并发造成 0/多默认 | 2 | 3 | 6 | 规避：独立失效收藏投影、空 GET 零副作用、条件唯一兜底、稳定默认提升与 If-Match；质量负责人 | 状态矩阵、数据库写计数、默认地址并发/删除、跨主体测试 |
| RQ-001 | 研发中重新引入多级代理、代理独立定价或复杂营销造成范围蔓延 | 2 | 2 | 4 | 规避：以 v2.4.8/CH-022 为唯一基线，新增范围必须新建变更单并重估数据与结算影响；产品负责人 | 每迭代范围审查、变更记录 |
| RQ-002 | “管理员”角色被误解为总部运营账号，破坏当前三角色模型 | 1 | 2 | 2 | 缓解：代码、接口和 UI 统一使用 `SUPER_ADMIN`、`AGENT_ADMIN`、`CUSTOMER`；产品与架构负责人 | RBAC 矩阵和角色常量扫描 |
| SC-001 | 微信、Supabase、对象存储或物流凭据延迟，阻塞真实联调 | 2 | 2 | 4 | 缓解：研发先使用契约 Mock；生产联调里程碑单独设置外部依赖截止日；项目负责人 | 依赖清单、凭据到位率、联调燃尽图 |
| RS-001 | 团队缺少支付、财务或 PostgreSQL 迁移经验 | 2 | 3 | 6 | 缓解：先做 Spike、故障演练和关键 SQL 自审；进入 staging 前由外部人员独立复核资金与迁移模块；技术负责人 | 自审记录、外部复核、演练报告、迁移回滚验证 |
| RS-002 | 单人维护导致 development 阶段无法形成独立 reviewer，缺陷发现缺少组织冗余 | 3 | 3 | 9 | CH-017/019 已失效；CH-021 只允许 B10.0-B10.6 脱敏 development，以自动化、同 SHA 远端证据和无残留断言补偿，B10.6 后自动失效；进入 staging 前强制外部独立复核；项目所有者 | Run/SHA、测试结果、无残留断言、外部复核报告 |
| EX-001 | 微信支付/退款或平台规则变化 | 1 | 3 | 3 | 缓解：Provider Adapter 隔离，联调前按官方文档复核签名、回调和错误码；支付负责人 | Provider 合同测试与版本变更订阅 |
| CO-001 | 手机号、地址、银行卡和账号删除处理不满足隐私或数据保留要求 | 2 | 3 | 6 | 转移与缓解：上线前由法律/隐私顾问确认协议、保留期、跨境与删除规则；内部实施数据最小化和去标识化；业务负责人 | 合规签字、数据地图、删除演练、访问审计 |
| CO-003 | 同步注销确认时出现新业务阻断或事务中断，造成会话未撤销/资料只删除一部分 | 2 | 3 | 6 | 规避：不合格 preview 不签发能力，confirm 重检，单事务去标识化与全部会话撤销，HASH_ONLY 不重放完成响应；隐私负责人 | 新阻断并发、崩溃点故障注入、事务外一致性和旧 token 拒绝测试 |
| CO-002 | 代理佣金、提现、税务或合同口径不合法/不可执行 | 2 | 3 | 6 | 转移：上线前由财税和法律专业方确认合同、开票、代扣与付款流程；业务负责人 | 合同模板、财税意见、付款 SOP 审批 |
| OP-001 | 退款失败、负余额或提现异常没有人工处置能力 | 2 | 3 | 6 | 缓解：建立财务异常队列、重试/关闭权限、证据附件和审计 SOP；运营负责人 | 异常处理时效、积压量、演练记录 |
| OP-002 | 商品、品牌、分类、SKU 软删除和迁移规则执行不当导致历史断链 | 2 | 2 | 4 | 缓解：在售依赖阻断、历史快照不可变、下架/迁移预览与审计；商品负责人 | 删除/停用矩阵测试、历史订单回归 |

## 4. 阶段门禁

### 4.1 B3 历史研发准入

CH-005 的历史研发准入证据保留，CH-006 曾重新打开 B3 契约门禁；以下 B3 条件均已通过。CH-007 调整整个 B3 的远端证据时序，CH-009 仅对单人维护项目的 development 独立 reviewer 作例外，未取消 B2/B3 云端证据或上线标准：

1. v2.4.1/CH-006 的产品规则、ADM-05/06 原型、API/OpenAPI 和生成 contracts 一致；页面/FR/AC/operation 数不变。
2. `pnpm contracts:check` 校验 172 paths、196 operations、196 unique operationId、306 schemas、0 dangling refs；Redocly 和生成漂移通过。
3. CH-006/B3 历史契约中 Product/SKU 的闭合 DTO 拒绝 ACTIVATE；该结论已由 CH-010 对 B4 取代。品牌/分类排序、DRAFT 创建、三动作状态机、ARCHIVED 查询和 restore-to-DRAFT 的既有证据继续有效。
4. Prisma Schema 和 `0001_initial` 逐字节不变，Prisma validate、冻结检查和 migration diff 为零。
5. CH-006 原型回归覆盖排序、固定草稿、启用预览、归档筛选、恢复草稿、409/422、重复提交和文件校验；所有 P1 阻塞项关闭。
6. B3.0 至 B3.3 已通过各自本地/一次性环境验收并暂停；B3.3 专用 E2E 为 `26 passed / 4 designed skips`，安全复核 `P0=0 / P1=0`，本地结论 `GO`。
7. B3.3 本地验收后必须另行执行覆盖 B3 repository 的 Supabase development rollback-only smoke；它与 B2 前置证据相互独立。Run `32678252828` 已在实现基准 SHA 上完成 B2、B3 文件与 B3 主数据 rollback-only，并由各 spec 的事务外查询确认 synthetic 标识无残留。
8. CH-009 只允许 B3 development `GO` 和后续 B4 开发；第一次进入 staging 前必须取得外部独立代码、安全、数据库权限/迁移及验收证据复核，不能由项目发起人自我确认替代。

研发准入不包含真实资金、生产数据或真实银行卡操作。研发环境必须使用 Mock Provider、测试账号和脱敏数据。

下列为 CH-005 历史准入证据；这些证据本身不替代上方已独立完成的 CH-006 门禁：

- 三端原型：78 个响应式渲染、21/9/22 页面契约、14 条小程序流程和 16 条后台/代理流程全部通过，敏感明文、破图、横向溢出和 `pageerror` 为 0。
- API：OpenAPI 172 paths / 196 operations / 305 schemas，与文本接口 196/196 覆盖；Redocly recommended lint 零警告。
- 数据库：Prisma 7.9.1 `format/validate/generate` 通过；PostgreSQL 18.3 空库回放得到 76 表、59 枚举、17 条件唯一索引、165 个 `CHECK`、76 张 RLS 表、76 个 policy 和 24 个用户触发器（45 个事件绑定）。
- 迁移权限：`mall_migrator` 拥有 76 张应用表、59 个枚举和 15 个迁移函数，可执行后续 `ALTER TABLE`；`mall_runtime` 拥有 0 张表，仅可删除收藏、购物车项、客户手机号授权和客户地址四类非交易数据，其他业务表无 `DELETE`。
- 边界 DML：全部通过，覆盖固定 30 分钟支付窗口、验货人/时间/原因完整性、证据封存与追加拒绝、退货验货精确覆盖/数量等式/单次处置、0% 佣金和最小权限。
- Prisma 对最终落库结构执行 `migrate diff` 返回 `No difference detected`；独立复审未发现 P0/P1 准入阻断。

### 4.2 B4 研发准入

CH-010 的 B4.0 至 B4.4 已按顺序完成；下列 P0 门禁均已关闭，并作为 B4 development 证据继续保持：

1. 普通 CI 中 MinIO root 与 runtime S3 密钥必须相互独立；在准确提交 SHA 上依赖安装、lint/typecheck、契约、数据库、测试、构建和 E2E 全部实际执行并成功，不能只通过初始化步骤。
2. OpenAPI `2.4.2-ch010` 本轮实测保持 172 paths、196 operations、196 unique operationId、307 schemas、685 schema refs 和 0 dangling refs；旧 `LifecycleAction` 删除，Product/SKU 专用 DTO 闭合。
3. Product/SKU 固定创建状态、完整三动作矩阵、恢复目标、不级联、首次 `published_at`、nullable 最低活动价、归档 SKU、8 图、零库存余额、不可变 code 和四个 422 在产品、原型、API、生成 contracts 与实现说明中一致。
4. Prisma 与 `0001_initial` 逐字节不变；Prisma validate、空库回放、权限验证、冻结检查和 migration diff=0 全部通过。
5. B4 按 B4.0 至 B4.4 严格串行，每批验收后暂停，当前批次只有 `P0=0/P1=0` 才能进入下一批。Banner 和库存人工调整/流水不得混入 Product/SKU 最小闭环。
6. B4.4 必须补齐真实 browser → Nest → PostgreSQL/Redis/MinIO 纵向证据、普通 CI 的 B3/B4 full gates，以及最终 B4 SHA 的 Supabase rollback-only 和无残留断言。
7. CH-009 对单人维护 development reviewer 风险的接受已在 B4 结束时耗尽；CH-011 仅将同类补偿控制延长至 B5.4，staging 前仍必须取得外部独立代码、安全、数据库和验收证据复核。

### 4.3 B5 研发准入

CH-011 已批准单人维护者在脱敏 development 范围内执行 B5；CH-012 随后已批准 B5.0 产品/API 契约变更。B5.0 审计发现的 Banner 状态写入、`low_stock` 口径和库存高风险 preview 三个 P0 已按批准方案闭合：

1. CH-012 必须落实到产品说明、OpenAPI、生成 contracts、公共内核、原型与契约门禁；B5.0 验收暂停前不得开始 B5.1 repository、API 或工程前端。
2. B5 不新增数据库迁移；Prisma 与 `0001_initial` 必须逐字节不变，并通过 validate、冻结检查、权限验证和 migration diff=0。
3. B5.1 至 B5.4 必须串行实施，每批 `P0=0/P1=0` 后暂停；最终准确实现 SHA 必须同时取得普通 CI 与 Supabase rollback-only 成功证据。
4. 第三方 Actions 已固定到核验 commit，Supabase CA 已增加固定 SHA-256 校验，根级 B4 纵向 runner 已纳入 lint；后续修改须在新的准确 SHA 上重新验证。
5. CH-011 在 B5.4 结束时自动失效，不覆盖 staging、production、真实客户数据或真实资金链路。

### 4.4 B6 研发准入

CH-013 曾批准仅适用于 B6 脱敏 development 的单人维护门禁例外，CH-014 已批准 B6 匿名目录产品/API 契约。B6.0-B6.4 已按顺序完成；最终 SHA `9d8911934a5aa2b09ece2e12935bcafc9ccdcba6` 的普通 CI Run `32953444096` 与 Supabase rollback-only Run `32954611250` 同 SHA 双绿，B6 development `GO`，CH-013 已自动失效：

1. OpenAPI 升级为 `2.4.4-ch014`，统计保持 172 paths / 196 operations / 196 unique operationId / 312 schemas；售罄投影、综合排序、搜索、无分页品牌/分类、首页分区状态和 429/Retry-After 必须可机器校验。
2. B6 不新增迁移；Prisma 与 `0001_initial` 逐字节不变。B6.0 已实测冻结检查、Prisma validate 和 migration diff=0。
3. B6.3 已实现版本化游客本地购物车、详情回源刷新和登录提示；404/缺 SKU 与售罄项从合计排除，429/网络失败保留且不误判。当前累计 unit 为 `6 files / 84 passed`，miniapp typecheck/scoped lint、H5/MP-Weixin build、全仓 `pnpm check`、`git diff --check` 和 `db:check-frozen` 均通过，五视口 Playwright 为 `34 passed / 56 designed skips`，不包含 CUSTOMER 会话、服务端 Cart、订单或交易写入。
4. B6.4 已在同一最终 SHA 取得普通 CI 与 Supabase rollback-only 双绿，并证明 PostgreSQL、MinIO 和 Redis fixture 无残留。
5. CH-013 已自动失效；微信登录、收藏、服务端购物车、结算、订单、staging、production、真实客户数据和资金链路均未因 B6 获得准入。

### 4.5 B7 研发准入

CH-015 曾只允许 B7.0-B7.5 脱敏 development 的单人维护补偿控制；CH-016 冻结消费者身份、资料手机号、归因/服务代理和同步注销契约。B7.0-B7.5 已完成；最终 SHA `3f844bfb9866854ceedb975ad0dc4fd7cacfb04a` 的普通 CI Run `33055090596` 与 Supabase development rollback-only Run `33056078437` 同 SHA 双绿，B7 development `GO`，CH-015 已自动失效：

1. OpenAPI 升级为 `2.4.5-ch016`，专项实测 173 paths / 197 operations / 197 unique operationId / 320 schemas / 699 schema refs / 2,617 local refs / 0 dangling refs，Redocly 0 warning；三份法律文本、role/assurance/audience、Provider、If-Match、候选/服务代理和注销 DTO 已闭合。
2. B7 不新增迁移；Prisma、两份 `0001_initial`、76 models/59 enums 和冻结哈希逐字节不变，migration diff=0。
3. B7.0 已完成 Redocly、生成漂移、专项契约、静态原型和冻结数据库门禁；B7.1 已完成 Store/Admin audience 隔离、Mock 登录、会话轮换/重放、no-store、限流、HASH_ONLY、审计及 full/rollback；B7.2 已完成 profile/手机号双主体隔离、If-Match、AES-GCM/HMAC、服务端 Provider、重授权/撤回、并发、故障整体回滚和敏感扫描；B7.3 已完成可选认证 fail closed、候选 HMAC/TTL、登录迁移、并发绑定、服务代理最小投影、故障回滚和敏感扫描；B7.4 已完成 5 分钟能力、阻断 fail-closed、单事务匿名化、全部会话 tombstone、HASH_ONLY、审计、durable `PENDING account.anonymized` Outbox 事实以及 full/rollback 门禁，且未把持久化事实冒充事件已投递或消费。
4. B7.4 退出复审达到 `P0=0/P1=0`；B7.5 小程序工程、本地自动化、最终只读复审和最终同 SHA 双绿均已闭合，并证明 token、会话、fixture 和个人数据无残留。
5. CH-015 已失效；交易、真实微信和 staging/production 仍未因 B7 获得准入。

### 4.6 B8 研发准入

CH-017 只允许 B8.0-B8.5 脱敏 development 的单人维护补偿控制；CH-018 冻结收藏、服务端购物车、游客合并与地址契约。B8.0-B8.5 已完成；最终 SHA `0fc5a8d3d1f07d3b5c9fcadf7ea4ca9560a0911a` 的普通 CI Run `33141704459` 与 Supabase development rollback-only Run `33142971501` 同 SHA 双绿，B8 development `GO`，CH-017 已自动失效：

1. OpenAPI 升级为 `2.4.6-ch018`，专项实测 173 paths / 198 operations / 198 unique operationId / 323 schemas / 701 schema refs / 2,653 local refs / 0 dangling refs，Redocly 0 warning；Favorite/Cart/Address DTO、ULID、no-store、HASH_ONLY 与 120/60 限流必须可机器校验。
2. Prisma、根目录与文档目录的 `schema.prisma`、两份 `0001_initial` 必须逐字节不变；Prisma validate、冻结检查和 migration diff=0 全部通过。
3. 小程序已开放收藏/地址入口并实现失效收藏、登录后服务端购物车、合并 journal、默认地址/409/422；结算、订单、支付仍不可从 B8 主流程进入。
4. B8.1-B8.5 已逐批实施并在每批复审 `P0=0/P1=0` 后暂停；B8.5 同 SHA 远端双绿已证明 PostgreSQL/Redis/MinIO 无测试残留。
5. CH-017 已在 B8.5 自动失效，不适用于 B9、staging、production、真实微信身份、真实客户数据或资金链路；首次 staging 仍须外部独立复核。

### 4.7 B9 研发准入

CH-019 曾只允许 B9.0-B9.5 脱敏 development 的单人维护补偿控制；CH-020 冻结报价凭证、待付款订单、库存预占、取消/超时释放和 0002 索引迁移契约。B9.0-B9.5 已完成，三项原 P1 已关闭，最终复审保持 `P0=0/P1=0/P2=1`；最终同 SHA 双绿已登记，B9 development `GO`，CH-019 已自动失效：

1. OpenAPI 升级为 `2.4.7-ch020`，专项实测 173 paths / 198 operations / 198 unique operationId / 325 schemas / 703 schema refs / 2,665 local refs / 0 dangling refs，Redocly 0 warning；报价、提交、四个 409、ULID、no-store、HASH_ONLY 与 120/60 限流必须可机器校验。
2. 两份 `0001_initial` 必须逐字节不变；`0002_b9_inventory_fact_indexes` 只可增加预占 SKU-first 普通索引与 ledger 条件唯一索引，并通过历史重复预检、空库/0001 升级回放、权限、冻结指纹和 migration diff=0。
3. B9.0 已在准确 SHA `97d5f979e63994c4fc136217d870a726f92b3d93` 的 main-only 受保护 workflow 完成 Supabase development 前向迁移和随后 rollback-only smoke；项目 ref、SHA、TLS、迁移历史和 DB concurrency 均已受控验证。
4. B9.1-B9.5 已完成本地实现与验收；数据库、API、Worker、真实纵向、全仓和远端 workflow 门禁均已接入并通过本地验证，最终本地独立复审为 `P0=0/P1=0/P2=1`。
5. 首次下单按 `idempotency -> account/customer -> CART/cart items -> address -> binding/agent -> brand -> category -> product -> SKU ASC -> balance ASC -> insert order/reservation/snapshots -> ledger -> audit/outbox`；只有取消/Worker 按 `idempotency（Worker 跳过） -> order -> payment_intent -> SKU ASC -> balance ASC -> reservation ASC -> ledger -> audit/outbox`。候选 ID 只用于无锁定位且锁内重验，禁止混用或让 reservation 先于 SKU/balance；B9.2/B9.3 已以 Product/SKU lifecycle confirm、库存调整并发回归验证该锁序。B10 创建 payment_intent 必须先锁定并重验订单，再插入意图，不得反向加锁。
6. 最终 SHA `19f9ad57190b28d11922db805b39af95b2f7ba3b` 的普通 CI Run `33230769777` 与 Supabase development rollback-only smoke Run `33233087710` 同 SHA 且均为 `completed/success`；B9.5 的订单、预占、流水、购物车、审计、幂等、Outbox、Redis 和对象精确清理门禁随之闭合。
7. B9 development `GO`，CH-019 已自动失效。唯一非阻断 P2 为 TR-020；支付、退款、履约、售后、真实微信、staging 和 production 未因 B9 获得准入，真实支付仍为 `NO-GO`。

### 4.8 B10 研发准入

CH-021 只允许 B10.0-B10.6 脱敏 development 的单人维护补偿控制；CH-022 冻结 Mock 支付、结果消费、关单对账、迟到退款和 0003 支付事实索引。B10.0 与 B10.1 本地范围均已完成；B10.1 退出复审为 `P0=0/P1=0`，现暂停等待用户批准进入 B10.2。B10.2 未开始，B10 尚未 `GO`，CH-021 继续有效：

1. OpenAPI `2.4.8-ch022` 本地解析实测为 173 paths / 198 operations / 198 unique operationId / 326 schemas / 705 schema refs / 2,678 local refs / 0 dangling refs；支付 DTO、错误、attempt 状态、no-store/HASH_ONLY、服务端 Provider 选择及 order/payment intent/refund ULID 路径必须可机器校验。
2. 既有 0001/0002 逐字节不变；0003 只增加两个支付事实条件唯一索引，并通过两类历史重复预检、空库/升级回放、权限、冻结指纹与 migration diff=0。本地门禁已通过。
3. B10.0 SHA `8cd5781eed9349d6f110fa43510e78c7f525a482` 已依次取得普通 CI Run `33242561514`、受控 Supabase development migration Run `33243979003`、rollback-only smoke Run `33244107293` 成功证据，退出复审为 `P0=0/P1=0`；后续批次仍不得以本地结果替代各自要求的远端证据。
4. B10.1-B10.6 必须严格串行，每批 `P0=0/P1=0` 后暂停。B10.1 的 Mock Redis 状态 TTL 固定 7 天，初始 `next_reconcile_at` 固定为数据库当前时间后 60 秒；Provider 网络调用不得持有数据库锁，Redis/Provider UNKNOWN 必须 fail closed。既有 `CREATING` 先 query、仅 `NOT_FOUND` 才 create；`OPEN` 原子推进订单 `PROCESSING/version+1`，明确终态原子恢复 `UNPAID`，陈旧 `If-Match` 新命令返回 409；Mock result 仅允许 `development + MOCK` 路由且只写 Inbox。
5. 消费者取消在 Provider 未确认时只能返回 `202 + CLOSE_PENDING` 并保持预占；ADM-10 只读待办必须闭合为 `PAYMENT_INTENT | PAYMENT_SETTLEMENT | LATE_PAYMENT_REFUND`，其列表及 200/202 响应均不得携带 capability。迟到退款 attempt 必须从 `INITIATED` 开始。
6. B10.6 必须覆盖 Provider 响应丢失、关单 UNKNOWN、重复/乱序/延迟结果、Worker 崩溃、支付/取消/超时竞争和退款失败，并证明支付/退款/流水唯一及 `physical_qty >= locked_qty >= 0`。
7. CH-021 在 B10.6 自动失效且从不适用于 staging/production；真实微信回调、证书、验签、ACK 与真实资金链路仍须外部独立复核和单独准入。

### 4.9 生产上线

下列事项属于生产门禁，即使研发准入通过也不得跳过：

1. 正式微信小程序主体、登录、支付和退款凭据到位并完成签名/回调联调。
2. Supabase 项目区域、套餐、备份、PITR、恢复目标和数据驻留通过评审。
3. 最小权限角色、关闭 Data API、RLS/default grants、密钥轮换和监控告警完成实库验证。
4. 隐私政策、手机号授权、账号删除、银行卡处理、代理合同、佣金税务和付款 SOP 获得专业审批。
5. 容量、故障恢复、对账、退款失败、负余额和提现异常完成演练，并具备回滚方案。

## 5. 监控机制

| 节点 | 动作 | 输出 |
|---|---|---|
| 每日开发 | 监控 CI、契约漂移、迁移失败、资金/库存测试失败和安全扫描 | 缺陷或风险更新 |
| 每次合并 | 对订单、库存、支付、退款、佣金、钱包和认证变更进行领域审查 | 评审记录与测试证据 |
| 每个迭代 | 复核风险概率、影响、责任人和剩余风险，检查范围变更 | 更新后的风险登记表 |
| 联调前 | 复核第三方官方契约、凭据、回调白名单和 Mock/真实环境隔离 | 联调准入清单 |
| 上线前 | 执行生产门禁、恢复演练、安全与合规签字 | Go/No-Go 决策记录 |

## 6. 事件升级

- 任何资金、库存、隐私或银行卡明文异常立即暂停对应写路径并升级为 P0。
- 同一幂等键产生不同业务结果、订单状态轴无法解释或钱包不平，应停止发布并先完成对账。
- 高风险项分数升至 6 以上且无有效控制时，自动撤销对应阶段准入。
- 新需求若改变角色、归因、价格、库存、资金或履约边界，必须先创建新的需求变更记录。

## 7. 当前状态

CH-022 已将产品/API 基线升级到 v2.4.8/2.4.8-ch022，B0-B9 development 既有结论和 B9 `GO` 证据保持有效。B10.0 本地契约、完整迁移回放、权限、diff、静态原型与准确 SHA 三项远端门禁均已通过。B10.1 Provider/支付意图/Mock Inbox 本地实现与验收已完成，退出复审为 `P0=0/P1=0`，现暂停待批 B10.2；B10.2 未开始，B10 尚未 `GO`。CH-021 只覆盖 B10.0-B10.6 脱敏 development 且继续有效。新增 TR-021 至 TR-024、SE-008/009 均须在对应批次通过故障注入、安全扫描和环境矩阵收口；TR-020 仍为 B9 遗留非阻断 P2。正式微信凭据、storage origin allowlist、真实支付、恢复演练和外部合规继续作为 staging/production 门禁；staging、production 与真实支付仍为 `NO-GO`。
