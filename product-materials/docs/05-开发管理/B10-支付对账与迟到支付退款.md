# B10 支付、对账与迟到支付退款

> 批次：B10；当前产品/API 基线：v2.4.8 / CH-022；数据库修复：CH-023；交付门禁：CH-021；更新日期：2026-08-30；当前状态：B10.5 已完成本地实现、五视口验收和退出复审，B10.6 未开始。在 B10.6 最终门禁闭合前不得标记 B10 development `GO`，CH-021 继续有效；数据范围：仅 Mock Provider 与脱敏 development；真实微信、真实客户数据、staging/production 和真实资金链路：`NO-GO`。

## 1. 当前结论

B0-B9 development 均已完成。B9 最终实现 SHA `19f9ad57190b28d11922db805b39af95b2f7ba3b` 的 [普通 CI Run `33230769777`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33230769777) 与 [Supabase development rollback-only smoke Run `33233087710`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33233087710) 同 SHA 且均为 `completed/success`，B9 development `GO`，CH-019 已自动失效。最终复审的唯一非阻断 P2 为 TR-020：大量正常到期订单下完整性扫描工作量尚未有界。

用户已批准 CH-021、CH-022 和 CH-023。B10.0-B10.5 已完成本地实施，其中 B10.5 已交付 MP-09、订单 PAY/CANCEL、支付 journal、严格响应解码以及 ADM-10 最小只读对账待办；支付意图级会话锁、Callback Inbox 回排锁、关单收敛和财务审计/Outbox 原子性均已闭合。B10 总范围仍仅包括 Mock 支付意图、支付结果消费、库存结算、最终归因/佣金快照、取消/超时关单、自动对账和迟到支付全额退款；B10 尚未标记 development `GO`，CH-021 继续有效。真实微信回调契约保留但未配置时不注册路由；普通售后退款、物流履约、真实微信支付和生产发布不在本阶段。

## 2. CH-021 单人 development 门禁

CH-021 来源为团队资源约束，类型为修改交付门禁，中等影响，经产品负责人和技术负责人批准立即执行。

- 只适用于 B10.0-B10.6 的脱敏 development，不能用于 staging、production、真实微信、真实客户数据或真实资金链路。
- 每批完成自动化、故障注入、无残留验证和只读复审，`P0=0/P1=0` 后暂停并等待下一批批准。
- B10.0 必须以准确 SHA 依次取得普通 CI、Supabase development migration 和 rollback-only smoke 证据。
- B10.6 必须在同一最终实现 SHA 上取得普通 CI 与 Supabase development rollback-only 双绿；完成后 CH-021 自动失效。
- 第一次进入 staging 前必须恢复外部独立代码、安全、数据库与资金链路复核。

## 3. CH-022 冻结决策

CH-022 来源为技术限制与阶段路线，类型为修改需求，中等影响，经产品负责人和技术负责人批准立即执行。

### 3.1 版本与契约目标

- 产品/PRD 基线升级为 `v2.4.8 / CH-022`，OpenAPI 升级为 `2.4.8-ch022`。
- 本地解析实测为 `173 paths / 198 operations / 198 unique operationId / 326 schemas / 705 schema refs / 2,678 local refs / 0 dangling refs`。
- 支付 Provider 由服务端配置决定，消费者不得在请求中选择 Provider。
- 所有 `order_id`、`payment_intent_id` 和 `refund_id` 路径参数统一使用 ULID。
- Mock 调试结果仅接受 `SUCCEEDED | FAILED | CANCELLED`；交易号、事件号和是否迟到均由服务端判定。
- 写操作使用 `HASH_ONLY`，同键重放重新鉴权并返回当前投影；支付 capability、Provider payload、签名和完整响应不得进入幂等缓存。消费者 capability 只在支付发起响应的内存态使用，ADM-10 列表和 reconcile 的 200/202 响应均不得携带 capability。

### 3.2 支付与结果状态

- 每单最多一个活动支付意图；本地先写稳定 `intent_no + CREATING`，事务外调用 Provider，再以短事务收敛。
- Provider 结果闭合为 `OPEN | SUCCEEDED | FAILED | CANCELLED | CLOSED | NOT_FOUND | UNKNOWN`；UNKNOWN 必须 fail closed。
- 支付成功原子完成 attempt、订单已支付/待发货、预占消耗、实物与锁定库存扣减、唯一 `ORDER_PAID_DEDUCT` 流水、最终归因、逐订单项佣金快照、审计和 Outbox。
- `product.sales_count` 只是可重建销量缓存：仅在上述结算全部校验通过并原子收敛时按订单项数量递增；首次落入 `MANUAL_REQUIRED` 时不得递增，后续补偿成功时只递增一次；达到 PostgreSQL `integer` 上限后饱和，缓存异常不得否认或回滚 Provider 成功事实。
- 支付前候选代理仍为 ACTIVE 才冻结 AGENT；否则降级 DIRECT，不阻断支付。
- 失败或取消保留待付款订单和有效预占，允许以新幂等键重新支付；处理中禁止重复创建意图。

### 3.3 关单、对账与迟到支付

- 有 `CREATING/OPEN` 意图的取消或超时先写 `CLOSE_PENDING`，事务外 query/close；明确不可支付后才关单并释放库存。消费者取消若尚待 Provider 确认，返回 `202` 和 `CLOSE_PENDING` 当前投影。
- Provider 状态未知时返回当前 pending 投影，保留库存锁定；不得伪造失败或提前释放。
- 关闭后到达的支付成功只写 `SUCCEEDED_LATE` 和唯一全额 `LATE_PAYMENT` refund；订单保持关闭，不恢复库存、履约、归因或佣金。
- 退款尝试初始状态固定为 `INITIATED`，之后才进入 `PROCESSING | SUCCEEDED | FAILED`；自动退款失败或未知进入 `MANUAL_REQUIRED`。
- ADM-10 最小待办类型闭合为 `PAYMENT_INTENT | PAYMENT_SETTLEMENT | LATE_PAYMENT_REFUND`；200 表示已收敛，202 表示仍待 Provider 确认。后台响应不得携带 capability，也不得直接修改支付、订单、库存、退款或佣金状态。
- TR-020 在 B10.3 使用稳定候选游标和页内问题上限收口。

### 3.4 错误、缓存与环境

- 新增闭合错误：`ORDER_PAYMENT_EXPIRED`、`PAYMENT_NOT_ALLOWED`、`PAYMENT_RESULT_CONFLICT`（409），以及 `PAYMENT_PROVIDER_UNAVAILABLE`、`PAYMENT_CONFIGURATION_UNAVAILABLE`（503）。
- CUSTOMER 支付接口继续使用 CUSTOMER+来源 IP 的 120/60 fail-closed 限流，并对成功和 JSON 错误设置 `Cache-Control: no-store, private`、`Pragma: no-cache`。
- development/test 固定使用 Mock Provider；production 配置为 Mock 时必须拒绝启动。
- 微信支付/退款回调仍保留在线协议中，但 B10 development 不注册真实路由，也不要求微信密钥、证书、raw-body 验签或 Provider ACK。

### 3.5 B10.1 实施决策

- Payment Provider port 闭合为 `create/query/close/refund`；所有 Provider 网络或 Redis 调用必须在数据库事务外执行。
- Mock Provider 外部状态在 Redis 保留 7 天，key 使用支付域 HMAC 摘要，不保存原始客户、订单、支付意图或 IP 标识。
- 新建 `CREATING` payment intent 的 `next_reconcile_at` 固定为数据库当前时间后 60 秒，支持 Provider 响应丢失后按稳定 `intent_no` 恢复。
- 任何既有 `CREATING` intent 一律先调用 `query(intent_no)`；只有 Provider 明确返回 `NOT_FOUND` 才允许以同一 `intent_no` 调用 `create`，不得在响应丢失或重试时直接 create。
- Provider 返回 `OPEN` 时，最终短事务必须原子写 intent `OPEN`、订单 `payment_status=PROCESSING` 并递增订单版本；明确 `FAILED/CANCELLED/CLOSED` 终态时原子恢复订单 `payment_status=UNPAID`。完整幂等重放先于 `If-Match`，新命令携带陈旧版本返回 409。
- 超时、Redis 故障或 Provider `UNKNOWN` 统一 fail closed，不完成不安全的幂等事实、不伪造成功或失败，也不进入 B10.2 结算。
- Mock result 路由只在 `development + STORE_PAYMENT_PROVIDER=MOCK` 时注册；验证后仅写 Callback Inbox 并返回 202，不直接更新 intent/订单、消耗预占、扣减库存、冻结归因或佣金。

## 4. CH-023 佣金头寸触发器解阻

CH-023 来源为技术限制，类型为修改需求，影响开发进度、技术架构、测试范围和数据迁移，影响程度为中等；产品负责人和技术负责人已批准于 2026-08-30 立即执行。该变更不修改产品/API 基线、OpenAPI、generated contracts 或任何在线行为。

- 新增事务型 `0004_b10_commission_position_trigger_fix`，只以 `CREATE OR REPLACE FUNCTION` 修订 `public.enforce_commission_position_snapshot()`。
- 保留 snapshot ID 不可变及 position 原佣金必须等于不可变 snapshot 的触发器校验，只移除读取 snapshot 时不必要的 `FOR SHARE`，并显式声明 `SECURITY INVOKER`。
- 不修改 `0001` 至 `0003`，不授予 `mall_runtime` 对 `order_item_commission_snapshot` 的 `UPDATE/DELETE`，不改为 `SECURITY DEFINER`，不新增表、列、枚举、索引、RLS 或 Data API 权限。
- B10.2 解阻必须验证完整 0001→0002→0003→0004 链、函数定义/所有权/权限、不可变快照拒绝更新，以及真实 `mall_runtime` 的正佣金、0% AGENT 和 DIRECT 三条路径。
- 准确实现 SHA 必须先通过普通 CI，再通过受控 Supabase development migration，最后执行 rollback-only smoke；任一证据缺失都保持 B10.2 阻塞。

## 5. 数据库与迁移边界

- 两份 Prisma schema 以及既有两份 `0001_initial`、两份 `0002_b9_inventory_fact_indexes` 和两份 `0003_b10_payment_fact_indexes` 必须逐字节不变。
- 新增事务型 `0003_b10_payment_fact_indexes`，只增加：
  - `payment_attempt(payment_intent_id)` 在 `status IN ('SUCCEEDED','SUCCEEDED_LATE')` 时的条件唯一索引；
  - `refund(order_id)` 在 `origin_type='LATE_PAYMENT'` 时的条件唯一索引。
- 建索引前必须检查历史重复事实，发现重复则整个迁移失败；不增表、列、枚举、RLS 或权限能力。
- CH-023 在其后新增 `0004_b10_commission_position_trigger_fix`，只替换触发器函数并移除 `FOR SHARE`；完整 0001→0002→0003→0004 空库/升级回放、冻结哈希、权限/所有权、原生指纹、Prisma validate 和 `migration diff=0` 均属于 B10.2 解阻门禁。
- 0003 一旦部署即作为加法索引保留；代码回退不得执行数据库降级。
- 0004 一旦部署也保留；不得通过恢复旧 `FOR SHARE`、授予 runtime UPDATE 或改成 SECURITY DEFINER 进行降级。

## 6. 实施批次

| 批次 | 交付范围 | 退出条件 | 当前状态 |
|---|---|---|---|
| B10.0 | B9 GO 补录、CH-021/022、产品/技术/风险/静态原型、OpenAPI/contracts、0003 与受控 development 迁移 | 契约/生成、静态原型、冻结迁移链、准确 SHA 普通 CI→migration→smoke、复审 `P0=0/P1=0` | **已完成；SHA `8cd5781eed9349d6f110fa43510e78c7f525a482` 三项远端门禁成功，`P0=0/P1=0`** |
| B10.1 | Provider 抽象、Redis Mock、支付意图创建/复用与 Mock Inbox | create/query/close/refund 幂等、UNKNOWN fail closed、生产拒绝 Mock、无敏感泄露，`P0=0/P1=0` | **本地实现与验收已完成；两个复审 P1 已修复，退出复审 `P0=0/P1=0`；后续 B10.2 状态见下行和第 9 节** |
| B10.2 | 支付成功结算、库存扣减、最终归因和佣金快照 | 唯一成功 attempt/流水、库存不变量、代理并发和配置异常；CH-023 完整迁移链、真实 runtime 正佣金/0%、远端 migration/smoke，`P0=0/P1=0` | **已完成；实现 SHA `dc045fbc15d6abdd48959915ec14df9e90bc4306` 的普通 CI Run `33253341654`、Supabase development migration Run `33279894978`、rollback-only smoke Run `33280068003` 均成功；本地复审 `P0=0/P1=0/P2=2`** |
| B10.3 | 取消/超时三段关单、自动对账及 TR-020 游标扫描 | CLOSE_PENDING、UNKNOWN 保留预占、多 Worker/并发无重复释放，`P0=0/P1=0` | **已完成；SHA `524ffe84cc6e88d0e0b6b1fb9c132a9dc752317f` 的普通 CI Run `33288346901`、development migration Run `33290094491`、rollback-only smoke Run `33290244592` 均成功** |
| B10.4 | 迟到支付与全额自动退款 | 订单不复活、唯一 refund、失败转 MANUAL_REQUIRED、重复/乱序收敛，`P0=0/P1=0` | **已完成；SHA `8998e4a51605e97a952f13f4538811c19de14b63` 的普通 CI Run `33293709228`、development migration Run `33294573472`、rollback-only smoke Run `33294644725` 均成功** |
| B10.5 | MP-09、订单 PAY/CANCEL 与 ADM-10 最小对账待办 | 五视口、严格解码、支付 journal、只读待办/幂等触发，`P0=0/P1=0` | **本地完成；27 个浏览器用例通过、43 个设计跳过，退出复审 `P0=0/P1=0`** |
| B10.6 | 数据库/API/Worker/真实纵向与全仓总验收 | 最终同 SHA CI+smoke 双绿、0004 完整迁移历史正确、无残留、复审 `P0=0/P1=0` | **未开始** |

## 7. B10.0 验收门禁

| 门禁 | 必须取得的证据 |
|---|---|
| 治理一致性 | B9 双绿/GO、CH-019 失效、CH-021/022、v2.4.8、范围排除和 B10.1 准入边界在所有上游文档一致 |
| OpenAPI | 版本 `2.4.8-ch022`；本地实测 173/198/198/326，705 schema refs、2,678 local refs、0 dangling refs；闭合错误、ULID 和 Redocly/生成仍由 CI 重验 |
| 生成稳定性 | generated contracts 与 OpenAPI 同步，二次生成无漂移，contracts typecheck 通过 |
| 数据库 | 既有冻结文件不变，0003 重复预检/索引/所有权通过，完整链回放与 `migration diff=0` |
| 静态原型 | 页面总数不变；`97 responsive renders / 21 MP / 9 AGT / 22 ADM / 17 miniapp flows / 19 admin-agent flows` 通过。MP-09 展示支付处理中/成功/失败/取消/迟到退款/人工处理，订单详情提供合法 PAY/CANCEL；ADM-10 只有三类只读待办与幂等触发，且无 capability |
| 远端 | SHA `8cd5781eed9349d6f110fa43510e78c7f525a482`：[普通 CI Run `33242561514`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33242561514) → [Supabase development migration Run `33243979003`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33243979003) → [rollback-only smoke Run `33244107293`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33244107293)，均为成功 |
| 退出复审 | 证据齐全，`P0=0/P1=0`；B10.0 已完成，用户已批准进入 B10.1 |

## 8. B10.1 本地退出复审

| 门禁 | 当前证据 |
|---|---|
| Provider 恢复 | 新建 intent 才直接 create；所有既有 `CREATING` 先 query，只有明确 `NOT_FOUND` 才以原 `intent_no` create，覆盖 Provider 创建成功但响应丢失 |
| 状态原子性 | Provider `OPEN` 与订单 `PROCESSING/version+1` 在同一短事务提交；明确失败/取消/关闭终态与订单恢复 `UNPAID` 在同一短事务提交 |
| 并发与版本 | 完整幂等重放先于 `If-Match`；新命令使用陈旧版本返回 409，不以旧版本覆盖当前支付投影 |
| 范围边界 | Mock result 仅 `development + MOCK` 注册且只写 Inbox；本行保留 B10.1 退出时的范围证据，后续 B10.2 实现状态见第 9 节 |
| 退出结论 | 两个复审 P1 已修复并补测试，本地实现与验收完成，`P0=0/P1=0`；该历史结论不覆盖 CH-023 新增的 B10.2 门禁 |

本结论只关闭 B10.1 本地批次，不代表 B10 development `GO`，不触发 CH-021 失效，也不替代 B10.6 最终实现 SHA 的普通 CI 与 Supabase development rollback-only 双绿。

## 9. B10.2-B10.5 退出证据与 B10.6 准入状态

| 门禁 | 当前状态 |
|---|---|
| 结算实现 | 支付结果消费、唯一 attempt、订单/预占/库存结算、最终归因、佣金快照/position/流水、审计与 Outbox 代码及单元回归已完成 |
| 销量缓存 | 只在结算收敛时递增；`MANUAL_REQUIRED` 首次落库保持不变，补偿成功只递增一次，`integer` 上限饱和；缓存更新不能覆盖支付事实裁决 |
| 缺口根因 | 真实 `mall_runtime` 正佣金事务在冻结 `0001` 触发器的 `FOR SHARE` 处因缺少不可变 snapshot 表 UPDATE 权限失败；最小权限设计本身正确，不得扩大 runtime 权限 |
| CH-023 修复 | `0004` 只移除该行锁并显式保持 SECURITY INVOKER；不修改 0001-0003 和产品/API 契约 |
| 本地退出证据 | 完整迁移链、函数定义/权限/不可变拒绝、真实 runtime 正佣金与 0% AGENT/DIRECT 路径已通过；`db:test-b10-payments` full 为 `4 files / 65 tests` |
| 自动化入口 | 普通 CI 以 full 模式运行 B10 结算仓储；Supabase workflow 在 0004 部署后以 rollback 模式使用 `SUPABASE_RUNTIME_URL` 运行同一真实仓储测试 |
| 本地复审 | `P0=0/P1=0/P2=2`；TR-026 为畸形历史成功事实的完整形状校验，QA-005 为多商品销量原子回滚显式 fixture，均登记为 B10.6 前关闭的非阻断项 |
| 远端退出证据 | 准确实现 SHA `dc045fbc15d6abdd48959915ec14df9e90bc4306`：[普通 CI Run `33253341654`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33253341654) → [Supabase development migration Run `33279894978`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33279894978) → [rollback-only smoke Run `33280068003`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33280068003)，三项均为 `completed/success`，且按该 SHA 闭合 |
| 当前结论 | **B10.2-B10.4 已闭合远端门禁，B10.5 已本地完成；B10.6 未开始，B10 development 不得 `GO`，直至最终同 SHA 门禁闭合** |

### 9.1 B10.3 当前实施与本地验收

| 项目 | 当前证据 |
|---|---|
| 三段关单 | 首个短事务将活动 `CREATING/OPEN` intent claim 为 `CLOSE_PENDING`；Provider `query/close` 在事务外执行；最终短事务先再次获取同域幂等锁，重放赢家投影或完成明确终态关单；Provider `UNKNOWN` 保持 `CLOSE_PENDING` 与库存锁定 |
| 成功恢复 | Provider 查询/关单若返回 `SUCCEEDED`，API 与 Worker 均在 finalize 同一 Serializable 事务中生成可验签 Mock callback 并写入唯一 Callback Inbox；写入失败会连同 intent 更新回滚，正常支付或后续迟到支付路径可继续消费；二次幂等命中也不会丢弃刚观察到的成功事件 |
| 超时扫描 | 候选订单按 `pay_expires_at ASC, id ASC` 稳定游标遍历；健康候选也推进游标，避免只翻页问题订单造成遗漏；Worker 对账复用现有 poll/batch/retry 配置 |
| 数据库定向验收 | `pnpm --filter @qingxu/database exec vitest run src/store-order.repository.spec.ts`：1 file / **17 passed**；覆盖稳定候选游标、CLOSE_PENDING claim、Provider 延迟后的取消裁决、数据库时钟和 UNKNOWN 不释放预占 |
| Worker 定向验收 | `pnpm --filter @qingxu/worker exec vitest run src/order-timeout.service.spec.ts`：1 file / **15 passed**；覆盖事务外 Provider close、明确终态关单、UNKNOWN pending、成功 Inbox 恢复与审计/Outbox 边界 |
| API/构建与静态门禁 | Store Orders HTTP 路由 **16 passed**（含 202 `CLOSE_PENDING`），Store Payments service **13 passed**，Payment Provider **10 passed**；全仓 `test`：database **499 passed**、API **933 passed**、Worker **70 passed**、miniapp **212 passed**；`pnpm typecheck`、`pnpm build`、`pnpm build:miniapp`、`pnpm contracts:check`、`pnpm contracts:lint`、`pnpm db:check-frozen`、`pnpm scan:sensitive`、`pnpm config:check`、`pnpm prisma:validate` 与串行 `pnpm lint` 均通过。Provider 时间戳不写入订单关单事实，关单事实统一使用数据库事务时钟；只读复审为 `P0=0/P1=0` |
| 远端证据 | 准确实现 SHA `524ffe84cc6e88d0e0b6b1fb9c132a9dc752317f`：[普通 CI Run `33288346901`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33288346901) → [Supabase development migration Run `33290094491`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33290094491) → [rollback-only smoke Run `33290244592`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33290244592)，三项均为 `completed/success` |

### 9.2 B10.4 当前实施与本地验收

| 项目 | 当前证据 |
|---|---|
| 迟到支付准备 | 仅已由用户取消或支付超时关闭、预占已释放/过期且没有结算、归因或佣金事实的订单可进入迟到支付；原子写唯一 `SUCCEEDED_LATE` attempt、intent 成功事实、订单 `LATE_SUCCESS_REFUND_PENDING`、唯一全额 `LATE_PAYMENT` refund/refund item 和 `INITIATED` attempt |
| 事务外退款 | Worker 先提交支付与退款准备事实，再以订单首锁认领退款；Mock Provider `refund` 只在数据库事务外调用，固定 `refund_no` 支持响应丢失后同事实重试；成功或失败结果在最终 Serializable 事务收敛 |
| 成功与失败 | 成功写 refund/attempt `SUCCEEDED`、订单 `LATE_SUCCESS_REFUNDED` 与全额退款计数；订单继续 `CLOSED/NOT_STARTED`，不恢复或扣减库存，不冻结归因或佣金。Provider `FAILED/UNKNOWN` 或异常原子写 refund/attempt `FAILED`、订单 `MANUAL_REQUIRED`，进入后续 ADM-10 对账待办 |
| 并发与乱序 | 支付交易身份按 Provider transaction 与金额收敛，不因重复事件的新 event ID/时间戳重复记账；stale claim/finalize 在另一 Worker 已完成后返回 `TERMINAL/REPLAY`；退款明细与订单金额闭包在 Provider I/O 前验证 |
| 定向验收 | 数据库仓储 3 files / **61 passed**；Worker callback/module 2 files / **31 passed**；真实 `mall_runtime` rollback `store-payment.integration.spec.ts` **7 passed**，覆盖成功、失败、重复事件与事务外无残留 |
| 冻结边界 | Prisma、`0001` 至 `0004`、OpenAPI 与 generated contracts 均未修改；B10.4 不新增迁移、路由或前端页面 |
| 复审与后续 | 两路只读复审均为 `P0=0/P1=0`。`PAYMENT_TIMEOUT` 显式迟到退款、真实跨提交崩溃恢复、Redis Mock Provider 与浏览器纵向故障注入留在 B10.6 总验收；B10.5 已本地完成 |
| 远端证据 | 准确实现 SHA `8998e4a51605e97a952f13f4538811c19de14b63`：[普通 CI Run `33293709228`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33293709228) → [Supabase development migration Run `33294573472`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33294573472) → [rollback-only smoke Run `33294644725`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33294644725)，三项均为 `completed/success`，且按该 SHA 闭合 |

### 9.3 B10.5 本地退出证据

| 项目 | 当前证据 |
|---|---|
| 消费者支付 | MP-09 支付结果页只读取服务端订单投影；订单详情开放合法 PAY/CANCEL。支付 journal 仅保存订单 ID、订单版本和幂等键，Provider capability 仅存在内存，丢包以同键重试 |
| 管理对账 | ADM-10 仅展示支付意图、结算和迟到退款三类待办；触发接口精确返回 200/202，不提供人工改状态、人工退款或履约能力 |
| 并发与收敛 | 对账命令以 payment intent 为粒度持有有界 session advisory lock，使用独立 `coordinationPool(max=1)` 覆盖重验、事务外 Provider I/O 和完成提交，避免占满 Prisma 主池后自锁；Callback Inbox 回排与 Worker 共用 Inbox 锁。锁超时、连接丢失或安全解锁失败均 fail closed，不得返回伪成功，同幂等键可重试收敛 |
| 终态关单补偿 | 终态非成功 intent 已落库但订单未关闭或仍有 ACTIVE 预占时，对外继续投影为 `PAYMENT_INTENT/CLOSE_PENDING`，`last_error_code=ORDER_CLOSE_INCOMPLETE`；reconcile 不再次调用 Provider，而是在 Serializable 事务内重验 intent/订单/成功 attempt 后恢复 `UNPAID`、完成关单、释放预占并原子写审计/Outbox |
| Provider 边界 | 当前 Provider 配置与存量 intent 不一致时返回 503；Provider intent locator 不一致时返回 `PAYMENT_RESULT_CONFLICT`，两者都必须在 close、数据库回写或幂等完成事实之前 fail closed |
| 财务可追溯 | payment intent、refund、order 的状态变化在各自 Serializable 事务内同时写 `STATUS_VERSION` 审计和 Outbox；存量 Provider 与当前配置不一致时以 503 fail closed |
| 契约勘误 | 迟到退款已收敛分支允许当前 `payment_intent_status=SUCCEEDED`，并兼容既有 `CLOSED/EXPIRED` 历史事实；不新增状态、路径或 schema，统计保持 173/198/198/326 |
| 自动化 | 全仓单元/服务测试 **2,052 passed / 127 designed skips**；B10 Miniapp **12 passed / 28 skipped**，ADM-10 **15 passed / 15 skipped**；覆盖 375/390/414/1024/1440 五视口 |
| 工程门禁 | lint 0 error、typecheck、全仓 build、H5/MP-Weixin build、敏感扫描、Redocly、契约统计/生成无漂移、Prisma validate、冻结迁移链和 `migration diff=0` 全部通过 |
| 退出结论 | **本地 `P0=0/P1=0`，B10.5 完成并暂停；B10.6 未开始。该结论不等于 B10 development `GO`，CH-021 继续有效** |

## 10. 回退与未授权事项

- B10.1/B10.2 已完成，不能再整体回滚 CH-022 基线；已部署的 0003/0004 作为加法索引和函数修复保留，不执行数据库降级。
- 当前可通过关闭支付意图路由、Mock 结果入口、支付结果 Worker handler、小程序支付入口和 ADM-10 对账导航/路由回退应用实现；已部署的 0004 保留，不恢复旧 `FOR SHARE`，不执行数据库降级。
- B10.0 的静态原型只表达已批准交互，不是 repository、API、Worker、迁移或真实资金链路验收证据。
- 普通售后退款、物流、履约、确认收货、完整 Admin/Agent 订单、真实微信和 production 仍须后续独立变更与准入。
