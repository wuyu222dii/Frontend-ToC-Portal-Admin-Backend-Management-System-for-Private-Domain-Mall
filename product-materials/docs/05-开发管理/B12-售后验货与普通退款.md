# B12 售后验货与普通退款

> 批次：B12；目标产品/API 基线：`v2.4.10 / CH-026`、OpenAPI `2.4.10-ch026`；更新日期：2026-09-02；当前状态：CH-026 已经 CCB 批准并立即执行。B12.0 已在精确 SHA `03a1e9f98bea0926887fb68354ca602566148f6a` 取得普通 CI、Supabase development migration 与 rollback-only smoke 三项成功证据并完成退出。CH-027 已由产品负责人和技术负责人批准并仍有效。B12.1-B12.6 本地实现与本地验收已完成：B12 full DB `6 files / 65 passed`、miniapp/admin E2E `45 passed`、全仓单测 `2738 passed / 166 skips`、完整 `RETURN_REFUND` 真实纵向 `1/1` 及全资源精确清理通过；lint `0 errors / 437 existing warnings`、typecheck、完整 build 与敏感扫描通过。最终实现 SHA 的普通 CI 与 Supabase rollback-only 同 SHA 双绿尚未取得。B12 整体仍非 development `GO`，当前 `P0=0/P1=0/P2=1`，唯一 P2 为 B12.5 orphan；真实微信退款、真实客户数据、staging/production：`NO-GO`。

## 1. 当前结论

B0-B11 development 均已完成。B11 最终实现 SHA `c8e42c44a6a31099c00070294a85f628913165df` 的普通 CI 与 Supabase development rollback-only smoke 已在同一 SHA 成功，B11 development `GO`，CH-025 已自动失效。该历史结论只证明 B12 的上游基线可用，不替代 B12 自身准入。

B12.0 最终准入 SHA `03a1e9f98bea0926887fb68354ca602566148f6a` 的[普通 CI Run `33462227180`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33462227180)、[Supabase development migration Run `33464158444`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33464158444)与[rollback-only smoke Run `33464267703`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33464267703)均为 `completed/success`。三项 `head_sha/target_sha` 一致，B12.0 已完成；该结论只准入下一批，不代表 B12 development `GO`。

B12.1 已完成独立 Store aftersale repository/module、PREVIEW/CONFIRM、本人列表/详情、允许阶段取消、订单售后摘要与服务端权威 `APPLY_AFTERSALE`，并将 CUSTOMER `AFTERSALE_EVIDENCE` 上传/完成/下载纳入与其他登录后 Store 操作相同的 CUSTOMER+来源 IP `120/60` fail-closed 限流。完整幂等重放、五分钟凭证、额度 CAS、证据归属、跨客户 404、审计时间线因果顺序和 `order -> order_item -> aftersale -> aftersale_item -> evidence` 锁序均已闭合；本批没有修改 Prisma 或迁移。

B12.2 已完成 Admin 售后列表/详情、初审通过、初审拒绝 preview-confirm、总部退货地址版本发布，以及消费者退货物流提交。退货地址按版本与快照分别使用 AES-256-GCM 和独立 AAD，首次配置、版本替换、准确历史幂等重放、缺失地址 422、拒绝释放占用、退货物流/取消与发货竞态均已闭合；审计、HASH_ONLY 幂等事实和错误响应不含地址或物流 PII。本批继续不修改 Prisma 或迁移。

B12.3 已完成实收验货、`PASS | ABNORMAL` 证据封存、异常继续退款和退货后拒绝 preview-confirm。验货精确覆盖全部售后项并在锁内校验数量等式与异常形状；证据限定当前管理员创建、`READY/PRIVATE/AFTERSALE_EVIDENCE/private/{file_id}` 且全局未绑定，封存后由数据库不可变与 manifest 触发器保护。继续退款只保留批准额度，退货后拒绝释放全部剩余额度，两者均不写库存；幂等重放、`If-Match`、独立高风险 action、证据下载和 no-store 已闭合。本批继续不修改 Prisma 或迁移。

B12.4 已完成普通退款 preview-confirm、总部纯金额补偿、稳定失败重试及 Mock Provider -> Inbox -> Worker 收敛链路。成功事实原子更新 refund/attempt、库存与销量、订单退款双轴、完成原因、佣金头寸/流水与代理钱包；金额补偿仅占金额且绝不回库。Provider 事务外调用、同 attempt 结果精确重放、历史 attempt 和旧 Outbox 事件保护、创建丢响应恢复、重复/乱序结果、Worker 崩溃后 durable retry、单活动/单成功 attempt 与 HASH_ONLY 幂等均已闭合。本批没有修改 OpenAPI、generated contracts、Prisma、既有迁移或静态前端。

B12.5 已完成 MP-13/14 与 ADM-12/13/16 工程前端，覆盖售后创建/详情、初审、总部退货地址、消费者退货物流、验货、异常解决、普通退款/失败重试和金额补偿。两端均使用 generated contracts 与严格响应解码器，服务端投影权威决定金额、状态、配额和可用动作；Admin 证据上传额外绑定 intent/complete 的用途、文件 ID、状态、时间戳、MIME、SHA-256 与 PRIVATE `public_url=null`。移动导航、409 后配额 clamp 和精确金额求和问题已闭合。登录态请求绑定账户/客户与会话代次；消费者售后确认和总部退款命令使用 24 小时、分账户、仅保存加盐完整请求哈希的 durable journal，在刷新、响应丢失和重新认证后继续使用同一幂等键，且不持久化自由文本原因或原始身份。miniapp E2E 为 `21 passed / 29 designed skips`，admin E2E 为 `24 passed / 21 designed skips`，合计 `45 passed / 50 designed skips`，覆盖 375/390/414/1024/1440 五视口；全仓单测为 `2735 passed / 165 designed or environment skips`。本批未修改 OpenAPI、Prisma schema 或既有迁移。

B12.6 已完成本地验收实现、普通 CI 接线与本地总验收。补充后的 B12 full DB 为 `6 files / 65 passed`，其中 admin-refund 真实 integration `6/6`；两端 E2E 合计 `45 passed`，全仓单测 `2738 passed / 166 skips`。完整 `RETURN_REFUND` 浏览器→Nest→PostgreSQL/Redis/MinIO→Worker 真实纵向已在全新 r3 隔离环境 `1/1` 通过，用时 `25.98s`，覆盖消费者私有凭证、跨所有者管理员受控下载、退货地址快照、退货物流、验货、Callback Inbox、`RETURN_RESTOCK`、佣金与订单收敛。首轮联跑发现并修复 AuditRepository 白名单遗漏 `RECORD_INSPECTION / CONTINUE_REFUND / REJECT_AFTER_RETURN` 的应用层 500，审计单测 `74/74` 通过；最终纵向同时以 9 条精确审计多重集断言验证 file `CREATE / COMPLETE / READ_SENSITIVE`、aftersale `CREATE / APPROVE / UPDATE / RECORD_INSPECTION` 及 refund 管理员/Worker 两条 `REFUND`。Redis DB 15、唯一 bucket、11 类数据库事实、数据库连接与四个端口均归零；测试数据库、容器、网络、两个卷和 bucket 均精确清理。B12.5 的 `2735 passed / 165 skips` 保留为该批历史结果，不替代 B12.6 当前结果。

B12 交付普通售后申请、可退额度占用、初审、退货物流、实收验货、异常处置、普通退款与失败重试，并在退款成功事实上原子完成库存回库、订单净额和佣金冲正。总部纯金额补偿使用独立来源，只占金额、不占数量且不回库。

CH-026 解决的是已预留售后/退款契约与数据库事实之间的可执行性缺口：收紧 Store 资源版本、限流、禁止缓存和输入闭合性；将普通退款与失败重试统一为 preview-confirm；用 `0005` 保护退款来源包络、头明细金额和单活动/单成功 attempt 事实。B12.0 只允许治理、契约、迁移及其门禁，不允许创建 B12 repository、controller/service、Worker handler 或工程前端业务代码。

## 2. 治理决策

### 2.1 CH-026 已批准变更

| 项目 | 用户确认 |
|---|---|
| 变更来源 | D：技术限制 |
| 变更类型 | B：修改需求 |
| 影响范围 | A/B/C/D/E/F：功能范围、开发进度、技术架构、测试范围、用户体验、数据迁移 |
| 影响程度 | C：重大 |
| 审批级别 | C：变更控制委员会（CCB） |
| 审批结果 | A：批准变更 |
| 执行时机 | A：立即执行 |

CH-026 将产品/PRD 基线升级为 `v2.4.10 / CH-026`，OpenAPI 升级为 `2.4.10-ch026`。该批准授权 B12.0 修订文档、OpenAPI、generated contracts、契约门禁和前向迁移 `0005_b12_aftersale_refund_guards`；它不授权真实微信退款、staging、production 或真实客户数据。

### 2.2 CH-027 单人 development reviewer 例外

CH-025 已在 B11.5 失效，不得沿用到 B12。CH-027 已按以下决策批准，仅补偿当前一名维护者在 B12.1-B12.6 脱敏 development 无法配置第二 reviewer 的团队资源约束：

| 项目 | 用户确认 |
|---|---|
| 变更来源 | 团队资源约束 |
| 变更类型 | 修改交付门禁 |
| 影响范围 | 开发进度、测试范围 |
| 影响程度 | 中等 |
| 审批级别 | 产品负责人 + 技术负责人 |
| 审批结果 | 批准变更 |
| 执行时机 | 立即执行 |

CH-027 不虚构第二 reviewer，也不降低 CH-026 契约、权限、隐私、退款、库存、佣金、幂等、并发或验收标准。B12.1-B12.6 必须严格串行，每批完成自动化、故障/并发和无残留验证，在 `P0=0/P1=0` 后暂停；B12.6 最终实现必须取得同 SHA 普通 CI 与 Supabase development rollback-only smoke 双绿。CH-027 只在 B12.6 最终同 SHA 双绿后自动失效，不得沿用到后续 development，且从不适用于 staging、production、真实客户、真实支付/退款、真实资金或真实物流。首次 staging 仍须外部独立复核。

## 3. 交付范围与边界

### 3.1 B12 交付

- CUSTOMER 创建本人 `REFUND_ONLY | RETURN_REFUND` 售后，查看列表/详情，在允许阶段取消，并在退货退款通过后填写退货物流。
- SUPER_ADMIN 查看售后列表/详情，执行初审通过、初审拒绝、退货实收验货、验货异常继续退款或验货后拒绝。
- 普通退款创建、Mock Provider 执行、失败重试和最终状态收敛；同一售后只保留一条稳定 refund 生命周期，重试只追加 attempt。
- 未发货退款的 `REFUND_RESTOCK`，退货验货的 `RETURN_RESTOCK`，订单/订单项成功退款净额，退款进度/处理双状态轴，以及基于支付时快照的佣金冲正。
- SUPER_ADMIN 通过独立的 `AMOUNT_COMPENSATION` preview-confirm 创建纯金额补偿，只占金额、不占数量、不回库，仍在退款成功后按原订单项佣金快照冲正。
- MP-13/14 与 ADM-12/13/16 工程交互，包含服务端试算、总部退货地址配置、禁止缓存的退货地址快照、退货物流、验货证据、异常处置和退款失败进度。

### 3.2 明确排除

- 真实微信退款验签、平台证书轮换、Provider ACK、真实商户退款号和真实资金链路。
- 客服任意输入金额、人工直接改订单/退款/库存/佣金状态、绕过 preview-confirm 或修改历史快照。
- 换货、维修、补发、多次退货包裹、第三方退货物流轨迹、自动验货、跨仓回库和售后升级工单。
- Agent 售后写操作。代理只能通过已冻结的订单/佣金投影查看售后摘要与 `REFUND_DEBIT`，不得审核、验货、退款或修改库存。
- 真实客户数据、staging、production 和任何对外发布结论。

## 4. CH-026 契约基线

### 4.1 统计与操作面

CH-026 不增加 path 或 operationId，目标保持：

- `173 paths`
- `198 operations`
- `198 unique operationId`
- `326 schemas`
- `706 schema refs / 2,726 local refs`
- `0 dangling refs`

既有 18 个 Store/Admin 售后与退款 operation 的 path 和 operationId 保持不变；既有 2 个 Admin 纯金额补偿 operation 继续使用高风险 preview-confirm。上述统计已由 CH-026 解析脚本实测固化；Redocly 为 0 warning。`CreateAftersaleRequest` 与 `ReturnInspectionRequest` 的生成类型均保持真实判别联合，不再退化为 `& unknown`。

### 4.2 Store 售后契约

| 方法 | 路径 | CH-026 约束 |
|---|---|---|
| `POST` | `/store/aftersales` | 同一 operation 使用闭合 `PREVIEW -> CONFIRM` 两阶段命令；只允许 `REFUND_ONLY | RETURN_REFUND`，订单、订单项、文件均为 ULID；PREVIEW 返回服务端试算和短时凭证但不占用，CONFIRM 重验后计价并占用；两阶段使用不同幂等键和 `HASH_ONLY` |
| `GET` | `/store/aftersales` | 本人隔离、稳定分页、闭合状态/动作与退款进度 |
| `GET` | `/store/aftersales/{aftersale_id}` | 本人详情、冻结地址/物流/验货/退款尝试和稳定时间线；跨客户统一 404 |
| `POST` | `/store/aftersales/{aftersale_id}/cancel` | 仅 `PENDING_REVIEW` 或未提交退货物流的 `WAITING_RETURN`；`Idempotency-Key + If-Match + HASH_ONLY` |
| `POST` | `/store/aftersales/{aftersale_id}/return-shipment` | 仅本人 `WAITING_RETURN`；`carrier_code` 为 1-40 位 ASCII 字母/数字及 `._-`，`carrier_name` 为 1-80 字符且无控制字符，`tracking_no` 为 1-120 位 ASCII 字母/数字及 `._/-`；`Idempotency-Key + If-Match + HASH_ONLY` |

五个 Store 接口统一使用 CUSTOMER 鉴权和已有 CUSTOMER+来源 IP `120/60` Redis 固定窗口限流；Redis 只保存域隔离 HMAC 摘要，故障时 fail-closed。全部成功和 JSON 错误响应都必须返回 `Cache-Control: no-store, private` 与 `Pragma: no-cache`。

`CreateAftersaleRequest` 是以 `action` 判别的闭合联合。`PREVIEW` 和 `CONFIRM` 携带完全相同的业务字段；`CONFIRM` 额外必填 `preview_token` 与 64 位小写十六进制 `confirmation_hash`。`items` 固定为 1-100 个不重复订单项，数量为正整数且不超过剩余可退数量；`evidence_file_ids` 最多 9 个不重复 ULID，服务端必须校验 READY/PRIVATE、purpose、当前客户和业务引用边界。`reason_code=OTHER` 在生成类型和运行时都强制非空 `reason_text`，客户端不得提交退款金额、库存处置、佣金影响或内部状态。

PREVIEW 使用 Repeatable Read 试算并返回逐项申请数量、剩余可退数量/金额、服务端分配金额、总申请金额和闭合 blockers：`ORDER_NOT_ELIGIBLE | ITEM_UNAVAILABLE | AFTERSALE_QUOTA_EXCEEDED | EVIDENCE_UNAVAILABLE`。`can_submit=false` 仍返回 200，但 token/hash/expiry 必须全部为 `null`；可提交时使用现有幂等密钥环经 HKDF 域 `qingxu:store-aftersale-preview:v1` 派生密钥，签发 5 分钟无状态 HMAC，绑定 CUSTOMER、session、规范请求、订单/订单项版本、退款与售后占用、证据事实、服务端金额和 expiry。CONFIRM 必须使用不同于 PREVIEW 的新幂等键，并在 Serializable 事务内重验全部绑定事实；过期、不匹配或事实漂移分别返回 `AFTERSALE_PREVIEW_EXPIRED | AFTERSALE_PREVIEW_MISMATCH | AFTERSALE_REQUOTE_REQUIRED` 409。消费者 `available_actions` 不包含 `RETRY_REFUND`，退款失败只可查看进度，由总部受控重试。

本人售后列表固定按 `created_at DESC,aftersale_id DESC` 分页。每项必须返回闭合 `available_actions`、`refund_progress_status=NONE|PARTIAL|FULL` 和 `refund_processing_status=IDLE|REFUNDING|FAILED`，客户端不得从售后主状态自行合成动作或退款结论。

订单列表和详情同步解除旧 B9 的“售后固定为空”限制。`APPLY_AFTERSALE` 的闭合矩阵为：`payment_status=PAID`、`payment_resolution=NORMAL`，订单为 `PENDING_SHIPMENT|SHIPPING` 时允许 `aftersale_expires_at=null`，订单为 `COMPLETED` 时必须具有非空期限且数据库当前时间不晚于该期限，`CLOSED` 永不允许；同时至少一个订单项的剩余可退数量和金额都大于 0。活动售后只通过逐项占用减少剩余额度，不得在仍有其他可退项时全局隐藏动作。详情 `aftersales` 返回本人关联售后摘要，`refund_attempts` 可返回普通退款和金额补偿的安全尝试投影。客户端不得按状态、时间或数量自行推导申请资格，也不得看到 Provider 原文或 Admin 专属动作。

### 4.3 Admin 售后、验货与退款契约

| 操作组 | 路径 | 关键约束 |
|---|---|---|
| 查询 | `GET /admin/aftersales`、`GET /admin/aftersales/{aftersale_id}` | SUPER_ADMIN 范围；稳定分页/时间线；返回占用、冻结快照、验货、退款、库存和佣金最小经营投影 |
| 初审通过 | `POST /admin/aftersales/{aftersale_id}/approve` | 只推进售后状态，不直接调用退款；`RETURN_REFUND` 在同一事务锁定唯一当前 PUBLISHED 退货地址并加密冻结快照 |
| 初审拒绝 | `reject-preview`、`reject` | HR-08；原因必填；preview 返回占用释放影响，confirm 使用新幂等键、`If-Match`、token/hash |
| 退货验货 | `POST /admin/aftersales/{aftersale_id}/return-inspections` | 一次精确覆盖全部售后项，封存 `PASS | ABNORMAL`、逐项数量/处置和证据清单，提交后不可改写 |
| 验货异常 | `continue-refund`、`return-resolution/reject-preview`、`return-resolution/reject` | 只允许一次 `CONTINUE_REFUND | REJECT_AFTER_RETURN`；继续退款原因必填；拒绝必须 preview-confirm；不允许重传已封存证据/处置 |
| 普通退款 | `refund-preview`、`refunds` | 服务端按冻结明细计价；preview 只返回影响、短时 token/hash；confirm 创建稳定 refund 和第一个 attempt |
| 失败重试 | `POST /admin/refunds/{refund_id}/retry-preview`、`retry` | 覆盖普通售后退款与金额补偿的失败待处理事实；新 preview、新 token/hash、新幂等键；复用原 `refund_id/refund_no` 与来源明细并只追加 attempt，响应按 `AFTERSALE | MANUAL_COMPENSATION` 来源闭合 |

Admin 写操作使用 ULID、`Idempotency-Key`、对已有资源使用 `If-Match`、`HASH_ONLY`和最小审计。高风险 preview 只存 token 哈希并绑定 actor、session、action、target、规范请求与资源版本；不缓存预览正文、退款 Provider 能力或退货地址明文。409 后必须刷新并重新预览，不得自动覆盖或复用旧 token。

### 4.4 纯金额补偿

`POST /admin/orders/{order_id}/manual-compensations/preview` 和 `POST /admin/orders/{order_id}/manual-compensations` 继续使用 HR-09 preview-confirm。请求必须指向单一订单项，由服务端检查：

```text
amount <= line_paid_amount - refunded_amount - aftersale_reserved_amount
```

确认只增加金额占用，不改变数量占用、验货或回库事实，并返回稳定 `refund_id/refund_no` 与 `origin_type=MANUAL_COMPENSATION`。成功退款后才更新净退款额和佣金；Provider 失败保留占用并进入统一 `/admin/refunds/{refund_id}/retry-preview -> retry` 受控重试，不允许另建补偿单绕过。迟到支付退款不进入该通用重试，继续只走 ADM-10 对账。

### 4.5 闭合错误与安全

CH-026 新增/固化 422：

- `AFTERSALE_QUOTA_EXCEEDED`：请求数量或服务端计算金额超过订单项剩余可退范围。
- `RETURN_ADDRESS_NOT_CONFIGURED`：`RETURN_REFUND` 初审时不存在唯一当前 PUBLISHED 总部退货地址。

CH-026 同时闭合 Store 售后预览 409：`AFTERSALE_PREVIEW_EXPIRED`、`AFTERSALE_PREVIEW_MISMATCH`、`AFTERSALE_REQUOTE_REQUIRED`。Admin 普通退款和金额补偿的额度失败必须在响应 schema 中将 `AFTERSALE_QUOTA_EXCEEDED` 固化为真实 wire code，不得只写在描述中仍引用任意字符串错误。

版本冲突、非法状态、preview 过期/篡改/重放、退款结果冲突和 Provider 不可用继续使用已冻结通用错误；不将内部异常、Provider payload、地址明文或证据存储键暴露给客户端。

## 5. 售后、验货与退款状态机

### 5.1 申请和占用

创建售后必须在 Serializable 事务内按订单项锁定后重算：

```text
remaining_qty = quantity - refunded_qty - aftersale_reserved_qty
remaining_amount = line_paid_amount - refunded_amount - aftersale_reserved_amount
```

服务端按成交快照为请求数量分配金额，尾笔退清直接使用剩余可退金额消除舍入尾差。同一订单项的并发申请只能在剩余数量/金额内成功，不得依赖前端顺序。成功创建后进入 `PENDING_REVIEW`，任一活动占用继续阻断 B11 整单发货。

### 5.2 初审、退货和验货

```text
PENDING_REVIEW
  -> REJECTED
  -> REFUNDING                         (REFUND_ONLY 通过)
  -> WAITING_RETURN                    (RETURN_REFUND 通过并冻结地址)

WAITING_RETURN
  -> WAITING_RECEIPT                   (客户提交退货物流)
  -> CANCELLED                         (未提交物流时允许)

WAITING_RECEIPT
  -> REFUNDING_AFTER_RETURN            (PASS)
  -> RETURN_EXCEPTION                  (ABNORMAL)

RETURN_EXCEPTION
  -> REFUNDING_AFTER_RETURN            (CONTINUE_REFUND)
  -> REJECTED_AFTER_RETURN             (REJECT_AFTER_RETURN)
```

验货请求必须精确覆盖该售后全部项，且每个订单项恰好一次。逐项必须满足：

```text
approved_refund_qty + return_to_customer_qty = received_qty
approved_refund_qty = restock_qty + damaged_qty + scrap_qty
```

`PASS` 要求全量实收、全量批准且 `return_to_customer_qty=0`，可以封存空证据清单；少件、0 件或任何数量异常必须为 `ABNORMAL`，并封存至少一份合法证据。验货 `evidence_file_ids` 同样最多 9 个不重复 ULID。验货结果、逐项处置、处理人、时间和 canonical 证据清单提交后均不可追加、删除、换序、替换或改绑。

### 5.3 退款处理

售后或金额补偿经 preview-confirm 后创建唯一稳定 `refund`和第一条 `refund_attempt`。Provider 调用必须在数据库事务外执行；回传结果先保存到 Inbox，Worker 再以 Serializable 事务收敛业务事实。development/test 只使用 Mock Provider，真实微信退款 callback 不注册。

```text
REFUNDING | REFUNDING_AFTER_RETURN
  -> COMPLETED                         (Provider 明确成功)
  -> REFUND_FAILED                     (Provider 明确失败)

REFUND_FAILED
  -> REFUND_FAILED                     (重试失败，追加 attempt)
  -> COMPLETED                         (重试成功)
```

Provider 失败或未知不得更新成功退款数量/金额、不得回库、不得冲正佣金，且必须保留可退占用。重试使用新幂等键与新 preview，复用稳定 `refund_id/refund_no/provider_refund_id/items`，不得创建第二条 refund。

### 5.4 退款成功的原子收敛

退款成功事务必须精确一次：

1. 固化成功 attempt 与 refund，更新售后/补偿和订单项成功退款数量、金额及占用。
2. 重算 `refund_progress_status=NONE|PARTIAL|FULL` 和 `refund_processing_status=IDLE`；历史成功进度不得被后续失败回退。
3. 未发货数量按 SKU 写唯一 `REFUND_RESTOCK` 流水并增加实物库存；已发货退货只按验货冻结的 `restock_qty` 写 `RETURN_RESTOCK`，不根据退款金额或申请数量自动回库。
4. 按原订单项支付时佣金快照逐项计算冲正。完成前追加 `EXPECTED_REDUCED/EXPECTED_CANCELLED`；完成后保留原 `AVAILABLE/AVAILABLE_CREDIT` 并追加 `REFUND_DEBIT`。
5. 未发货全额退款收口为 `CLOSED/FULL_REFUND_BEFORE_SHIPMENT`且履约为 `CANCELLED`；已发货全额退款保留包裹/物流事实，收口为 `COMPLETED/FULL_REFUND_AFTER_SHIPMENT`。
6. 写入最小审计、Outbox 和幂等完成事实；不得在任何载体中保存 Provider payload、完整地址、客户标识或预览 token。

订单完成与退款成功并发时必须锁定相同订单项佣金快照/position：退款先提交则只结转减少后的剩余 EXPECTED；完成先提交则追加 `REFUND_DEBIT`。两种顺序必须得到相同净佣金，且不得重复流水或扩大负余额。

## 6. `0005_b12_aftersale_refund_guards`

### 6.1 迁移目标

`0005` 为事务型前向数据防护迁移，不新增表、列或枚举。两份 Prisma schema 的数据模型形状不变，两份 `0001_initial`与既有 `0002`至`0004` 必须逐字节不变；`prisma/migrations` 和产品材料镜像中的两份 `0005` 必须逐字节一致。

`0005` 包含：

1. 更正 `enforce_refund_item_return_limit()`，保持 `SECURITY INVOKER`，移除对不可变 `return_inspection/return_inspection_item` 的不必要 `FOR SHARE`，避免 `mall_runtime` 因无 UPDATE 权限无法执行合法退款。
2. 增加部分唯一索引，保证每个 refund 同时最多一个 `INITIATED|PROCESSING` attempt，且整个生命周期最多一个 `SUCCEEDED` attempt。
3. 增加可延迟约束触发器，校验 refund、order、aftersale、customer、order_item 的来源包络，并要求 manual compensation 精确绑定其唯一 `quantity=1` 订单项资金归属明细；校验 `refund.amount = SUM(refund_item.amount)`，同时兼容在同一事务内先写头再写明细。退款项写入先以 `FOR UPDATE` 串行父 refund，延迟校验对父 ID 去重升序锁定。
4. 迁移以 5 秒 `lock_timeout` 获取最小来源/退款表集合的 `SHARE` 锁，再执行历史活动/成功 attempt 重复、来源包络跨主体、金额补偿跨订单项和头明细金额一致性预检；任一异常使整笔迁移失败，不得自动修改历史资金事实。远端 deploy 脚本在调用 Prisma 前执行同形预检，避免已知数据错误留下失败 migration history。

空库回放与 `verify.sql` 实测为 22 个 partial indexes、26 个用户触发器、50 个事件绑定、16 个自有/runtime 函数；迁移历史精确为 5 条。双份 0005 SHA-256 均为 `95f362667bdc6a0b751ae636d91a139a71a3f40155ba764937db01d5bbce412b`。

### 6.2 数据库与远程门禁

B12.0 必须完成：

- 空库从 `0001 -> 0002 -> 0003 -> 0004 -> 0005` 完整回放，并验证五条迁移历史的名称、checksum 和 owner；回放还必须覆盖金额补偿合法/跨订单项路径和两个真实 `mall_runtime` 连接争用同一 refund 父行的串行结果。
- 既有 `0001`至`0004` 冻结哈希不变；两份 `0005` 逐字节一致；Prisma validate、权限、函数指纹、触发器和索引形状通过。
- 使用与 remote migration history 相同的基线执行 `prisma migrate diff`，最终为 `migration diff=0`。
- 在同一精确 B12.0 SHA 上依次取得普通 CI、手动 Supabase development migration、Supabase development rollback-only smoke 三项 `completed/success`；后两者必须使用受控 development environment、精确 `target_sha`、项目范围和数据库 concurrency group。
- migration 成功后的代码回退保留加法索引、函数和触发器，不执行 development 数据库降级。

上述门禁已由 SHA `03a1e9f98bea0926887fb68354ca602566148f6a` 的普通 CI Run `33462227180`、Supabase development migration Run `33464158444` 和 rollback-only smoke Run `33464267703` 依次闭合。`0005` 已保留在 development 迁移历史中，B12.0 数据库与远程门禁完成。

## 7. 事务、Provider 与锁序

### 7.1 固定事务边界

- 普通只读列表/详情及 repository 内部只读 helper 使用 Repeatable Read 快照并批量读取，不使用 N+1 查询或应用内分页后过滤；Admin 退款/金额补偿公开 preview 由 API 服务显式包裹在 Serializable 事务中，repository helper 的 Repeatable Read 默认值不代表公开路由边界。
- 占用、释放、审核、验货、异常解决、退款 confirm/结果收敛和补偿 confirm 使用 Serializable 与统一重试，所有状态、主体、版本、数量和金额在事务内重验。
- Provider `refund/query` 只在事务外执行。超时、网络断开、结果未知和 Redis 异常均 fail-closed，不得推定退款成功或发起第二条 refund。
- Inbox 消费、退款 attempt、库存流水、佣金流水、审计、Outbox 和幂等事实必须具备稳定业务键，Worker 在业务提交后、Inbox 标记完成前崩溃也只能重放收敛，不能重复回库、退款或冲正。

### 7.2 固定锁序

退款/金额补偿 confirm 的实际顺序固定为：

```text
request idempotency claim -> read source candidate header
-> actor account FOR UPDATE
-> sales_order FOR UPDATE -> order_item.id ASC
-> aftersale.id ASC -> aftersale_item.id ASC -> manual_compensation.id ASC
-> refund.id ASC -> refund_attempt.id ASC
-> payment_intent.id ASC -> payment_attempt.id ASC
-> product.id ASC -> sku.id ASC -> inventory_balance.id ASC
-> commission_position by snapshot_id/id ASC
-> agent advisory locks by agent_id ASC -> agent_wallet.agent_id ASC
-> build/reverify preview -> verify/consume high-risk preview
-> domain writes -> audit/outbox
```

候选 ID 只可在无锁查询中定位，事务内必须重读并锁定；有序集合均按上述稳定键排序。公开 preview 与 confirm 都使用 Serializable，但 preview 只读且不取得上述业务行锁；confirm 在完整包络锁定后才重算预览并验证/消费高风险 preview。完整幂等重放先于 `If-Match` 检查，但每次重放都要重新鉴权并返回当前投影；无法安全重放时返回 409 并要求刷新。

## 8. 实施批次

| 批次 | 交付范围 | 退出条件 | 当前状态 |
|---|---|---|---|
| B12.0 | CH-026 治理；产品/API/架构/数据库/风险/原型说明；OpenAPI/contracts；`0005` 与迁移链门禁 | 契约、Redocly、生成漂移、冻结数据库、空库回放、Prisma、`migration diff=0`、普通 CI→development migration→rollback-only smoke 在同 SHA 全部通过，`P0=0/P1=0` | **已完成；SHA `03a1e9f98bea0926887fb68354ca602566148f6a` 的 CI Run `33462227180`、migration Run `33464158444`、smoke Run `33464267703` 三项成功** |
| B12.1 | Store 售后 repository/module；PREVIEW/CONFIRM、列表、详情、取消和额度占用 | 服务端试算/凭证绑定、篡改/过期/漂移、不同幂等键、并发占用、本人 404、证据权限、限流/no-store、列表排序/双轴、版本和发货阻断通过，`P0=0/P1=0` | **已完成并暂停；全仓 `2455 passed`，隔离 PostgreSQL full `5/5`，Supabase rollback-only `1/1`，复审 `P0=0/P1=0`** |
| B12.2 | Admin 列表/详情、初审通过/拒绝；Store 退货物流；退货地址快照 | 唯一 PUBLISHED 地址、快照加密/AAD、preview-confirm、占用释放、无 PII 泄露与竞态通过，`P0=0/P1=0` | **已完成并暂停；全仓 `2530 passed / 151 skips`，隔离 PostgreSQL full `8/8`，Supabase rollback-only `1/1`，复审 `P0=0/P1=0/P2=0`** |
| B12.3 | 实收验货、PASS/ABNORMAL 证据封存、继续退款/验货后拒绝 | 精确覆盖、数量等式、空/非空证据、不可变事实、只解决一次、版本/并发通过，`P0=0/P1=0` | **已完成并暂停；全仓 `2567 passed / 158 skips`，隔离 PostgreSQL full `51/51`，Supabase rollback-only `6/6`，真实文件 API/Worker `6/6`，复审 `P0=0/P1=0`** |
| B12.4 | 普通退款 preview-confirm、Mock Provider/Inbox/Worker、失败重试、金额补偿、库存/订单/佣金收敛 | 创建丢响应、重复/乱序结果、单活动/单成功 attempt、回库和佣金精确一次、完成/退款竞态通过，`P0=0/P1=0` | **已完成；当批基线 B12 full DB `64 passed`，B12.6 补测后为 `65 passed`；锁序文字与零批准多行真实 DB 覆盖均已补齐，复审 `P0=0/P1=0/P2=0`** |
| B12.5 | MP-13/14 和 ADM-12/13/16；创建/详情、初审、总部退货地址、退货物流、验货、异常解决、退款/重试/金额补偿 | generated contracts、严格解码、服务端动作权威、五视口、敏感持久化扫描和完整错误/慢请求/丢包通过，`P0=0/P1=0` | **已完成并暂停；miniapp `21 passed / 29 designed skips`、admin `24 passed / 21 designed skips`，合计 `45 passed / 50 designed skips`，全仓 `2735 passed / 165 designed or environment skips`，复审 `P0=0/P1=0`；未登记 B12 最终远端双绿** |
| B12.6 | DB/API/前端/真实纵向总验收与 Supabase rollback-only repository smoke | 最终同 SHA 普通 CI + Supabase development rollback-only smoke 双绿，`0005` 已在 development 历史，全部 fixture/Redis/对象无残留，`P0=0/P1=0` | **本地实现与本地验收已完成；B12 full DB `65/65`、两端 E2E `45 passed`、全仓单测 `2738 passed / 166 skips`、完整 `RETURN_REFUND` 真实纵向 `1/1` 及全资源精确清理通过；最终同 SHA 远端双绿待完成** |

每批必须在验收后暂停，只有当前批次 `P0=0/P1=0` 且下一批次的治理门禁完整时才可继续。B12.0 通过不等于 B12 development `GO`；只有 B12.6 完成并取得最终同 SHA 双绿后才能标记 `GO`。

## 9. 测试与验收计划

### 9.1 契约与数据库

- OpenAPI/Redocly、闭合 Schema、ULID、Store 限流/no-store、输入上限/唯一性、`PREVIEW|CONFIRM` 判别联合、200/201 响应、阻断预览空凭证、闭合 reason/action/error、Admin preview-confirm、来源判别 retry、生成漂移和操作统计。
- 空库完整回放、既有库 `0004 -> 0005` 升级、历史异常预检、迁移原子失败、部分唯一索引、包络/金额可延迟触发器、权限、函数指纹和 `migration diff=0`。
- 使用真实 `mall_runtime` 验证合法 refund item 可写、跨订单/客户/售后的包络被拒绝，且 runtime 仍不能修改不可变验货事实。

### 9.2 领域与并发

- 售后 PREVIEW 的服务端计价、尾差、blockers、凭证篡改/过期/跨会话/请求漂移和响应丢失重试；CONFIRM 必须使用新幂等键并锁内重验。覆盖数量/金额超占、两个并发申请、活动占用与整单发货竞态，取消/拒绝只释放未消耗占用。
- 退货地址缺失、版本变更后快照不回写，地址 AAD/密文篡改/密钥轮换，明文不进入日志、审计、Outbox、幂等缓存或截图。
- PASS/ABNORMAL 全量矩阵，少件/0 件，数量等式，空/非空证据清单，证据交叉售后/非法 purpose，验货与上传/异常解决并发。
- Provider 创建成功但响应丢失、失败、超时、未知、重复/乱序结果、Worker 崩溃重放、重试与原结果竞态、单活动/单成功 attempt；普通售后和金额补偿失败均由稳定 refund 进入统一 retry，迟到支付退款不得进入该分支。
- 未发货部分/全额回库、已发货逐项验货回库、金额补偿绝不回库，库存流水唯一且始终满足库存不变量。
- 完成前部分/全额退款佣金减少/取消，完成后 `REFUND_DEBIT`，多 SKU 逐项舍入/尾笔还原，订单完成与退款并发的净结果一致。

### 9.3 前端与纵向

- 375/390/414/1024/1440 五视口，覆盖 loading、empty、401、403、404、409、422、429、500、慢请求、重复点击、响应丢失、证据上传失败、退款处理中/失败与网络恢复。
- 页面只使用 generated contracts 和严格响应解码器；金额、状态、可用动作、回库和佣金影响均以服务端投影为准，客户端不推导成功。
- 真实纵向覆盖浏览器 -> Nest -> PostgreSQL/Redis/MinIO -> Worker -> Mock Provider，并精确清理售后、证据引用、退货物流/地址快照、验货、refund/attempt、库存/佣金流水、钱包、审计、幂等、Inbox/Outbox、Redis key 和测试对象。

## 10. 风险、回退与发布边界

- B12.1 前可整体回退 CH-026 文档、OpenAPI、generated contracts 和静态原型说明。`0005` 一旦部署到 development，作为加法安全防护保留，不执行数据库降级。
- B12.1 以后通过关闭 Store/Admin 售后、验货、普通退款、金额补偿路由，停用 Worker 普通退款处理器和隐藏 MP/ADM 导航回退；不修改或删除已存资金、库存、佣金和审计事实。
- Mock Provider 仅允许 development/test；production 配置为 Mock 必须拒绝启动。未完成真实微信退款验签、证书、原始请求体、ACK、对账和故障演练前，不得注册真实 callback 或开启资金链路。
- B12 development 最终只能标记 `GO`，不等于 staging/production 准入。首次 staging 仍必须由外部独立人员复核代码、安全、数据库、PII、退款、库存、佣金和全部验收证据。

## 11. B12.0-B12.6 当前退出检查

| 门禁 | 当前结论 |
|---|---|
| CH-026 决策 | **已批准：D / B / A,B,C,D,E,F / C / C / A / A** |
| 产品/API 基线 | **目标已确定：`v2.4.10 / 2.4.10-ch026`** |
| OpenAPI/contracts | **本地统计、Redocly、闭合 Schema、确定性生成与漂移检查已通过：`173/198/198/326/706/2,726/0`；精确 SHA 普通 CI 已通过** |
| `0005` | **双份字节一致、空库完整回放、迁移原子失败、权限、运行时包络/并发、冻结哈希、原生指纹和 `migration diff=0` 均通过；development migration 与 rollback-only smoke 已成功** |
| 远程证据 | **SHA `03a1e9f98bea0926887fb68354ca602566148f6a` 的 CI Run `33462227180`、migration Run `33464158444`、smoke Run `33464267703` 三项均为 `completed/success`** |
| CH-027/独立 reviewer | **CH-027 已由产品负责人和技术负责人批准并立即执行，仅补偿 B12.1-B12.6 脱敏 development 第二 reviewer 缺失，B12.6 最终同 SHA 双绿后自动失效** |
| B12.1 实现 | **Store 售后 PREVIEW/CONFIRM、本人查询/取消、额度占用、订单投影、CUSTOMER 证据文件权限与角色感知限流已完成；该批退出范围仅含 B12.1 后端，后续能力的当前状态见 B12.2-B12.5 行** |
| B12.1 自动化 | **全仓 `2455 passed / 148 environment or designed skips`；API DTO/路由 `58/58`、文件权限/限流 `39/39`、repository `15/15`；临时 PostgreSQL 18.3 完整回放 `0001-0005` 后 full `5/5`，含四类真实双连接竞态；Supabase development rollback-only `1/1`，临时容器和 fixture 无残留** |
| B12.1 静态门禁 | **lint `0 errors`、typecheck、build、Redocly、生成漂移、敏感扫描、冻结数据库、Prisma validate 与 `migration diff=0` 全部通过；当前契约为 `173/198/198/326/706/2,726/0`** |
| B12.1 复审 | **最初发现的售后单号长度 500、CUSTOMER 文件限流缺失和 `aftersale_item` 锁序风险均已修复；同毫秒审计时间线按资源版本恢复因果顺序；最终 `P0=0/P1=0`** |
| B12.2 实现 | **Admin 售后列表/详情、初审通过、拒绝 preview-confirm、总部退货地址版本发布与 Store 退货物流已完成；退货地址版本/快照加密和历史重放闭合；该批退出时尚未开始 B12.3** |
| B12.2 自动化 | **全仓 `2530 passed / 151 environment or designed skips`；database `677/104`、API `1231/44`、platform-core `139/0`；临时 PostgreSQL 18.3 完整回放 `0001-0005` 后 full `8/8`，Supabase development rollback-only `1/1`，临时容器与 fixture 无残留** |
| B12.2 静态门禁 | **lint `0 errors`、typecheck、test、build、MP-Weixin build、Redocly、确定性生成、敏感扫描、冻结数据库、Prisma validate 与 `migration diff=0` 全部通过；当前契约为 `173/198/198/326/706/2,726/0`，无 schema/migration 改动** |
| B12.2 复审 | **preview/confirm 幂等键隔离、首次地址版本、准确历史地址重放、完整当前验货投影、单字符可选审批备注及申请证据投影均已闭合；Admin 成功响应 no-store 契约和两类真实双连接竞态已补齐；地址/物流不进入审计或幂等正文；最终 `P0=0/P1=0/P2=0`** |
| B12.3 实现 | **实收验货、`PASS | ABNORMAL` 判别、canonical 证据 manifest、继续退款、退货后拒绝 preview-confirm、当前投影和 SUPER_ADMIN 证据下载均已完成；B12.3 不创建退款、不调用 Provider、不写库存或佣金** |
| B12.3 自动化 | **全仓 `2567 passed / 158 environment or designed skips`；database `692/111`、API `1253/44`、platform-core `139/0`；临时 PostgreSQL 18.3 完整回放 `0001-0005` 后 full `51/51`，其中 Admin return 7 个真实 DB 场景全部执行；Supabase development rollback-only `6/6`，真实文件 API/Worker `6/6`，临时容器、fixture、Redis 和对象无残留** |
| B12.3 静态门禁 | **lint `0 errors`、typecheck、test、build、MP-Weixin build、Redocly、确定性生成、敏感扫描、冻结数据库、Prisma validate 与 `migration diff=0` 全部通过；契约为 `173/198/198/326/706/2,726/0`，Prisma schema 与迁移逐字节不变** |
| B12.3 复审 | **已修复 SUPER_ADMIN 本人文件授权回归、无数量异常伪装 ABNORMAL、manifest 测试假绿和 Store 验货证据包络；真实 DB 覆盖 PASS/ABNORMAL、零件/少件、多项精确覆盖、不可变、非法 purpose、确定性拒绝、上传/验货和双 resolution 竞态；最终 `P0=0/P1=0`。当批保留的跨所有者已绑定证据真实下载正向纵向 P2，已在 B12.6 通过消费者私有凭证上传与管理员受控下载闭合** |
| B12.4 实现 | **Admin 普通退款和金额补偿 preview-confirm、稳定 refund/追加 attempt 重试、Mock Provider、Inbox/Outbox 与 Worker 收敛已完成；成功事实原子闭合库存回库、净销量、订单退款双轴、全额退货后完成原因、佣金冲正与 AVAILABLE 钱包扣减，金额补偿不回库** |
| B12.4 可靠性 | **Provider 外调保持事务外；同 attempt 精确重放、新 attempt 隔离、历史 attempt 幂等键和旧执行事件均受保护；历史 attempt 的精确延迟回调可幂等吸收且冲突 fail-closed；创建丢响应、重复/乱序结果、Worker 提交后崩溃及超过通用重试上限时使用有界 durable retry 收敛，不重复回库、退款、冲正或扣钱包；非 Mock runtime 从 preview 起即 fail-closed** |
| B12.4 自动化 | **当批基线为 B12 full DB `6 files / 64 passed`、真实 integration `5/5`；B12.6 补充零批准多行用例后，本轮 B12 full DB `6 files / 65 passed`、admin-refund 真实 integration `6/6`，临时 PostgreSQL 精确清理通过。refund API `16/16`、Admin order `19/19`、fulfillment `6/6`、Worker `93 passed / 3 designed skips` 和全仓 `2629 passed / 165 skips` 为 B12.4 当批历史结果** |
| B12.4 静态门禁 | **lint `0 errors / 437 existing warnings`、typecheck、test、build、MP-Weixin build、Redocly、contracts 确定性生成且无漂移、敏感扫描、冻结数据库、Prisma validate 与 `migration diff=0` 均通过；契约保持 `173/198/198/326/706/2,726/0`，OpenAPI、generated contracts、Prisma schema、既有迁移和静态前端均未修改** |
| B12.4 复审 | **已闭合 `RETURN_DAMAGED` 误发 restocked 语义、历史 attempt 延迟回调、历史失败 attempt 误开放重试、旧 attempt/旧 Outbox 事件驱动新 attempt、Provider 成功重放冲突、非 Mock preview 未提前拒绝、部分退货按原申请而非批准数量退款、完成幂等键在全额退款后失效等风险；最终 `P0=0/P1=0/P2=0`。原 P2-1 已通过第 7 节准确记录公开 preview/confirm 的 Serializable 边界与 confirm 固定锁序闭合；原 P2-2 已补多行退货中一行批准 0 的真实 DB 用例，证明该行不生成 refund item、不回库、不改销量且不冲正佣金，本轮 integration `6/6` 通过** |
| B12.5 实现 | **MP-13/14 与 ADM-12/13/16 已完成，覆盖售后创建/详情、初审、总部退货地址、退货物流、验货、异常解决、退款/重试/金额补偿；generated contracts、严格响应解码器和服务端权威动作投影已接入** |
| B12.5 自动化 | **miniapp E2E `21 passed / 29 designed skips`、admin E2E `24 passed / 21 designed skips`，合计 `45 passed / 50 designed skips`；全仓单测 `2735 passed / 165 designed or environment skips`；Admin 文件服务 `24/24`，B3/B4/B5 历史上传回归分别为 `26/4`、`27/8`、`30/20`；lint `0 errors / 437 existing warnings`，全仓 typecheck/build、MP-Weixin build、敏感扫描、契约与冻结门禁均通过；375/390/414/1024/1440 五视口已建立，移动导航、409 配额 clamp、精确金额求和、对象存储拒绝时禁止 complete/验货提交和全部服务端授权动作矩阵已覆盖** |
| B12.5 契约/数据库 | **OpenAPI 保持 `173/198/198/326/706/2,726/0`；B12.5 未修改 OpenAPI、Prisma schema 或既有 `0001` 至 `0005` 迁移，数据库冻结继续有效** |
| B12.5 复审 | **已闭合替换登录覆盖旧请求、恢复 journal 前提前读取业务数据、确认响应丢失后刷新/重新认证生成新键、售后入口重复执行退款重试，以及 Admin 验货证据上传信任畸形 intent/complete 或接受私有证据公开 URL 等风险。消费者与总部 journal 均按账户隔离、最长 24 小时、仅保存加盐完整请求哈希；损坏或身份无法确认时 fail-closed，退款重试统一由订单详情恢复原命令。Admin 上传严格绑定 purpose/file/status/timestamp/MIME/SHA-256，PRIVATE purpose 强制 `public_url=null`。批次结论 `P0=0/P1=0/P2=1`；残余 P2 沿用 B3 已登记的 orphan 边界：用户移除凭证或放弃表单后，已完成但未绑定的 READY/PRIVATE 证据当前没有冻结删除契约，既有 Worker 只清理 PENDING staging。B12 仅使用脱敏 development fixture；首次 staging/真实数据前必须经变更流程补充“确认无业务引用后回收”的闭合策略。本地证据仅支持 B12.5 退出，未登记 B12 最终实现 SHA 的普通 CI 或 Supabase development rollback-only 成功** |
| B12.6 本地自动化 | **普通 CI 已接入 B12 真实浏览器→Nest→PostgreSQL/Redis/MinIO/Worker 纵向与精确清理。完整 `RETURN_REFUND` 纵向在全新 r3 隔离环境 `1/1` 通过（`25.98s`），覆盖消费者私有凭证上传、跨所有者管理员受控下载、退货地址快照、退货物流、验货、Callback Inbox `PROCESSED/signature_valid=true/retry_count=0/processed_at`、`RETURN_RESTOCK`、佣金和订单收敛；9 条精确审计多重集覆盖 file `CREATE/COMPLETE/READ_SENSITIVE`、aftersale `CREATE/APPROVE/UPDATE/RECORD_INSPECTION`、refund 管理员/Worker 两条 `REFUND`。Redis DB 15、唯一 bucket、11 类 DB 事实、连接和四端口均为 0，测试数据库、容器、网络、两卷和 bucket 精确清理。B12 full DB `6 files / 65 passed`、admin-refund integration `6/6`、audit `74/74`；miniapp `21 passed / 29 skips`、admin `24 passed / 21 skips`，合计 `45 passed`；全仓单测 `2738 passed / 166 skips`，lint `0 errors / 437 existing warnings`、typecheck、完整 build 与敏感扫描通过** |
| B12.6 待办 | **在最终同一实现 SHA 上取得普通 CI 与 Supabase development rollback-only smoke `completed/success`；核对 `head_sha/target_sha` 一致后才能标记 B12 development `GO` 并令 CH-027 自动失效** |
| 当前结论 | **B12.1-B12.6 本地实现与本地验收已完成，当前 `P0=0/P1=0/P2=1`，唯一 P2 为 B12.5 的未绑定 READY/PRIVATE orphan 回收边界。最终远端同 SHA 双绿尚未取得，B12 仍非 development `GO`，CH-027 继续有效且未扩大至 staging/production** |
