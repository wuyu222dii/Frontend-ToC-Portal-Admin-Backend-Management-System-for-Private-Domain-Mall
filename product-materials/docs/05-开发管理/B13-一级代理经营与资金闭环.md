# B13 一级代理经营与资金闭环

> 批次：B13；目标产品/API 基线：`v2.4.11 / CH-028`、OpenAPI `2.4.11-ch028`；更新日期：2026-09-03；当前状态：B13.0-B13.2 均已按顺序取得普通 CI、development migration 与 rollback-only smoke 同 SHA 三绿并退出；B13.3 本地候选实现与最终复审已完成，复审结论为 `P0=0/P1=0`，正在等待本批最终同 SHA 普通 CI -> development migration -> rollback-only smoke。B13.3 未退出，B13.4 未准入，B13 development 未标记 `GO`。仅允许 Mock Provider、虚构银行卡和脱敏 development；真实客户数据、真实微信身份、真实支付/退款、真实资金、staging/production：`NO-GO`。

## 1. 准入结论与前置事实

B12 已完成 development 准入。最终实现 SHA `8c3589afcf7bb0dd5a4b8711d418e4c61b1ad09c` 的[普通 CI Run `33592754575`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33592754575)与[Supabase development rollback-only smoke Run `33594513127`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33594513127)均为 `completed/success` 且 `head_sha` 一致。B12 development `GO`，CH-027 已自动失效。

B12 仍保留一个不阻断 development 的 `P2=1`：用户移除凭证或放弃表单后，未绑定的 `READY/PRIVATE` evidence orphan 没有冻结删除契约。该风险继续阻断 staging/真实数据；B13 不扩大许可，首次 staging 前必须另行变更并闭合“确认无业务引用后回收”策略。

B13 只允许在 B13.0 完成后进入 B13.1。每个后续批次都必须在当前批 `P0=0/P1=0` 后暂停，并取得下一批治理准入；不得把预埋 OpenAPI、Prisma 模型、静态原型或状态壳描述成已交付业务能力。

## 2. CH-028/CH-029 治理

### 2.1 CH-028 B13 契约与数据防护变更

| 项目 | 决策 |
|---|---|
| 变更来源 | 产品路线图未实现域与现有预埋契约之间的交付缺口 |
| 变更类型 | 新增一级代理经营、归属和资金闭环能力 |
| 影响范围 | 功能范围、开发进度、技术架构、测试范围、用户体验、数据迁移 |
| 影响程度 | 重大 |
| 审批级别 | 变更控制委员会（CCB） |
| 审批结果 | 已批准，立即执行 |
| 目标基线 | `v2.4.11 / CH-028`、OpenAPI `2.4.11-ch028` |

CH-028 允许在 B13.0 修订 B13 文档、OpenAPI/generated contracts、配置、契约门禁和前向 `0006_b13_agent_finance_guards`。`0006` 只可增加历史预检和数据库守卫，不得修改 `0001` 至 `0005`，不得借迁移删除或改写既有交易、佣金、钱包、审计和提现事实。CH-028 不授权真实微信、真实资金、staging 或 production。

### 2.2 CH-029 单维护者 development 例外

| 项目 | 决策 |
|---|---|
| 变更来源 | 团队资源约束 |
| 变更类型 | 修改 development 交付门禁的 reviewer 组织条件 |
| 影响范围 | 开发进度、测试范围 |
| 影响程度 | 中等 |
| 审批级别 | 产品负责人 + 技术负责人 |
| 审批结果 | 已批准，立即执行 |
| 适用范围 | B13.1-B13.9、Mock Provider、脱敏 development |

CH-029 不虚构第二 reviewer，也不降低契约、数据库、权限、隐私、资金、并发、幂等、审计、Outbox、敏感扫描、无残留或远端证据标准。每批必须严格串行并在 `P0=0/P1=0` 后暂停；B13.9 最终实现必须在同一 SHA 取得普通 CI 与 Supabase development rollback-only smoke 双绿。B13.9 完成后 CH-029 自动失效，不得沿用到下一阶段或 staging/production。首次 staging 仍须外部独立人员复核。

## 3. 范围与边界

### 3.1 本批覆盖

- AGT-01 登录/临时密码、AGT-02 看板、AGT-03 推广、AGT-04 客户、AGT-05 订单、AGT-06 佣金、AGT-07 钱包/提现、AGT-08 提现记录/详情、AGT-09 账户/安全。
- ADM-14/15 客户列表/详情/归属转移，ADM-17 代理管理，ADM-18/19 代理详情/编辑，ADM-20/21 提现审核，ADM-22 佣金规则，以及 ADM-16 最低提现设置。
- 一级代理生命周期、独立 Agent JWT realm、商品授权、邀请码和推广素材、当前客户/历史订单投影、统一佣金规则版本、钱包流水、银行卡密文与提现冻结/审核/支付、TOTP reauth、付款凭证和审计。

### 3.2 明确排除

- 多级代理、代理独立店铺、代理专属佣金、固定/阶梯/活动佣金、手续费、自动打款和微信自动提现。
- 代理改价、改库存、发货、售后审核、验货、退款、客户地址读取和任意订单写操作。
- 真实微信登录/支付/退款、真实银行卡、真实资金、真实客户数据、staging、production 和对外发布。

## 4. 契约、身份与数据约束

### 4.1 API 基线

- 保留现有 operationId 与 `173 paths / 198 operations` 的全局统计；B13.1 当前契约实测为 `173 paths / 198 operations / 198 unique operationId / 327 schemas / 716 schema refs / 2,746 local refs / 0 dangling refs`，Redocly 0 warning。B13 实现现有 Agent `21 paths / 23 operations` 及配套 Admin operation，不新增重复 operationId；完整 operation manifest SHA-256 保持 `128509f76f2e62ebe78cd0465205a94236f87f70904bf614664dd1b02548dc80`，generated contracts 两次生成稳定为 `2207286938dd174c0d6b36879407131253ef1ad4ffe6dc3e5726a83827852448`。B13.0 的 `2,734` local refs 与旧 generated hash 仅保留在 6.4 历史证据中。
- `/agent/**` 使用独立 Bearer realm：`aud=qingxu-agent-web`、`role=AGENT_ADMIN`、`assurance=PASSWORD`。Store、Admin、Agent token 必须交叉拒绝；临时会话固定 `CHANGE_PASSWORD_ONLY` 且无 refresh。
- B13.0 配置固定覆盖 `AGENT_AUTH_TOKEN_AUDIENCE`、Agent access/session TTL 与登录限流、`STORE_PROMOTION_PUBLIC_BASE_URL`、既有 `FIELD_ENCRYPTION_*` 字段加密环，以及用途隔离的 `BANK_ACCOUNT_HASH_*` 当前/历史 HMAC key ring；不同用途密钥不得复用。
- 新增专用 `AgentSessionResponse`、`RestrictedAgentSessionResponse`、`AgentCurrentResponse`；当前账号仅返回代理 ID、编号、名称、状态和授权模式，不返回秘密或未授权经营数据。
- 创建代理、重置密码、轮换邀请码的首次响应携带 `disclosure_state` 并只披露一次秘密；披露交接窗口从成功事务完成后起算。幂等重放返回脱敏资源和重新签发指引，不保存或再次披露秘密。
- `/agent/products` 删除可请求非 ACTIVE 状态的筛选参数，服务端固定只返回已上架且当前授权的 ACTIVE 商品；白名单只限制推广可见性，不污染历史客户绑定和已冻结佣金。

### 4.2 推广与归属

- 推广链接使用 `STORE_PROMOTION_PUBLIC_BASE_URL` 生成，邀请码须有效、未过期且属于 ACTIVE 代理；禁用代理不得创建新绑定。
- QR 由服务端生成，不经客户端 upload/complete；推广素材与 `READY/PRIVATE/PROMOTION_QR` 文件原子绑定，创建响应仅返回结构化文件事实，不直接返回签名 URL。下载必须通过唯一受控的 Agent Files 例外重新鉴权，且只允许当前 Agent 读取本人 `promotion_asset.qr_file_id` 绑定文件。撤权、下架或邀请码失效后，旧链接安全退化，不删除历史归属和佣金事实。
- 客户转移、转直营与支付归因均由服务端在锁内复核；当前代理只能读取当前绑定客户，历史已支付订单使用支付时归属快照。

### 4.3 佣金、钱包与提现

- 平台/分类/SKU 规则按 `SKU > 分类 > 平台` 继承；`null` 表示继承，`0` 表示明确零佣金。规则只追加版本，不回写历史支付快照。
- 预计佣金、已入账佣金、退款冲正和钱包余额均由服务端投影；佣金流水、余额流水、审计和 Outbox 与业务事实同事务提交。
- 冻结提现固定为 `available=-amount/frozen=+amount`，拒绝为 `available=+amount/frozen=-amount`，支付为 `available=0/frozen=-amount`。统一锁 Agent Wallet，幂等提交只能产生一个进行中申请。
- 所有资金命令复用 B10-B12 的稳定锁序并统一锁 Agent Wallet；幂等、审计和 Outbox 必须与余额、佣金或提现业务事实在同一事务提交。
- 银行账号去除 ASCII 空格和连字符后须为 6-32 位数字。只保存 AES-GCM 密文、用途隔离 HMAC、末四位和 key ID；常规接口、日志、审计、Outbox、幂等响应和前端持久化永不返回明文。
- 银行卡完整查看仅允许已认证 SUPER_ADMIN 通过 60 秒 TOTP reauth、单次授权和固定用途访问；五次失败必须锁定。离页、复制、超时或失败均清除敏感值并写最小审计。

### 4.4 `0006_b13_agent_finance_guards`

`0006` 为前向、可回放、失败原子化的防护迁移，不新增业务表或枚举。迁移前必须执行历史预检，异常不得自动修复。至少覆盖：

1. AgentProfile 与 `AGENT_ADMIN` 角色一致性、代理状态和版本包络。
2. 邀请码生命周期、代理归属和推广目标的主体包络。
3. 提现状态字段组合、金额边界、银行卡快照不可变性和 PAID 凭证要求。
4. 佣金规则版本、佣金流水、钱包流水与订单/订单项支付快照的引用形状。

空库回放、`0005 -> 0006` 升级、runtime 权限、函数/触发器指纹和 `migration diff=0` 均为 B13.0 强制门禁；已有 `0001` 至 `0005` 必须逐字节不变。

## 5. 串行实施批次

| 批次 | 交付内容 | 退出条件 | 状态 |
|---|---|---|---|
| B13.0 | B12 收口；B13 主记录；CH-028/029；契约、配置和 `0006` 实现/门禁 | Redocly、生成无漂移、迁移回放/权限/diff、审计与风险同步；同 SHA CI → development migration → rollback-only smoke 三绿 | **已退出：SHA `250f4c8f...8875` 同 SHA 三绿** |
| B13.1 | 总部创建/编辑/停用/重启/重置代理；Agent 独立 JWT realm；7 个认证接口与账户安全 | 临时密码限制、refresh 轮换、旧 token replay、停用与支付竞态、会话撤销、登录限流和账号枚举通过 | **已退出：SHA `a565a3c...62d6` 同 SHA 三绿** |
| B13.2 | 商品授权、邀请码轮换/启停/有效期、授权商品和服务端推广二维码 | 撤权/下架/邀请码失效后旧链接安全退化，历史绑定与佣金不变 | **已退出：SHA `8df89b98...8acd` 同 SHA 三绿** |
| B13.3 | Agent 客户/订单只读域；Admin 客户查询、归属转移/转直营及代理经营下钻 | 当前绑定/历史支付快照隔离，跨代理 404，转移与订单提交竞态通过 | **本地候选与最终复审完成，等待同 SHA 三门禁** |
| B13.4 | 佣金规则版本、订单佣金解释、Agent 佣金/钱包/看板和 Admin 账本下钻 | `null` 继承与 `0%` 分离，规则发布/支付并发只命中完整旧版或新版，流水精确一次 | 待准入 |
| B13.5 | 银行卡维护、最低提现设置、Agent 提现申请/列表/详情 | 加密/掩码、快照、余额冻结、单活动申请、幂等和退款竞态通过 | 待准入 |
| B13.6 | Admin 提现审核/拒绝、TOTP reauth、单次卡号查看、付款凭证、PAID | 拒绝解冻、PAID 精确扣减、凭证必需、60 秒单次授权及五次失败锁定通过 | 待准入 |
| B13.7 | Agent Web AGT-01 至 AGT-09 | generated contracts、严格解码、完整状态与五视口 E2E 通过 | 待准入 |
| B13.8 | Admin Web ADM-14/15/17/18-22 与 ADM-16 | preview-confirm、敏感值离页清除、下钻过滤和五视口 E2E 通过 | 待准入 |
| B13.9 | 三端真实基础设施纵向、全仓回归和最终复审 | `P0=0/P1=0`、零残留、同 SHA CI 与 rollback-only smoke 双绿 | 待准入 |

每批完成后必须暂停并记录测试、复审和残留检查；失败的 run、不同 SHA 的证据或历史阶段证据不得拼接为退出依据。

## 6. 测试与验收门禁

### 6.1 契约、数据库和权限

- Redocly、闭合 Schema、operationId、生成两次无漂移、Agent/Admin/Store token 交叉拒绝、临时会话无 refresh、敏感响应 schema 和错误包络。
- 空库完整回放、`0005 -> 0006` 升级、历史异常预检、原子失败、runtime 最小权限、函数/触发器指纹、冻结文件和 `migration diff=0`。
- 账号枚举、登录限流、refresh replay、停用旧会话、预览/确认幂等、一次性秘密重放、邀请码有效期、跨代理 404 和敏感扫描。

### 6.2 领域并发与资金

- 停用与支付、客户转移与下单、规则发布与支付、提现双提交、提现与退款/佣金入账、审核与重复付款均使用双连接真实数据库测试并验证锁序。
- 钱包冻结/拒绝/支付余额不变量、负余额、退款冲正、PAID 凭证、银行卡快照不可变和 TOTP 五次失败锁定。
- 佣金规则版本、订单项支付快照、预计/入账/冲正流水、审计和 Outbox 必须同事务精确一次；未知或不完整历史事实 fail-closed。

### 6.3 前端与真实纵向

- 375/390/414/1024/1440 五视口；覆盖 loading、empty、401、403、404、409、422、429、500、慢请求、响应丢失、重复点击和敏感值离页清除。
- Agent Web 和 Admin Web 仅使用 generated contracts 与严格响应解码器，客户端不推导状态、佣金、余额或可用动作。
- 真实纵向覆盖：总部建代理 → 首次改密 → 生成推广 → Store 绑定并支付 → 完成后佣金入账 → 绑卡提现 → 总部审核/TOTP 查看 → 上传凭证 → PAID；验证 PostgreSQL、Redis、MinIO、Worker、审计、幂等、Outbox 和所有 fixture 精确清理。

### 6.4 B13.0 本地候选证据

- 契约：Redocly 0 warning；`173/198/198/327/714/2,734/0`；Agent `21 paths / 23 operations`；完整 operation manifest SHA-256 为 `128509f76f2e62ebe78cd0465205a94236f87f70904bf614664dd1b02548dc80`，generated contracts SHA-256 为 `c4f3ef3759e277a43ffa7780009e028eb5cdb08b3d56d5d54e3b0bdbb720940b`；两次生成无漂移。
- 配置与工程：CI 等价配置合同通过；Agent/Admin/Store JWT audience 交叉拒绝测试通过；全仓 `2,758 passed / 166 skipped`，lint `0 errors / 437 existing warnings`，typecheck、生产 build、MP-Weixin build、Prisma validate、敏感扫描及 3 份 workflow YAML 解析均通过。
- 数据库：`0006` 正式/冻结副本 SHA-256 均为 `355311f6a5091f03bcb879f927ca78c984ec2cb26efb7f14bb4133161ccc2ea0`；完整空库回放、`0005 -> 0006`、10 项历史异常预检、原子失败、权限故障、checksum tamper、exact-B13 no-op、B10 `85/85`、B11 `77/77`、B12 `65/65`、B13 迁移专项 `12/12`、冻结检查及 `migration diff=0` 均通过。
- 最终数据库形状为 `76 tables / 59 enums / 23 partial indexes / 175 CHECKs / 76 RLS tables / 76 runtime policies / 47 user triggers / 92 trigger bindings / 28 owned functions`。function/trigger/CHECK/partial-index 指纹依次为 `af58e950c026d83e837f87edbef2f314`、`3d1547c613fd1f82c139a94dc0b2cc95`、`aa50500fa26840d5728dedae27b5c9b1`、`e0fdfdd16fcbb5640ce584f3a9981ceb`；76 张应用表零 fixture 残留、无剩余测试连接，临时 PostgreSQL 已删除。
- 两路最终独立只读复审均为 `P0=0 / P1=0 / P2=0`，未发现 B13.1 业务实现越界。非门禁测试残余为：10 组共享历史预检中 4 组具有独立畸形历史 fixture，其余由静态等价、约束、正路径和完整回放覆盖；尚无专门的 SUPER_ADMIN 并发降权与提现状态迁移 replay。两项均不得被解释为 staging 或真实资金准入。
- 远端最终证据：最终 SHA `250f4c8fabb08e170c012889c392461794ed8875` 的[普通 CI Run `33615918343`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33615918343)、[development migration Run `33618959837`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33618959837)与[rollback-only smoke Run `33619152221`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33619152221)均为 `completed/success`；时序为 CI 完成后 migration、migration 完成后 smoke，migration artifact `supabase-migration-250f4c8fabb08e170c012889c392461794ed8875-gphyhqryivtpicuqpdkq` 与 development project ref 匹配。B13.0 已退出，B13.1 获得准入。

### 6.5 B13.1 最终证据

- 交付：Admin 已实现代理列表、详情、创建、编辑、停用 preview-confirm、重启和密码重置 preview-confirm；Agent 已实现独立 realm 下的登录、refresh、登出、临时改密、普通改密、全端登出和当前账号 7 个接口。一次性秘密仅首次披露，幂等重放脱敏，10 分钟交接窗口从事务完成后起算。
- 安全与并发：临时会话固定 `CHANGE_PASSWORD_ONLY` 且无 refresh，同值临时改密返回 400 且不写 session、幂等或审计事实；Agent/Admin/Store token 交叉拒绝；refresh 轮换与 replay、登录限流、账号枚举、停用/密码重置会话撤销、停用与支付的共享 Agent 锁序均通过。preview 动态数量仅是预览时点估算，不属于确认身份；confirm 绑定 actor/session/action/target/request/version，并在锁内按当前事实重验。
- 本地门禁：契约为 `173/198/198/327/716/2,746/0`，generated contracts SHA-256 为 `2207286938dd174c0d6b36879407131253ef1ad4ffe6dc3e5726a83827852448`，operation manifest SHA-256 保持 `128509f76f2e62ebe78cd0465205a94236f87f70904bf614664dd1b02548dc80`；7 个 Agent 认证 operation 的全部非 200 分支均引用 required no-store/no-cache 专用响应，429 另要求 `Retry-After: 1..900`。Redocly、CH-028 检查、两次生成无漂移、冻结检查和敏感扫描均通过。
- 全仓门禁：`2,878 passed / 168 skipped`；API `1,364 passed / 46 skipped`；lint `0 errors / 437 existing warnings`，typecheck、完整 build、MP-Weixin build 与 3 份 workflow YAML 解析通过。
- 真实基础设施：全新 PostgreSQL 18.3 + Redis 7.4.10 执行 `B13_AGENT_AUTH_DATABASE_TEST_MODE=full pnpm db:test-b13-agent` 通过；覆盖临时改密、refresh、普通改密保留当前并撤销其他会话、logout、logout-all、停用/支付双序竞态、`0001 -> 0006` 回放与 `migration diff=0`。两种竞态时序均通过 PostgreSQL `pg_locks` 的 holder/waiter 观测确认真实阻塞，不依赖定时推断；停用先行后支付降级为 `DIRECT_AGENT_UNAVAILABLE` 且隐私/佣金事实为 0。Account、Agent、规则、订单、session 和 Redis DB 15 残留均为 0，临时容器已移除。
- 本地复审为 `P0=0/P1=0/P2=0`。最终 SHA `a565a3c406ad2f1af498f9f58b556315d30262d6` 的[普通 CI Run `33704252016`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33704252016)、[development migration Run `33706364405`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33706364405)与[rollback-only smoke Run `33706498758`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33706498758)均为 `completed/success` 且 `head_sha` 一致；CI 完成后 29 秒创建 migration，migration 完成后 24 秒创建 smoke。migration artifact `supabase-migration-a565a3c406ad2f1af498f9f58b556315d30262d6-gphyhqryivtpicuqpdkq` 与 development project ref `gphyhqryivtpicuqpdkq` 匹配。B13.1 已退出，B13.2 正式准入；B13 development 仍非 `GO`。

### 6.6 B13.2 本地候选与最终复审证据

- 交付：Admin 已实现商品授权查询/更新、邀请码轮换与启停/有效期 preview-confirm；Agent 已实现授权商品列表/详情和服务端推广二维码。一次性邀请码仅首次披露，幂等重放脱敏；Agent 仅可下载本人已绑定的 `READY/PRIVATE/PROMOTION_QR` 文件。
- 安全与一致性：商品、代理、邀请码和白名单在锁内复核；二维码先登记 `PENDING` 文件，再将 `READY` 文件、推广素材、审计、Outbox 和幂等结果同事务提交。撤权、下架或邀请码失效后旧链接退化为公开路径，不创建新候选，历史绑定、订单归属与佣金快照不变；密钥元数据不一致时 fail-closed。
- 本地门禁：全仓 `2,911 passed / 169 skipped`，其中 API `1,381 passed / 46 skipped`、数据库 `770 passed / 120 skipped`；lint `0 errors / 437 existing warnings`，typecheck、build、配置合同、Prisma validate、迁移冻结和敏感扫描均通过。OpenAPI 为 `173 paths / 198 operations / 198 unique operationId / 327 schemas / 716 schema refs / 2,746 local refs / 0 dangling refs`；Redocly、CH-028 检查和两次生成无漂移通过，generated contracts SHA-256 保持 `2207286938dd174c0d6b36879407131253ef1ad4ffe6dc3e5726a83827852448`。
- 真实基础设施：全新 PostgreSQL 18.3 + Redis 7.4.10 完成 `0001 -> 0006` 回放与 `migration diff=0`；Agent auth full 为 `1 passed / 1 skipped`，Agent commerce full 为 `1 passed`。76 张应用表 fixture 行数、Redis DB 15 key 和剩余测试连接均为 0，临时容器已删除。
- 三路最终复审：API、安全与数据库复审均为 `P0=0/P1=0/P2=0`。数据库复审另登记三项 B13.9 非缺陷残余：真实双连接竞态、真实 MinIO/Worker recovery、PostgreSQL `null`/`0` 投影；它们不改变 B13.2 复审结论，也不得被解释为 staging 或真实资金准入。
- 远端最终证据：最终 SHA `8df89b98f8b243e36c3bf8bf86b95a8e9d418acd` 的[普通 CI Run `33718349743`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33718349743)、[development migration Run `33721734961`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33721734961)与[rollback-only smoke Run `33722097476`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33722097476)均为 `completed/success` 且 `head_sha` 一致；CI 完成后创建 migration，migration 完成后创建 smoke，两个受保护任务分别通过匹配 CI 和 migration 证明。B13.2 已退出，B13.3 正式准入；B13 development 仍非 `GO`。

### 6.7 B13.3 本地候选与最终复审证据

- 交付：Agent 已实现客户列表/详情与订单列表/详情，只返回当前绑定客户和支付时冻结的历史订单归属；Admin 已实现客户列表/详情、生命周期聚合、当前归属与历史归属，以及代理间转移、转直营和直营转代理 preview-confirm。跨代理、已结束绑定和未知代理均返回 404。
- 安全与一致性：客户搜索只使用脱敏昵称、客户 ID 和别名，不提供原始昵称探测；自由文本转移原因经用途隔离 HMAC 后才进入归属历史、审计和输出。preview 与 confirm 使用不同幂等键，影响计数纳入请求 HMAC，confirm 在统一 `account -> customer -> binding -> agent` 锁序内重算并在漂移时 fail-closed。
- 并发：真实双连接覆盖 `ORDER_FIRST` 与 `TRANSFER_FIRST` 两种归属转移/下单时序；支付写入前重验代理版本、状态、候选 binding 身份及有效区间，只能得到完整旧归属或新归属快照，未出现混合快照或死锁。
- 本地门禁：全仓 `2,977 passed / 172 skipped`，其中 API `1,429 passed / 46 skipped`、数据库 `788 passed / 123 skipped`；lint `0 errors / 437 existing warnings`，typecheck、build、Prisma validate、迁移冻结、敏感扫描和 `migration diff=0` 均通过。OpenAPI 保持 `173 paths / 198 operations / 198 unique operationId / 327 schemas / 716 schema refs / 2,746 local refs / 0 dangling refs`；Redocly、CH-028 专项检查和两次生成无漂移通过，generated contracts SHA-256 保持 `2207286938dd174c0d6b36879407131253ef1ad4ffe6dc3e5726a83827852448`。
- 真实基础设施：全新 PostgreSQL 18.3 + Redis 7.4.10 完成 `0001 -> 0006` 回放；B13 Agent auth full 为 `1 passed / 1 skipped`，Agent commerce full 为 `1 passed`，Agent operations full 为 `3 passed`。76 张应用表 fixture 行数、Redis DB 15 key 和剩余测试连接均为 0，临时容器已删除。
- 三路最终复审：契约、安全/幂等和数据库/并发复审均为 `P0=0/P1=0`。B13.3 尚无最终 SHA 远端门禁证据，因此未退出；B13.4 未准入，B13 development 仍非 `GO`。

## 7. 回退、监控与发布边界

- `0006` 已进入 development 迁移历史，只能前向保留，不执行数据库降级。B13.1 失败时关闭 Agent/Admin 新路由并隐藏对应导航，不删除代理账户、钱包、归属、佣金、提现、审计或 Outbox 事实。
- 业务批次失败时关闭 Agent/Admin 新路由和导航，停用新 Worker handler；不删除代理、归属、佣金、钱包、提现、审计或凭证事实。恢复时必须重新进行鉴权、幂等和版本校验。
- 每次合并检查契约漂移、迁移失败、钱包/佣金不平、敏感扫描、Redis/MinIO 残留和远端 workflow；资金、银行卡明文、归属错写、跨主体读取或同键异结果立即升级 P0 并暂停发布。
- B13 development `GO` 只可在 B13.9 同 SHA 双绿、零残留和 `P0=0/P1=0` 后标记；不代表 staging、production 或真实资金准入。首次 staging 仍须外部独立代码、安全、数据库、隐私、佣金和提现复核。

## 8. 当前退出检查

| 门禁 | 当前结论 |
|---|---|
| B12 上游 | **最终 SHA `8c3589afcf7bb0dd5a4b8711d418e4c61b1ad09c`；CI `33592754575` 与 Supabase rollback-only smoke `33594513127` 同 SHA 双绿；B12 development `GO`** |
| CH-027 | **已自动失效；不得用于 B13** |
| CH-028 | **已批准，目标基线 `v2.4.11 / 2.4.11-ch028`** |
| CH-029 | **已批准，仅覆盖 B13.1-B13.9 脱敏 development；B13.9 后自动失效** |
| B13.0 本地 | **契约、generated contracts、配置、`0006`、完整回放/权限/diff、全仓测试/构建和零残留均通过** |
| B13.0 复审 | **两路独立只读复审均为 `P0=0/P1=0/P2=0`；无 B13.1 实现越界** |
| B13.0 远端 | **最终 SHA `250f4c8fabb08e170c012889c392461794ed8875`；CI `33615918343` -> migration `33618959837` -> smoke `33619152221` 同 SHA 三绿；B13.0 已退出** |
| B13.1 | **最终 SHA `a565a3c406ad2f1af498f9f58b556315d30262d6`；CI `33704252016` -> migration `33706364405` -> smoke `33706498758` 同 SHA 三绿；已退出** |
| B13.2 | **最终 SHA `8df89b98f8b243e36c3bf8bf86b95a8e9d418acd`；CI `33718349743` -> migration `33721734961` -> smoke `33722097476` 同 SHA 三绿；已退出** |
| B13.3 | **本地候选与最终复审完成，`P0=0/P1=0`；等待本批最终同 SHA CI -> migration -> smoke，未退出；B13.4 未准入** |
| 已知残余风险 | **B12 orphan `P2=1` 继续阻断 staging/真实数据；B13 不扩大许可** |
