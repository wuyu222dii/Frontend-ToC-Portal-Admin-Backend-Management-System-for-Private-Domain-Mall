# B14 总部经营看板与销售分析闭环

> 批次：B14；产品/API 基线：`v2.4.12 / CH-030`、OpenAPI `2.4.12-ch030`；交付门禁：CH-031（已随 B14.3 自动失效）；更新日期：2026-09-05；当前状态：B14.0-B14.3 已完成，最终实现 SHA `f4521d4188cb74c3bac34b992016d8730468ae2a` 的 development migration 与随后 rollback-only smoke 同 SHA、依次成功，B14 development `GO`。该结论仅限脱敏 development，staging、production 和真实客户/资金数据仍为 `NO-GO`。

## 1. 上游与治理

B13 最终 SHA `250cbb824ad11f4b0e11338c19494655ec384ff6` 的 development migration Run `33952031401` 与 rollback-only smoke Run `33952283771` 同 SHA 且依次成功。B13 development `GO`，CH-029 已自动失效；B13.8 五类 P2 和 B12 未绑定 `READY/PRIVATE` orphan `P2=1` 继续登记，不因 B14 消失。

### 1.1 CH-030 看板与销售分析基线

- CH-030 已批准并准入 B14.0，将产品/API 基线升级为 `v2.4.12 / 2.4.12-ch030`。
- 本轮只收口 ADM-02 总部经营概览、日/月销售、商品净销量排行和客户净消费排行，不扩展代理端看板。
- 保留现有 5 个 path 和 operationId：`getAdminDashboard`、`getAdminReportsDailySales`、`getAdminReportsMonthlySales`、`getAdminReportsProductRanking`、`getAdminReportsCustomerRanking`；B14.0 只允许收紧参数、响应和口径，不新增 path。

### 1.2 CH-031 单维护者 development 例外

- CH-031 已批准并按原范围执行，仅覆盖 B14.1-B14.3、Mock/脱敏 development 和第二 reviewer 缺失；不虚构外部独立复核。
- B14.1-B14.3 已严格串行并在各批暂停，最终复审为 `P0=0/P1=0`。CH-031 已随 B14.3 完成自动失效，不得沿用到下一阶段、staging 或 production。
- 2026-09-05 起不恢复 GitHub 普通 CI，不新建自动化 workflow；远端仅沿用现有手动 Supabase rollback-only smoke 及其强制的同 SHA no-op migration attestation。

## 2. 交付范围与边界

### 2.1 本批覆盖

- 总部概览：今日/本月/累计净销售、本月代理渠道净销售、今日创建/有效支付订单、客户快照、新增注册/绑定、活跃代理、提现待办和商品排行。
- 报表：日销售、月销售、商品净销量排行、客户净消费排行，支持 `GLOBAL | DIRECT | AGENT` 范围、`Asia/Shanghai` 自然日和稳定分页。
- 总部 Web ADM-02：概览、日报、月报、商品排行和客户排行五个视图，使用 generated contracts 和严格响应解码。

### 2.2 明确排除

- 不新增 Prisma model、字段、枚举、索引或 migration；`0001` 至 `0006` 继续逐字节冻结。
- 不新增 Worker、定时汇总、消息消费、数据仓库、BI、预测、实时推送或 Excel 导出。
- 不引入 ECharts、Chart.js 或其他图表依赖；首期使用密度可控的指标带、数据表和简单 CSS 长度提示。
- 不改写历史订单、支付、退款、归属、佣金或客户事实，不将可重建缓存当作权威业务事实。

## 3. 契约决策

- 五个读接口均只允许已认证 `SUPER_ADMIN`，使用现有 Admin Bearer realm、统一错误包络和私有缓存头；Agent/Store token 必须拒绝。
- 所有金额延用契约 decimal string，净值可为负；前端不用浮点数重算。五个响应都返回 `timezone=Asia/Shanghai` 与 `as_of`，四类报表另返回 `data_freshness=REALTIME`。
- 日/月报保留可加指标与不可相加快照的区分：仅 `GLOBAL` 返回实际 `new_registration_count`、`active_agent_count` 和 `customer_total_snapshot`，其他范围固定为 0；活跃代理按区间事实去重，客户总量取区间结束时最近快照，二者均不累加日报。
- `scope=AGENT` 且省略 `agent_id` 时汇总全部代理渠道，传入时限定指定代理且未知代理返回 404；`GLOBAL`/`DIRECT` 不接受 `agent_id`。归属按支付时冻结快照，不按客户当前绑定回写历史。
- 日期或月份起止边界必须成对传入或同时省略；默认值、最大跨度、倒置区间与未来区间拒绝必须在 B14.0 的 OpenAPI 中闭合，B14.1 不得自行推导。
- 排行固定按净值降序、资源 ID 升序收口，分页时排名在全量结果上确定。客户排行只返回 alias 和契约允许的脱敏昵称，不返回手机号或地址。

## 4. 查询与一致性决策

- 首期在单次只读 `REPEATABLE READ` 视图内直接聚合已冻结事实，所有分区共享一个 `as_of`；请求不写表、不发 Outbox、不依赖 Worker。
- 创建订单按 `sales_order.created_at` 统计全部状态；支付按首次成功事实所在日期统计；退款按成功退款事实所在日期统计。有效支付订单必须当前剩余实付大于 0。
- 商品排行按支付时 Product/SKU 快照聚合成功支付数量减成功退款数量；客户排行按成功支付金额减成功退款金额。集中退款周期允许净销售、净件数或净消费为负。
- 注册历史不因后续注销回写；`GLOBAL` 客户总数按 `as_of` 时当前未注销消费者统计，新增注册与新增绑定分开统计；`GLOBAL` 活跃代理是当前 `ACTIVE` 且区间内至少有一笔成功归属支付的代理去重数。`DIRECT/AGENT` 不复制这三项全局指标。
- 既有 `sales_daily_aggregate` 只是可重建缓存，B14 不增加其刷新者，不以其存在或完整作为返回前置。若事实查询出现具体慢查询证据，再单独走变更评估索引或汇总机制。

## 5. Admin Web 决策

- ADM-02 使用单一总部看板页和五个清晰标签视图；登录后默认进入概览，不建营销落地页。
- 概览使用紧凑指标带、待办和商品排行表；报表使用日期/范围筛选、数据表和分页。不用装饰卡片、嵌套卡片或图表依赖。
- 筛选状态通过路由 query 保留，只保存非敏感条件；返回页面时可恢复，无效 query 回到契约默认。
- 每个视图必须有稳定的 loading、empty、error/retry 和分页状态；401 走既有会话处理，403/404/422/429/500 使用服务端错误包络，不显示伪造的零值或旧数据。
- 下钻只导航到已实现的商品、客户、代理、订单和提现资源；不在前端重算指标或修正服务端排名。

## 6. 严格串行批次

| 批次 | 交付内容 | 最小退出条件 | 当前状态 |
|---|---|---|---|
| B14.0 | CH-030/031、B14 主记录、OpenAPI/generated contracts 与查询口径收口 | Redocly、两次生成无漂移与 contracts build 通过；路径和 operationId 数量不变 | **已完成** |
| B14.1 | 只读 repository、Admin API 与五个 operation 实现 | 两个定向基础测试通过；database/API 受影响模块构建通过 | **已完成** |
| B14.2 | Admin Web ADM-02 概览与四类报表 | generated contracts、严格解码、筛选恢复和完整请求状态；Admin Web build 通过，有限手工核对与环境缺口登记后由用户批准进入 B14.3 | **已完成；有限手工核对与缺口见第 11 节** |
| B14.3 | 聚焦收口、最终复审与远端 smoke | `P0=0/P1=0`，同 SHA no-op migration attestation -> rollback-only smoke，登记未验证项，随后才可标记 B14 development `GO` | **已完成；两项远端门禁依次成功** |

各批已按要求完成后暂停。B14.2 已实现 Admin Web 页面、严格解码、筛选恢复、请求取消和现有业务列表下钻；用户在获知第 11 节未验证项后明确批准进入 B14.3，该批准只接受本次脱敏 development 验收缺口，不把缺口改写为通过。B14.3 已完成同 SHA 远端门禁，B14 现停在 development `GO`，下一阶段尚未准入。

## 7. 验证决策

- B14.1 只新增两个定向基础测试文件：仓储测试覆盖直营/代理归属、上海零点、跨日退款负净值与排行稳定顺序；API 测试覆盖默认范围、AGENT 全渠道/指定代理、非法参数组合和严格响应形状。B14.2 不新增前端测试文件，只执行一次 Admin Web build 并做最小手工页面核对。
- 不新增大量低价值单测、全视口矩阵或重复全仓回归；只在契约强制、仓库规则要求或出现具体失败信号时增加最小聚焦测试。需扩大范围时先说明目的、范围与预计耗时，等待用户确认。
- GitHub 不运行普通 CI。保留现有 Supabase development smoke；由于 smoke 强制同 SHA migration attestation，B14.3 可运行既有 no-op development migration workflow，但不创建或部署新迁移。
- 如环境限制使某项未验证，退出记录必须列出未验证内容、原因、最小手动步骤、预期结果和注意事项，不得补写为通过。

## 8. 当前检查点

| 项目 | 结论 |
|---|---|
| B13 上游 | **development `GO`；最终 SHA `250cbb824ad11f4b0e11338c19494655ec384ff6` 两项远端门禁依次成功** |
| CH-029 | **已失效，不得用于 B14** |
| CH-030 | **已批准，目标基线 `v2.4.12 / 2.4.12-ch030`** |
| CH-031 | **B14.3 完成后已自动失效，不得沿用** |
| B14.0 | **已完成** |
| B14.1 | **已完成；P0=0/P1=0** |
| B14.2 | **已完成；Admin Web build、只读复审和有限手工核对已登记** |
| B14.3 | **已完成；最终实现 SHA 的 migration/smoke 同 SHA、依次成功** |
| 数据与发布 | **仅限脱敏 development；B13/B12 P2 继续携带；staging、production、真实数据和真实资金 `NO-GO`** |

## 9. B14.0 退出证据

- `pnpm contracts:lint` 通过；OpenAPI 保持 `173 paths / 198 operations / 198 unique operationIds`。
- generated contracts 连续生成两次，`packages/contracts/src/generated/openapi.ts` 两次 SHA-256 均为 `a7a15ad8fe9c7436a6b02e294612376a3efdb5546a4ec7186608141eaf2f55cc`；`pnpm --filter @qingxu/contracts build` 通过。
- 最终复审为 `P0=0/P1=0`；发现的日期边界、Dashboard 指标口径、私有缓存头和 CH-031 测试门禁四项 P1 均已在 B14.0 内关闭。
- `git diff --check` 通过；未新增迁移、Worker、依赖、GitHub workflow、业务实现或 B14.1-B14.3 测试。

## 10. B14.1 退出证据

- 新增只读 `AdminAnalyticsRepository`，在每个请求的一次 `REPEATABLE READ` 事务中以 PostgreSQL `transaction_timestamp()` 生成统一 `as_of`，实现 Dashboard、日/月销售、商品排行和客户排行五组权威事实查询；不读取 `sales_daily_aggregate`。
- 新增五个 `SUPER_ADMIN` Admin Analytics GET 路由，关闭未知参数并校验分页、固定时区、日期/月范围与 scope/agent 组合；全部使用既有统一响应包络和 `no-store, private`。
- `B141_ADMIN_ANALYTICS_DATABASE_TEST_MODE=rollback node --env-file=.env node_modules/vitest/vitest.mjs run packages/database/src/admin-analytics.repository.spec.ts --exclude 'dist/**'` 最终通过（1 file / 1 test）；fixture 位于 2001 年历史区间并由外层事务精确回滚。
- `pnpm --filter @qingxu/api exec vitest run src/admin-analytics/admin-analytics.spec.ts --exclude 'dist/**'` 通过（1 file / 10 tests）；`pnpm --filter @qingxu/database build` 与 `pnpm --filter @qingxu/api build` 均通过。
- 最终只读复审为 `P0=0/P1=0`。复审发现并关闭了非 GLOBAL 范围复制全局注册、活跃代理和客户总量的 P1；`git diff --check` 通过。
- 未运行全仓、历史阶段或浏览器 E2E，未新增迁移、Worker、依赖、前端页面或 GitHub workflow；B14.2 尚未准入。

## 11. B14.2 退出证据

- 新增 `/dashboard` 总部经营页和首位侧栏入口；登录、恢复码登录及首次 TOTP 绑定完成后统一落到经营概览。页面提供概览、日报、月报、商品排行、客户排行五个标签视图。
- 五个读接口全部使用 generated contracts 与严格运行时解码；筛选写入路由 query，指定代理时才加载代理选择器，切换查询会取消旧请求并拒绝迟到响应覆盖。401、403、400/404、422、429、500、网络失败、loading、empty 和 retry 均有闭合状态。
- 待发货、待审核售后和待审核提现跳转到现有业务列表并初始化相应状态；商品、客户和代理只下钻现有资源。Dashboard 未提供的待发货或待审核售后数量没有在前端伪造。
- 最终只读复审为 `P0=0/P1=0`。`pnpm --filter @qingxu/admin-web build` 最终通过；初次构建发现的 `exactOptionalPropertyTypes` 可选字段问题已修复。未新增前端测试、依赖、迁移、Worker 或 GitHub workflow，也未运行历史测试或浏览器 E2E。
- 本机 API 健康检查和 Admin Web 启动成功，未认证访问根路径正确进入 `/login`。使用既有已认证 development 会话核对了 `/dashboard?scope=GLOBAL&view=overview`、五个标签、`GLOBAL`、`DIRECT`、全部代理和指定代理模式；空数据均显示明确空态，指定代理模式在当前无代理 fixture 时显示选择提示。
- 待审核提现下钻已确认进入 `/withdrawals?status=PENDING` 并初始化状态筛选。浏览器完整刷新会按既有内存会话策略清除会话并回到 `/login`，没有绕过认证、伪造会话或改写管理员凭据。
- 未验证项：使用新凭据登录后的默认 `/dashboard` 落点、指定代理和报表/排行的非空结果、日期范围提交、故障后的错误重试，以及其余待办/资源下钻。原因是当前无可重复使用的管理员明文密码/TOTP、development 没有代理和经营分析 fixture，且完整刷新会结束既有会话；这些项目未补写为通过。
- 最小手工补验：仅在脱敏 development 使用现有管理员完成一次新登录，预期进入经营概览；准备一个虚构代理及支付/退款事实后切换范围和日期，预期 URL query 保留且结果与服务端事实一致；在浏览器中短暂阻断一个 Analytics GET 后恢复并点击重试，预期错误态消失且只展示最新响应。不得使用真实客户或资金数据，也不得在 staging/production 执行。

## 12. B14.3 退出证据

- 最终实现 SHA 为 `f4521d4188cb74c3bac34b992016d8730468ae2a`；`main` 与 `origin/main` 在远端门禁期间均固定于该 SHA，工作区无业务代码变化。
- [development migration Run `33960486774`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33960486774) 为 `completed/success`；其后创建的 [rollback-only smoke Run `33960618178`](https://github.com/wuyu222dii/Frontend-ToC-Portal-Admin-Backend-Management-System-for-Private-Domain-Mall/actions/runs/33960618178) 也为 `completed/success`。两者 `head_sha` 均精确等于最终实现 SHA，执行顺序满足 attestation 前置要求。
- Smoke 中 B10、B12、B13 三组 rollback-only 检查均成功。B14 不新增迁移，因此 migration 只部署和复核既有 `0001` 至 `0006` 前向链；未创建普通 GitHub CI、B14 专用 workflow 或新迁移。
- 后端、Admin Web 与远端门禁最终只读复审均为 `P0=0/P1=0`；`git diff --check` 通过。B14.3 未重复运行已经通过的本地测试、构建、全仓回归或浏览器 E2E，也未新增测试、依赖、Worker 或业务代码。
- B14 development 正式标记 `GO`，CH-031 同时自动失效。B12 orphan 与 B13 已登记 P2 继续阻断 staging、production、真实客户数据和真实资金；下一阶段尚未准入。
