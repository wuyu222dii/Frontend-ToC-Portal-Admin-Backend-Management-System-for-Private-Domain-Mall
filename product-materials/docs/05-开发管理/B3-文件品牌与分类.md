# B3 文件、品牌与分类

> 批次：B3；产品基线：v2.4.1 / CH-006；交付门禁例外：CH-007；下载签名幂等语义勘误：CH-008；更新日期：2026-08-14；当前状态：B3.0 已通过，B3.1 文件基础本地/一次性环境验收通过并暂停，B3.2 未开始；数据范围：仅脱敏开发数据。

## 1. 分段边界与硬门禁

B3 拆为四个独立小段，每段验收后暂停：

| 子批次 | 唯一交付 | 当前状态 | 准入条件 |
|---|---|---|---|
| B3.0 契约解阻 | CH-006 文档、OpenAPI、生成 contracts、静态原型和门禁 | 已完成并暂停 | 全部门禁通过 |
| B3.1 文件基础 | NestJS/Worker/MinIO 文件 intent、complete、download 和清理 | 本地/一次性环境验收通过并暂停 | B3.0 全部门禁 + CH-007 明确风险接受 |
| B3.2 品牌与分类 | CRUD、排序、乐观锁、名称保留、生命周期、归档和恢复 | 未开始 | B3.1 验收通过并暂停后收到明确继续指令 |
| B3.3 总部后台 | ADM-05/06 上传、主数据交互与五视口 E2E | 未开始 | B3.2 验收通过并暂停后收到明确继续指令 |

B3.0 与 B3.1 均已独立完成当前子批次并暂停。根据 2026-08-14 批准的 CH-007，GitHub Actions 与 Supabase development 远端证据延期，不阻塞 B3.1 本地实施和独立验收。该例外不把本地或一次性 PostgreSQL、Redis、MinIO 结果视为云端证据，也不放行 B3 整体验收、staging 或 production。

## 2. B3.0 当前交付

### 2.1 品牌与分类契约

- `BrandCreateRequest.sort_order` 为必填非负整数，update 为可选；Category 保持同样约束。两类列表固定 `sort_order ASC,id ASC`。
- 创建 `initial_status` 固定为 `DRAFT`，禁止创建即启用。
- 品牌/分类使用闭合 `MasterDataLifecycleAction`，动作仅 `ACTIVATE/DEACTIVATE/SOFT_DELETE`，原因 2-500 字符；Product/SKU 原 `LifecycleAction` 保持 `DEACTIVATE/SOFT_DELETE`。
- 状态机固定：`DRAFT/INACTIVE -> ACTIVE`；`ACTIVE -> INACTIVE`；`DRAFT/INACTIVE -> ARCHIVED + deleted_at`。ACTIVE 不得直接软删除，非法或同状态的新请求返回 409。
- 三个动作均先 preview，再提交相同业务字段、短时 token/确认哈希、新幂等键和 `If-Match`。preview 发现 ACTIVE 商品仍返回 200 影响清单，confirm 返回 `ACTIVE_PRODUCT_DEPENDENCY` 422。
- restore 保持原因、幂等键和 `If-Match`，只接受 soft-deleted ARCHIVED，清除 `deleted_at`、固定回 DRAFT、版本加一；不新增 restore-preview，重新启用必须另走 preview-confirm。
- 默认列表排除归档；显式 `status=ARCHIVED` 只返回软删除记录；详情允许读取归档。软删除名称全局保留，再次创建返回 `SOFT_DELETED_KEY_RESERVED` 409。

### 2.2 文件契约

- 七类 purpose 统一只接受 `image/jpeg`、`image/png`，最大 5 MiB；`UploadIntentRequest.sha256` 必填且为 64 位小写十六进制。
- complete 请求的 SHA-256/size 必须同时匹配 intent 和服务端对象 MIME、魔数、大小、SHA-256 实测结果；不匹配返回 `FILE_CONTENT_MISMATCH` 422。
- 上传签名 15 分钟，私有下载签名 5 分钟；对象 key 固定使用不含原文件名的 `staging/`、`public/`、`private/` 分区。
- bucket 默认私有，仅 `public/*` 匿名 GET；售后、提现和推广 QR 固定 `private/*`。PENDING/staging 满 24 小时才进入清理候选，删除前二次确认无 READY 和业务引用。
- 不新增 `completed_at` 或 `updated_at`。完成事实由 READY、最终对象 key、服务端实测 MIME/size/SHA-256、审计和闭合幂等响应联合表达；首次 `completed_at` 只保存在该响应及其幂等记录中。
- 上传 intent 与 lifecycle preview 使用 `HASH_ONLY`。download URL 是无 `Idempotency-Key` 的 GET，不创建幂等记录，响应 no-store/private 且签名 URL 不持久化、不记日志。`FILE_UPLOAD_COMPLETE` 是 B3.1 的闭合响应缓存策略：同键同请求精确重放，新键重复 complete 返回 409。

### 2.3 规模与不变项

| 项目 | CH-006 实测结果 |
|---|---|
| 页面 / FR / AC / US | 52（21/9/22）/ 142 / 116 / 24，均不增加 |
| OpenAPI | 172 paths / 196 operations / 196 unique operationId / 306 schemas / 0 dangling refs；原始 schema refs 685 |
| B3 operation | Files 3 + Brands 7 + Categories 7，共 17 个；不新增路径或 operation |
| 数据库 | 76 models / 59 enums；Prisma、`0001_initial`、RLS/CHECK/触发器/条件唯一索引逐字节不变 |
| 新业务错误 | `SOFT_DELETED_KEY_RESERVED` 409、`ACTIVE_PRODUCT_DEPENDENCY` 422、`FILE_CONTENT_MISMATCH` 422 |

上述 OpenAPI 和编号统计已由持久门禁实测通过；B3.0 只冻结设计与契约，B3.1 文件实现和验收事实以第 3.1、5 节为准，不能外推为品牌、分类或总部后台已经实现。

## 3. 分段实现与后续要求

### 3.1 B3.1 文件基础

- intent 已在短事务内原子创建 PENDING、预期 SHA-256/size/MIME、审计和 `HASH_ONLY` 幂等事实，再返回限制 `Content-Length`、MIME 和 15 分钟 TTL 的 presigned PUT；对象 key 不含原文件名。
- complete 已使用带续租心跳的 Redis owner lease，在数据库事务外读取对象并校验大小、MIME、魔数和 SHA-256，按 ETag 条件幂等复制并复核最终对象；短事务内原子写 READY、审计、闭合 `FILE_UPLOAD_COMPLETE` 响应和延迟 staging 清理 Outbox。
- READY 提交后立即尽力删除精确 staging key；延迟事件再次核对 READY/final key 后兜底删除，覆盖即时删除失败及旧 PUT URL 再上传窗口。copy 成功但数据库失败可用同一请求重试收敛，不依赖盲目对象前缀或 bucket 生命周期删除。
- Worker 仅处理满 24 小时的 PENDING 候选，并在破坏性操作前后续租、二次确认无 READY/业务引用，只删除记录绑定的精确 staging/final key；失败进入有界重试，成功将清理事实、审计和 Outbox 状态一致提交。
- MinIO root 与 runtime 凭据分离；runtime 仅拥有同一 bucket 下 staging/public/private 的必要 Get/Put/Delete，匿名策略只允许 `public/*` GET，不允许列举 bucket 或读取 staging/private。真实 MinIO 已覆盖超限、伪 MIME、哈希不符、copy source 变化、签名过期、匿名边界、CORS 和无盲目生命周期规则。
- `GET /files/{file_id}/download-url` 按 CH-008 每次重新执行身份/归属校验，不接受 `Idempotency-Key`、不创建幂等记录；响应 `no-store, private`，签名 URL 不持久化、不记日志，并写一条不含 URL 的 `READ_SENSITIVE` 审计。
- API 与 Worker 的 Redis 连接采用有界退避持续重连；注入 Redis 但尚未 ready 时健康检查返回 503，防止服务在 lease/清理依赖不可用时被误判为可接流量。

### 3.2 B3.2 品牌与分类

- 实现文件挂接、CRUD、稳定排序、乐观锁、软删除名称保留、归档查询、恢复和审计。
- lifecycle preview 只存 token 哈希并绑定 actor/session/action/target/request/version；confirm 在同一串行化事务内消费 preview、重查依赖、更新状态/version、写审计和幂等结果。
- 与未来商品发布统一使用 `brand -> category -> product IDs ASC` 锁序；同一幂等键精确重放，新键非法状态返回 409。

### 3.3 B3.3 总部后台

- 只开放品牌、分类和既有账户安全导航，不显示未完成模块。
- ADM-05/06 完成 Logo/Icon 上传、固定草稿创建、排序、编辑、筛选、preview-confirm、归档和恢复为草稿。
- 409 丢弃旧 preview、刷新最新记录并要求重新确认；不得自动覆盖。页面不展示编码、分类说明、佣金来源、商品/SKU 数等 API 不支持字段。
- 五视口覆盖 375/390/414/1024/1440，并验证加载、空、500、403、409、422、重复提交与成功状态。
- B3.3 本地验收后，必须在受保护的 Supabase development 环境以 `mall_runtime` 对 B3 repository 执行独立 rollback-only smoke；测试事务最终 ROLLBACK，并复核无测试数据、DDL、权限或 migration history 残留。该证据不同于 CH-007 前原定作为 B3.1 前置条件的 B2 smoke；缺少 B3 专项远端证据时不得把 B3 整体标记为完成。

## 4. 验收命令

### 4.1 B3.0 契约与冻结数据库

在 Node.js 22 和冻结 pnpm 版本下执行：

```bash
pnpm contracts:lint
pnpm contracts:check
cp packages/contracts/src/generated/openapi.ts /tmp/ch006-openapi.ts
pnpm contracts:generate
cmp /tmp/ch006-openapi.ts packages/contracts/src/generated/openapi.ts
pnpm prisma:validate
pnpm db:check-frozen
pnpm db:migrate:baseline
pnpm db:test-permissions
pnpm db:diff
pnpm lint
pnpm typecheck
pnpm test
pnpm build
pnpm build:miniapp
pnpm scan:sensitive
```

上面的 `cmp` 用于含本轮预期契约改动的本地工作树；CI 或 CH-006 已提交后的干净检出仍在生成后执行 `git diff --exit-code -- packages/contracts/src/generated`。两种方式都必须证明二次生成没有漂移，不得用工作树相对旧 HEAD 的预期差异误判失败或放宽 CI。

数据库三条命令必须依次指向同一个一次性空 PostgreSQL：先以 owner 连接执行 `db:migrate:baseline`，再用门禁脚本注入并回滚权限故障，最后以 `mall_migrator` 连接执行 `db:diff`。不得指向共享开发库，也不得用已有数据库替代空库回放。

原型验证使用仓库既有 CH-006/三端验收入口，实测通过 84 个响应式渲染、21/9/22 页面契约、14 条小程序流程和 16 条后台/代理流程；ADM-05/06 在 375/390/414/1024/1440 五视口均非空且无页面级横向溢出。

门禁必须确认：

1. `pnpm contracts:check` 输出 172 paths、196 operations、196 unique IDs、306 schemas、0 dangling refs，原始 schema refs 685。
2. `MasterDataLifecycleAction` 仅被品牌/分类四个 lifecycle operation 引用；Product/SKU ACTIVATE 请求被闭合 schema 拒绝。
3. 三项新错误码、排序、DRAFT 创建、状态矩阵、ARCHIVED 查询、restore-to-DRAFT 和文件约束在文本、OpenAPI、生成类型和原型中一致。
4. Prisma 与冻结迁移无字节变化；一次性空库完整回放和权限故障注入通过，Prisma validate 与 migration diff 为零。
5. 页面/FR/AC/US 与 path/operation 数量不变，敏感 URL/token 不进入缓存、日志或生成物。

### 4.2 B3.1 文件基础

```bash
pnpm db:test-b3-files
pnpm db:test-b3-files-api
pnpm test:b3-files:minio
pnpm --filter @qingxu/storage test
pnpm --filter @qingxu/api test
pnpm --filter @qingxu/worker test
pnpm --filter @qingxu/database test
pnpm check
pnpm contracts:lint
pnpm contracts:check
pnpm prisma:validate
pnpm db:check-frozen
pnpm scan:sensitive
```

前三个命令分别要求一次性 PostgreSQL、真实 PostgreSQL + Redis + MinIO 以及真实 MinIO 受控凭据。脚本保护器禁止把本地结果标记为 Supabase development 远端结果；测试结束后须销毁任务专用容器和数据卷。

## 5. 验收状态与回滚

当前 B3.0 已通过验收并暂停；B3.1 本地/一次性环境门禁也已全部通过，安全复核结论为 `P0/P1=0`、B3.1 本地 `GO`：

- `pnpm check` exit 0；config 33、platform-core 86、storage 16、database 141、API 118、Worker 30，共 `424 passed / 37 skipped`。全量 build 通过，lint 为 0 error / 207 个既有 Vue warning。
- 真实 PostgreSQL + Redis + MinIO API 集成 `3/3`，Worker 清理集成 `3/3`；B3 文件数据库集成 `2 passed / 1 skipped`。真实 MinIO 最小权限、匿名访问、内容完整性、签名与故障恢复边界全部通过。
- contracts lint/check/typecheck、二次 generate hash 一致均通过；统计保持 172 paths / 196 operations / 196 unique operationId / 306 schemas / 685 schema refs / 2,561 local refs / 0 dangling refs。CH-008 后当前生成文件 SHA-256 为 `a216f4bc665160cdfbff078410c87fbfc7d4748b23ddc1f08c7c702a817b245e`。
- Prisma validate、冻结文件、权限故障注入和 migration diff=0 通过；完整 synthetic env 下 `docker compose config`、MP-Weixin build、`git diff --check` 通过。敏感扫描覆盖 268 个文件，21 处命中均为精确白名单 synthetic fixture。
- 下载签名测试证明无幂等记录、URL 不落审计/数据库，并产生不含 URL 的 `READ_SENSITIVE` 审计。

B3.1 已进入业务实现，不能再使用“整体回滚 CH-006 文档/契约”的前置方案；需要回退时关闭文件模块路由、Worker 文件清理处理器和后续导航入口，不执行数据库降级。移除 `public/*` 匿名策略前必须先确认没有已发布素材引用，禁止按前缀盲删对象。

B3.1 已按小批次规则完成本地验收并暂停，B3.2 尚未开始，须收到明确继续指令后才能进入。B2 Supabase development rollback-only、B3.3 后覆盖 B3 repository 的专项 rollback-only smoke、head SHA/审批记录与 GitHub 受保护成功运行证据均继续列为未完成；补齐前 B3 整体保持未完成，不得进入 staging 或 production。
