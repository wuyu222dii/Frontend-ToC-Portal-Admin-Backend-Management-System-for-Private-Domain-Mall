# B6 消费者匿名商城目录

> 批次：B6；当前产品/API 基线：v2.4.4 / CH-014；交付门禁：CH-013（仅 B6 脱敏 development，B6.4 后自动失效）；更新日期：2026-08-26；当前状态：B6.3 MP-06 游客本地购物车已完成本地实现与验收并暂停，B6.4 总验收尚未开始；数据范围：仅脱敏 development；staging/production：`NO-GO`。

## 1. 当前结论

B0-B5 development 均已完成。B5 实现基准 `d97c43958142eaa0fa5a0a9954bb21d136944ba2` 的普通 CI Run `32822780209` 与 Supabase rollback-only Run `32823898006` 为同一 SHA 双绿，CH-011 已随 B5.4 自动失效。用户已批准 CH-013 和 CH-014：B6 只交付匿名浏览、SKU 选择与游客本地购物车，不伪造登录、收藏、服务端购物车、结算或订单成功。

B6.0-B6.3 已按批准顺序实施。B6.2 完成跨端 Store API 客户端、MP-01 至 MP-05；B6.3 已完成 MP-06 版本化游客本地购物车、详情回源刷新与游客加购。购物车仍完全保存在本地，结算只触发登录提示，不调用服务端 Cart 或订单接口。当前只关闭 B6.3 本地退出门禁并暂停，B6.4 的真实纵向、普通 CI、Supabase workflow 和同一最终 SHA 双绿尚未开始，B6 不是最终 development `GO`。

## 2. 基线与准入

| 门禁 | 证据 | 结论 |
|---|---|---|
| B0-B5 development | B5.4 真实纵向链路、全量回归、普通 CI 与 Supabase rollback-only 双绿 | 通过，作为 B6 历史前置 |
| CH-013 | 用户已批准单人 B6 development 门禁例外 | 生效；B6.4 后自动失效 |
| CH-014 | 用户已批准 v2.4.4/CH-014 匿名目录契约 | 生效 |
| OpenAPI 规模 | 172 paths / 196 operations / 196 unique operationId / 312 schemas / 691 schema refs / 2,577 local refs / 0 dangling refs | Redocly、CH-014 专项检查、生成漂移与 contracts typecheck 通过 |
| 数据库冻结 | `pnpm db:check-frozen`、`pnpm prisma:validate`、`pnpm db:diff` | 逐字节不变；`No difference detected` |
| 静态原型 | 96 个响应式渲染、21/9/22 页面、14 条小程序 + 18 条后台/代理流程 | 全部通过；与工程页面验收证据相互独立 |
| B6.1 公开 API | 5 个匿名 GET、独立 repository、Repeatable Read 批量投影、Redis 固定窗口、本地与受控 Supabase rollback-only | 通过并暂停 |
| B6.1 数据库证据 | repository unit 9；database 全包 332 passed / 52 环境模式跳过；隔离 PostgreSQL full 1 passed / 1 mode skip；事务外 0 | 通过 |
| B6.1 Supabase 证据 | 受控 development runtime rollback-only 1 passed / 1 mode skip；事务外 fixture 0 | 通过；不是 B6.4 workflow 双绿 |
| B6.2 工程页面 | miniapp unit 4 files / 58 passed；H5 与 MP-Weixin build；Playwright 19 passed / 36 designed skips | MP-01 至 MP-05 本地验收通过 |
| B6.3 游客购物车 | 当前累计 miniapp unit 6 files / 84 passed；H5 与 MP-Weixin build；Playwright 34 passed / 56 designed skips；root lint 0 errors（保留既有 warnings） | MP-06 本地验收通过并暂停，`P0=0/P1=0` |
| B6.4 最终门禁 | 真实 H5 纵向、普通 CI、Supabase workflow、同一最终 SHA 双绿 | 未开始，不得标记 B6 最终 development `GO` |

CH-013 不降低契约、数据库、权限、安全、并发或测试标准。每个业务批次仍须真实执行自动化；B6.4 最终实现 SHA 必须取得普通 CI 和 Supabase rollback-only 双绿，并以事务外查询证明 fixture 无残留。

## 3. CH-014 公开目录契约

### 3.1 公开资源边界

- B6 实现 5 个已有公开 GET：`/store/home`、`/store/categories`、`/store/brands`、`/store/products`、`/store/products/{product_id}`。
- 只返回 ACTIVE Product、Brand、Category 和 ACTIVE SKU。Product 须同时关联 ACTIVE Brand/Category 且至少有一个 ACTIVE SKU；SKU 零库存不隐藏。
- `StoreSkuView.is_salable` 为布尔值，`available_stock > 0` 才为 true；商品摘要同步返回 `is_salable`，任一 ACTIVE SKU 可售时才为 true。全 SKU 售罄的商品仍可浏览。
- 只有 READY/PUBLIC 且 purpose 符合使用场景的文件可进入投影。响应不得包含 `file_id`、管理 `status/version`、DRAFT/INACTIVE/ARCHIVED 记录、physical/locked 库存或其他管理字段。
- Banner PRODUCT target 必须仍能解析为当前公开 Product；失效 target 不修改 Banner 事实，只从首页公开投影排除。

### 3.2 查询与排序

- `/store/products.keyword` 只对 trim 后的 Product 名称进行大小写不敏感匹配，长度 1-200；不搜 SPU code、SKU code、品牌或分类名。
- 默认 `COMPREHENSIVE`：`is_hot DESC,is_new DESC,sales_count DESC,published_at DESC NULLS LAST,product_id ASC`。
- `HOT`：全生命周期支付成功数量减成功退款数量的净销量 DESC，`product_id ASC`。`NEWEST`：首次 `published_at DESC NULLS LAST`，`product_id ASC`。
- `PRICE_ASC/PRICE_DESC`：取公开 ACTIVE SKU 最低 `retail_price`，以 `product_id ASC` 打破平局。
- `/store/brands` 和 `/store/categories` 不接受 pagination，返回全部 ACTIVE 记录，固定 `sort_order ASC,id ASC`。

### 3.3 首页分区

`/store/home` 返回 `banners/categories/hot_products/new_products` 四个数组，以及对应的 `READY|UNAVAILABLE` 闭合分区状态。单区失败时该数组固定为空且总体响应仍为 200；四区全失败时才返回 500。

| 分区 | 上限 | 选取与排序 |
|---|---:|---|
| Banner | 10 | 当前有效公开投影，`sort_order ASC,id ASC` |
| 分类 | 8 | ACTIVE，`sort_order ASC,id ASC` |
| 热销 | 4 | 公开且 `is_hot=true`，净销量 DESC，`product_id ASC` |
| 新品 | 4 | 公开且 `is_new=true`，`published_at DESC NULLS LAST,product_id ASC` |

### 3.4 匿名限流

- 5 个 Store 公开 GET 共享 Redis 固定窗口：每个 HMAC 来源 IP 每 60 秒 120 次。
- 只保存不可逆 IP HMAC，不保存、记录或追踪原始 IP。
- 超限返回 429 和精确整数秒 `Retry-After`；Redis 超时、断连或脚本错误时 fail closed，不得降级为不限流。
- Express 默认不信任代理头；`API_TRUSTED_PROXY_CIDRS` 留空时固定 `trust proxy=false`。部署在反向代理后时只登记直接可信代理的明确数字 IP/CIDR，不接受 hostname、带 host bits 网段、全网段或语义重复项。
- 来源地址和可信代理网段统一规范化；IPv4-mapped IPv6 归一化为等价 IPv4 后再计算 HMAC，避免同一来源分裂为多个限流桶。未经显式 CIDR 信任的 `X-Forwarded-For` 不得影响来源 IP。

## 4. 实施批次

| 批次 | 范围 | 退出条件 | 当前状态 |
|---|---|---|---|
| B6.0 | CH-013/CH-014，MVP/PRD/API/架构/风险/原型、OpenAPI、生成 contracts、专项契约门禁 | Redocly、统计、闭合 Schema、生成漂移、静态原型、Prisma/冻结 DB/diff 通过，`P0=0/P1=0` | 已完成；用户已批准并完成 B6.1 |
| B6.1 | 独立 `StoreCatalogRepository` 与 Nest Store Catalog 模块，5 个公开 GET，限流 | PostgreSQL/Supabase rollback-only 覆盖快照、投影、排序、失效素材/Banner、分区降级和 429 | 已完成本地与受控 Supabase rollback-only 验收并暂停 |
| B6.2 | MP-01 至 MP-05：首页、分类、搜索、详情、SKU 选择层 | H5/MP-Weixin 构建，5 视口及 loading/empty/404/429/500/局部失败/慢请求通过 | 已完成本地验收并暂停，`P0=0/P1=0` |
| B6.3 | MP-06 版本化游客本地购物车、详情回源校验与登录提示 | 合并、数量上限、价格/库存/状态刷新、网络失败不误判、不调服务端 Cart/订单 | 已完成本地验收并暂停，`P0=0/P1=0` |
| B6.4 | 全量回归、真实 H5 browser -> Nest -> PostgreSQL/MinIO 纵向链路、普通 CI 与 Supabase rollback-only | 同一最终 SHA 双绿、无 fixture/对象/限流残留、`P0=0/P1=0` | 未开始 |

## 5. B6.1 实施与验收结果

- 已开放 `GET /store/home`、`GET /store/categories`、`GET /store/brands`、`GET /store/products` 和 `GET /store/products/{product_id}`。5 路由保持匿名只读，不引入 CUSTOMER session、收藏、服务端购物车、结算或订单写入。
- 独立 `StoreCatalogRepository` 在 Repeatable Read 中执行参数化列表/详情查询，以 ACTIVE SKU 最低价和闭合排序完成稳定分页，再按 ID 集合批量装配 Brand、Category、图片、SKU 与库存；查询次数不随结果条数增长。
- 公开投影实测排除非 ACTIVE Product/Brand/Category、没有 ACTIVE SKU 的 Product、非 READY/PUBLIC/正确 purpose 素材及无效 Banner target；零库存和缺失 `inventory_balance` 均 fail safe 为不可售，仍保留符合条件的 Product/ACTIVE SKU 可见性。
- 共享 Redis Lua 固定窗口使用 Redis 服务端时间，5 路由共用每 HMAC 来源 IP 120 次/60 秒预算；超限返回 1-60 秒动态 `Retry-After`。Redis 未注入、未 ready、断连、脚本异常或非法结果均返回 500，不降级放行；B2 既有登录/MFA `RATE_LIMITED/REAUTH_LOCKED` 900 秒行为未改变。
- `API_TRUSTED_PROXY_CIDRS` 默认空，不信任任何代理转发头；配置只接受最多 20 个明确数字 IP/CIDR，并使用规范网络地址，IPv4-mapped IPv6 同时在配置和请求来源处归一化为 IPv4。

| 验证 | 当前实测 |
|---|---|
| Store repository unit | 9 passed |
| database 全包 | 332 passed / 52 环境模式跳过 |
| 隔离 PostgreSQL full | 1 passed / 1 mode skip；事务外 fixture 0 |
| 受控 Supabase development rollback-only | 1 passed / 1 mode skip；事务外 fixture 0 |
| API 全包 | 426 passed / 26 环境模式跳过 |
| config | 67 passed |
| Store/API 聚焦回归 | 86 passed；包含可信代理与 HTTP 限流边界 |

上述证据只支持 B6.1 development 批次暂停。B6.4 尚未接入本批普通 CI、Supabase workflow 或真实 H5 browser -> Nest -> PostgreSQL/MinIO 纵向链路，也尚无同一最终实现 SHA 的双绿证据。

## 6. B6.2 实施与验收结果

- 新增基于 `uni.request` 和 generated contracts 的 Store API 客户端，覆盖 5 个匿名 GET、取消请求、标准错误信封、精确 `Retry-After` 与跨端 base URL 校验。H5 固定使用 `/api/v1`；MP-Weixin 仅允许本地 HTTP 或非本地 HTTPS，构建产物不依赖 WHATWG `URL` 全局。
- MP-01 首页实现 Banner、分类、热销、新品四个独立分区，按 `READY/UNAVAILABLE` 显示局部失败；只有整页失败才进入整页错误。Banner 支持 PRODUCT、CATEGORY、受控 HTTPS URL 和 NONE 跳转边界。
- MP-02 分类和 MP-03 搜索使用服务端五种排序、品牌/分类筛选、稳定分页、请求取消与代次保护。搜索只提交 trim 后 1-200 个 Unicode code point 的商品名关键字；本地历史最多 8 条、大小写不敏感去重。下一页失败保留已加载结果并局部重试。
- MP-04 详情只展示公开 API 数据，覆盖图片解码失败、404、429、500 和慢请求；MP-05 SKU 选择层展示全部 ACTIVE SKU，售罄规格仍可见但不可提交，数量上限为 `min(99, available_stock)`。
- SKU 选择确认只更新当前页面选择并关闭选择层，不写本地或服务端购物车。收藏、立即购买和“我的”只显示登录提示；没有伪造 CUSTOMER 会话或业务成功。
- H5 在宽屏继续采用最大 414px 的居中小程序信息架构，没有臆造桌面端页面。375/390/414/1024/1440 五视口均验证无横向溢出，分类、搜索、详情与 SKU 选择层未出现文字裁切或控件重叠。

| 验证 | 当前实测 |
|---|---|
| miniapp unit | 4 files / 58 passed |
| miniapp typecheck 与 scoped lint | 通过 |
| H5 build | 通过 |
| MP-Weixin build | 通过；构建产物未发现 `new URL` 依赖 |
| B6 UI Playwright | 19 passed / 36 designed skips；MP-01 至 MP-05 成功路径和无横向溢出覆盖 375/390/414/1024/1440，详细状态矩阵在 390 视口执行 |
| 交互与视觉 | loading、empty、404、429、500、局部失败、慢请求、下一页失败、售罄 SKU、登录提示、图片解码失败和重复交互通过 |
| 契约与数据库 | B6.2 未修改 OpenAPI、Prisma 或 `0001_initial` |
| 独立审查 | `P0=0/P1=0` |

上述证据是 B6.2 的历史退出门禁；当前 B6.3 实施结果见下一节。

## 7. B6.3 实施与验收结果

- 新增版本化游客本地购物车，按 `sku_id` 合并同一规格，不同 SKU 保持独立行；保存数量、选择状态和最小展示快照，不创建 CUSTOMER 会话或服务端购物车。
- 商品详情的游客加购写入本地购物车。每次进入 MP-06 时按 `product_id` 去重并顺序回源公开详情，刷新价格、库存和可售状态，避免同商品多 SKU 重复请求及突发匿名限流。
- 公开详情明确返回 404，或回源成功但目标 SKU 已不存在时，才将购物车项标记为失效；SKU 零库存/不可售时标记售罄。失效与售罄项均不计入选择合计。
- 429、网络失败及其他临时回源失败保留购物车项和本地快照，不误判为下架或售罄；界面提供明确刷新失败状态和重试入口。
- 有效项数量上限为 `min(99, available_stock)`；支持选择、全选、数量变更和删除。结算只提示登录，保留购物车，未调用服务端 Cart 或订单接口。

| 验证 | 当前实测 |
|---|---|
| miniapp unit | 6 files / 84 passed |
| miniapp typecheck 与 scoped lint | 通过 |
| H5 build | 通过 |
| MP-Weixin build | 通过 |
| B6 UI Playwright | 34 passed / 56 designed skips；覆盖 375/390/414/1024/1440 五个视口 |
| 购物车边界 | 同 SKU 合并、不同 SKU 分行、价格/库存/状态刷新、404/缺 SKU 失效、售罄排除合计、429/网络失败保留、结算登录提示及零 Cart/订单请求通过 |
| 全仓工程门禁 | `pnpm check` 与 `git diff --check` 通过；lint 0 errors，保留既有 warnings |
| 契约与数据库 | B6.3 未修改 OpenAPI、Prisma 或 `0001_initial`；`db:check-frozen` 逐字节通过 |
| 退出审查 | `P0=0/P1=0` |

上述证据只关闭 B6.3 本地退出门禁。B6.4 的真实 H5 browser -> Nest -> PostgreSQL/MinIO 纵向链路、普通 CI、Supabase rollback-only workflow 与同一最终实现 SHA 双绿均未开始；CH-013 继续生效，B6 不得标记最终 development `GO`。

## 8. 后续实施约束

- 使用独立 `StoreCatalogRepository`，不向管理端 `ProductCatalogRepository`、Banner 或 Inventory repository 混入公开投影逻辑。
- 同一请求在 Repeatable Read 快照中参数化查询分页、最低价和稳定排序，再批量装配图片、SKU、库存、品牌与分类，不使用 N+1。
- 库存与价格必须来自同一快照。缺失 `inventory_balance` 时 fail safe 为不可售，不将缺失记录当作无限库存或虚构 0/0 事实。
- H5 使用 `/api/v1` 代理；MP-Weixin 使用可选 `VITE_MINIAPP_API_BASE_URL`，非本地地址强制 HTTPS。
- 本地购物车以 `sku_id` 合并，数量上限 `min(99, available_stock)`；售罄/失效项不计入合计，结算只提示登录并保留购物车。

## 9. 验收与风险

### 9.1 必测矩阵

- API：公开访问、未知参数、非法 ULID、搜索 1/200 边界、五种排序、分页稳定、售罄商品、隐藏状态、文件失效、Banner 时窗/target、分区失败和 429。
- 数据库：价格/库存同快照、零库存、缺失余额 fail safe、稳定排序、管理字段零泄露与 rollback 后无残留。
- 前端：375/390/414/1024/1440，loading、empty、404、429、500、局部失败、慢请求、重复点击、图片解码和无横向溢出。
- 纵向：真实 H5 browser -> Nest -> PostgreSQL/MinIO，公开图片必须可读/可解码，结束后精确清理 fixture、对象与精确 Redis key。

### 9.2 保留风险

- 搜索模糊匹配和价格排序索引作为 staging 数量级 EXPLAIN 后的 P2；B6 不为此新增迁移。
- B5-C09 的 `inventory_reservation_item` 缺 SKU 前导索引与 ledger `business_id` 非唯一风险保留至订单/交易域，B6 只读不放大该风险。
- 无业务引用 READY/PUBLIC 素材回收与 storage origin allowlist 配置化仍为 P2；公开目录只使用当前有效关系，不按前缀盲删对象。

## 10. 回退与非准入项

CH-014 尚未进入 B6.1 业务实现时，可整体回退文档、OpenAPI、生成 contracts、专项检查与静态原型。进入 B6.1 后使用 Store Catalog 模块路由开关与小程序导航回退，不执行数据库降级，不按对象前缀盲删。

B6 最终最多只能标记 development `GO`。微信登录、CUSTOMER 会话、收藏、服务端购物车、结算、订单、staging、production、真实客户数据和资金链路均未准入。第一次进入 staging 前必须取得外部独立复核，CH-013 不可代替。
